#lang: es

// test_04_punteros.syn
// Comparaciones, bucles y operaciones logicas basicas.
// Solo usa tipos de nodo soportados por el nuevo generador:
// DefinicionFuncion, AsignacionVariable, SentenciaSi, SentenciaMientras,
// SentenciaRetornar, OpBinaria.

funcion principal() -> entero:
    x = 1
    max = 10
    mientras x < max:
        x = x + 1
        si x == 5:
            z = 100
        sino:
            z = 0
    retornar x
