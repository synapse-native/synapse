#lang: es

funcion consumir(t: Tensor) -> nulo:
    // Consumo
    retornar

funcion principal() -> nulo:
    t = crear_tensor(1, 1)
    consumir(t)
    consumir(t) // ERROR ESPERADO: use after move (t ya fue movido en el primer consumir)
