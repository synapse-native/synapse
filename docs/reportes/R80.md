# REPORTE R80 — F12-2 RESUELTA (opción b del Arquitecto): API `kd_agregar_par_n`

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R80 — Implementar la variante de API con longitud explícita para la destilación (decisión del Arquitecto sobre F12-2: opción b), migrar el probe y cerrar la última suite bloqueada de la Fase 12
FASE: 12 (IA Nativa)
MANUAL REFERENCIADO: Manual 7, Sección 3.2 (fine-tuning/adaptación; pipeline de destilación teacher→student); Manual 9 §6.1 (Definition of Done: suites al 100%); regla 5 de la auditoría (modificación de tests con aprobación explícita del Arquitecto — otorgada en esta sesión)
HASH COMMIT: 2be52ef
COMPILACION: gcc -O2 -Wall -Wextra -I. -c nucleo/distillation.c rc=0 SIN warnings (el warning sign-compare introducido y corregido en el mismo ME); probes rc=0
TESTS:
| Probe | Antes | Después |
|---|---|---|
| tests/validate_distillation.c | SEGV 0xC0000005 (bloqueado) | **101/101 PASS** |
| tests/validate_dist_orchestrator.c (regresión, enlaza distillation.o) | 119/119 | **119/119 PASS** |
| tests/validate_federated.c (regresión) | 114/114 | **114/114 PASS** |
COBERTURA: no aplica (módulo runtime C)
MODIFICACIONES DE TESTS: SÍ — aprobadas por el Arquitecto (opción b): `tests/validate_distillation.c` secciones 4/5/7 migran `kd_agregar_par` → `kd_agregar_par_n(..., n=10/8, ...)`; sección 10 usa sesión con `cfg.vocab_size=10` explícito. Secciones 6/8/9 conservan llamadas legacy que casan con su config (ejercitan el contrato antiguo).
MODULARIZACIÓN: ninguna
DETALLE TÉCNICO:
1. `nucleo/distillation.h`: `KDLogitPair.num_logits` nuevo campo (0 = legacy → usar config.vocab_size); prototipo `kd_agregar_par_n`; `KD_VERSION 1→2` (formato de persistencia añade num_logits por par).
2. `nucleo/distillation.c`: cuerpo real movido a `kd_agregar_par_n` (valida n>0; malloc/copy exactos; setea num_logits); `kd_agregar_par` queda como wrapper legacy (vs = config.vocab_size). `kd_paso_destilacion` honra longitud por-par en KL, cross-entropy y bucle de gradiente (fallback a vs si num_logits==0). `kd_guardar`/`kd_cargar` serializan num_logits por par (v2; el loader rechaza nlog<=0).
3. Nota de hallazgo durante el ME: el hook de pre-commit limpia los `.o` de raíz en cada commit ("Artefactos limpiados") — los objetos se reconstruyen desde fuente vía fixture de sesión conftest o compilación directa (comportamiento esperado F3-15/M7-R7; sin deuda).
RIESGOS IDENTIFICADOS: archivos de sesión KD v1 previos quedan invalidados por el bump de versión (loader los rechaza por diseño — roundtrip garantizado solo v2↔v2); no hay consumidores externos del formato (los wrappers kd_* de std/modelo.syn fueron podados en R46).
PRÓXIMO PASO: runners pytest nominales restantes (spawn/channels/listen/raft/discovery/multicast/serialization/transpile/stress — registro R78)
--- FIN ---
