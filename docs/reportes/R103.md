# REPORTE R103 — PARIDAD S1/NATIVO en use-after-move (H-OWN-10b)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: Cerrar H-OWN-10b alineando el compilador nativo al codigo canonico
ERR_MEM_USE_AFTER_MOVE (Manual 2 §9) para uso tras move, en paridad con S1.
FASE: 27 (herramientas de desarrollo) — ME-P1 + ME-P2 (refactor de paridad).
MANUAL REFERENCIADO: Manual 2 §9 (Deteccion de Use-After-Move ->
ERR_MEM_USE_AFTER_MOVE); Manual 2 §8/§10 (semantica/tipos nativos).
HASH COMMIT: 826e329 (ME-P2) / 9c4170f (ME-P1)
COMPILACION: rebuild del compilador nativo (S1->synapse_stage1.exe) requerido
y ejecutado; los artefactos generados (_diagnostics.c, synapse_stage1.exe) se
regeneran con el codigo renombrado.
TESTS: tests/integration/test_fase2_nativa_hm.py -k "r14 or r15" -> 10 passed
(nativos, ahora esperan E-504/ERR_MEM_USE_AFTER_MOVE). tests/integration/
test_ownership_10.py -> 21 passed (S1, sin cambios, sigue 21/21).
COBERTURA: N/A
MODIFICACIONES DE TESTS: SI — test_fase2_nativa_hm.py actualizado (E-501 ->
E-504 / ERR_SEM_VAR_MOVIDA -> ERR_MEM_USE_AFTER_MOVE) APROBADO por el
Arquitecto (regla 5, tests inmutables), en el cierre de H-OWN-10.
MODULARIZACION: ninguna.
RIESGOS IDENTIFICADOS: ME-P3 pendiente (limpieza de ERR_SEM_VAR_MOVIDA=22 ya
muerto tras ME-P2, y alinear mensaje S1 para quitar "por lanzar/concurrencia").
PRÓXIMO PASO: ME-P3 (opcional) o pasar a la siguiente deuda.
--- FIN ---

## 1. ME-P1 (9c4170f) — Unificar codigo canonico (aditivo)

- nucleo/errores.syn, nucleo/diagnostics.syn: renombrar ex-ACCESO_MEMORIA_MOVIDA
  (23) -> `ERR_MEM_USE_AFTER_MOVE` (con cita `// cumple Manual 2 §9`).
- compilador/generator/generator.py: mapeo `"ERR_MEM_USE_AFTER_MOVE": 23`
  (round-trip S1<->nativo).
- synapse_lsp/features/diagnostics.py: `ERR_MEM_USE_AFTER_MOVE` en
  `_CODIGOS_OWNERSHIP` (marcador [ERR_LIFETIME]).
- No se toco la emision ni los tests -> todos los tests previos en verde.
- Validado: `py_compile` OK; test_lsp_f12 19 passed; build nativo exitoso
  (_diagnostics.c regenerado con el nuevo nombre).

## 2. ME-P2 (826e329) — Cambiar emision nativa + tests inmutables

- nucleo/analizador_semantico.syn:679: `sem_error(est, ERR_SEM_VAR_MOVIDA,...)`
  -> `sem_error(est, ERR_MEM_USE_AFTER_MOVE,...)`; texto "(E-501)" -> "(E-504)".
- test_fase2_nativa_hm.py: 0 referencias E-501/ERR_SEM_VAR_MOVIDA restantes;
  assertions R14/R15 ahora esperan E-504/ERR_MEM_USE_AFTER_MOVE. Las `not in`
  (casos validos) siguen correctas.
- Validado: rebuild nativo (ejecutable 19:13:30); R14/R15 -> 10 passed;
  ownership S1 21/21.

## 3. Resultado de paridad

Ambos compiladores (S1 string, nativo entero) reportan `ERR_MEM_USE_AFTER_MOVE`
para uso tras move. Se cumple Manual 2 §9 en ambos. Herramientas/LSP coherentes
(mismo codigo canonico). Hallazgo H-OWN-10b CERRADO (paridad alcanzada).
