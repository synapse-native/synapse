# REPORTE R123 — ME-4 PILOTO: oráculos reales en test_transpile.py / test_rag.py + fix transpiler

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: ME-4 (PILOTO) — reemplazar `pytest.skip` interinos (ARQ-2026-08-27) por asserts de salida real en dos módulos, y pagar la deuda surfaced.
FASE: 27 / deuda ME-4
MANUAL REFERENCIADO: Manual 7 §2.3 (mapeo de tipos, oráculos), Manual 7 §7 (transpilación Python→Syquex, "código generado compila")
HASH COMMIT: 2fb58bd
COMPILACIÓN: no aplica a producción C; el transpiler es Python (`opensyn/transpiler.py`).
TESTS:
  - tests/opensyn/test_transpile.py: 3/3 PASS (test_router_syn_existe + test_transpilar_python_funcion [compila .syq con compilar_texto, 0 errores] + test_mapeo_tipos [int→entero, str→texto])
  - tests/opensyn/test_rag.py: 6/6 PASS (paridad declaración/definición synapse_rag_extraer_contexto / synapse_rag_liberar_contexto; RAG_RATIO_INYECCION_DEFAULT 0.3f; presence de synapse_rag_construir_prompt + contexto_archivo)
  - auditoria/verificar_alineacion.py: 0 brechas (a confirmar en gate)
MODIFICACIONES DE TESTS: los 2 módulos pilotos dejan de usar `pytest.skip` y usan oráculos reales (autorizado por ARQ-2026-08-27 / pago de deuda ME-4).
ARCHIVOS: tests/opensyn/test_transpile.py, tests/opensyn/test_rag.py, opensyn/transpiler.py (fix + cita), docs/plan_ME_me4.md
HALLAZGO H-TRANSPILE (crítico-funcional): `opensyn/transpiler.py:_convertir_funcion_def_con_retorno` aplicaba el tipo de retorno a TODOS los parámetros → `def f(x: int) -> str:` generaba `funcion f(x: texto) -> texto:` (int mal mapeado). Superfaced por el oráculo real de ME-4; corregido mapeando el tipo propio de cada parámetro (Manual 7 §2.3). El content-sniff previo lo ocultaba.
DEUDA RESTANTE: ~98 tests de ME-4 aún con `pytest.skip`; literals "REGLAS DE SYNAPSE"/"INSTRUCCION" no implementadas en synapse_rag.c (deuda de FEATURE). Al cerrar ME-4 se elimina ARQ-2026-08-27 de la bitácora.
PRÓXIMO PASO: continuar conversión incremental del resto de módulos ME-4 (test_ia_adv_10, test_axon_*, etc.) con plan_ME_me4_<mod>.md.
--- FIN ---
