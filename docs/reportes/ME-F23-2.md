--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: ME-F23-2 — rc<T> y arc<T> reference counting (Manual 4 §3.2-3.3)
FASE: 23 — Modelo de memoria SyQuex
MANUAL REFERENCIADO: Manual 4 §3.1 (cuándo usar RC: escape scope, no threads); Manual 4 §3.2 (RcHeader/ArcHeader structs + 6 funciones rc/alloc/inc/dec/arc); Manual 4 §3.3 (move semantics: origen invalidado, NO deep promotion a arc)
HASH COMMIT: 172cd8b

COMPILACIÓN:
  synapse_rt_types.h — +RcHeader struct (ref_count/weak_count/data/destructor no atómico)
  +ArcHeader struct (ref_count/weak_count/data/destructor) + WeakRef typedef
  +6 declaraciones (rc_alloc/rc_inc/rc_dec/arc_alloc/arc_inc/arc_dec)
  runtime/core/memory.c — +rc_alloc/ rc_incrementar/ rc_decrementar/ arc_alloc/
  arc_incrementar/ arc_decrementar (layout [Header][data], pointer-math para
  recuperar header, __atomic para arc, destructor opcional, NULL safety)

TESTS:
  tests/test_rc_arc.c + tests/syquex/test_rc.py
    test_compila              PASSED
    test_rc_basico            PASSED  (rc_alloc, ref_count=1, datos escribibles)
    test_rc_incrementar_decrementar   PASSED (inc→dec, destructor no al cero)
    test_rc_destructor_al_cero         PASSED
    test_rc_null_safety               PASSED (rc_inc(NULL)/rc_dec(NULL))
    test_rc_move_semantics             PASSED (transfer ownership, src NULL, dst owns)
    test_rc_shared_ownership           PASSED (3 refs, libera al último)
    test_arc_basico                    PASSED
    test_arc_atomic_increment          PASSED (100 incs → ref_count=101)
    test_arc_null_safety               PASSED
    test_arc_liberacion_final          PASSED
    test_no_leaks                      PASSED (0 failed)
  Sub-tests C: 27/27 passed, 0 failed
  Regression: test_arena_scope 4 pass/1 skip, test_memory_snapshots 9/9,
  test_syquex_s1_e2e 3/3

COBERTURA: 12/12 Python tests, 27/27 C sub-tests, 0 regressions

MODIFICACIONES DE TESTS: Tests nuevos creados desde cero.

RIESGOS/RESIDUALES:
  - Move semantics: el source se invalida (src=NULL) sin decrementar; el
    compilador Syquex debe emitir código equivalente. No hay API de "move"
    en C — es patrón manual.
  - arc usa __atomic_fetch_add/sub (no C11 _Atomic) por compatibilidad
    MinGW GCC 10.3. Funciona correctamente.
  - WeakRef struct definido en §4.2 pero la gestión (rc_weak_incrementar,
    weak_upgrade) se implementa en ME-F23-3.

PRÓXIMO PASO: ME-F23-3 — débil<T> + WeakRef API (Manual 4 §4.2)
--- FIN ---
