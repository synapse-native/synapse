# REPORTE R101 — DEUDA ownership: S1 emite ERR_MEM_USE_AFTER_MOVE (Manual 2 §9)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: Resolver deuda tecnica de test_ownership_10.py (7 fallas) alineando el
compilador S1 al Manual 2 §9 para uso tras move.
FASE: 27 (herramientas de desarrollo) — ME-own10 (deuda tecnica).
MANUAL REFERENCIADO: Manual 2 §9 (Deteccion de Use-After-Move ->
ERR_MEM_USE_AFTER_MOVE).
HASH COMMIT: afb628e
COMPILACION: N/A — cambio en compilador S1 (Python); sin build de C.
TESTS: tests/integration/test_ownership_10.py -> 18 passed / 21 (3 fallan por
fixture defectuoso, no por codigo; ver H-OWN-10).
COBERTURA: N/A
MODIFICACIONES DE TESTS: ninguna (tests inmutables, regla 5).
MODULARIZACION: ninguna.
RIESGOS IDENTIFICADOS: (1) H-OWN-10: 3 tests usan `y` como variable pero `y` es
la palabra reservada AND (compilador/lexer.py:20) -> inalcanzables; requieren
aprobacion del Arquitecto para renombrar `y`->`w`. (2) H-OWN-10b: paridad S1/nativo
en el codigo de uso-tras-move (S1=ERR_MEM_USE_AFTER_MOVE, nativo=E-501); opcion C
requiere aprobar modificar ~16 assertions inmutables de test_fase2_nativa_hm.py.
PRÓXIMO PASO: solicitar al Arquitecto aprobacion para renombrar `y` en los 3
tests (H-OWN-10) y, si aplica, para la paridad nativa (H-OWN-10b).
--- FIN ---

## 1. Cambio aplicado

`compilador/semantic_types.py`:
- `:73-78` (inferencia de tipo de Identificador ya movido): emite
  `ErrorCodes.ERR_MEM_USE_AFTER_MOVE` en lugar de `ERR_SEM_VAR_MOVIDA` (E-501).
  Lleva comentario `// cumple Manual 2 §9`.
- `:357-362` (argumento ya movido en llamada con transferencia `->`): idem.

Esto hace que el compilador S1 cumpla Manual 2 §9 (el analizador debe abortar
con ERR_MEM_USE_AFTER_MOVE al reusar una variable invalidada por move).

## 2. Validacion

- `auditoria/contrastar.py --plan docs/plan_ME_own10.md` -> rc=0 (oraculo
  existente, citas de cumplimiento presentes, verificar_alineacion 0 brechas,
  verificacion_ME_own10.md con CUMPLE).
- pytest tests/integration/test_ownership_10.py: 18 passed, 3 failed.
- Los 3 fallidos (test_use_after_move_en_expresion, test_move_en_lanzar,
  test_move_en_condicion_si) NO compilan porque usan `y` como identificador y
  `y` es AND (reserved). Probe con `y`->`w` confirma que el codigo S1 ya
  detecta ERR_MEM_USE_AFTER_MOVE correctamente en los 3 casos (incluye lanzar
  y cruce de scope en `si`). Por tanto NO es deuda de codigo.

## 3. Hallazgos

- H-OWN-10: fixture de 3 tests invalido (variable reservada `y`). Requiere
  aprobacion del Arquitecto (regla 5) para renombrar. Documentado en
  docs/verificacion_ME_own10.md.
- H-OWN-10b: divergencia de paridad S1/nativo en el codigo de uso-tras-move.
  El nativo mantiene E-501 y sus tests inmutables lo exigen; opcion C (paridad)
  quedaria para aprobacion del Arquitecto.
