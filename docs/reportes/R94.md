--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R94 — Inventario paridad .syn/.syq + fix cuerpo_sino JSON (cierre formal Fase 22)
FASE: 22 — FASE 22 FINALIZADA
MANUAL REFERENCIADO: Manual 1 §3.1 (pipeline backend compartido); Manual 2 §2 L47-54 (EBNF módulo);
Manual 3 §3 (gramática SyQuex); Manual 3 §11.1 (mapeo NODO_SI ↔ SentenciaSi);
Manual 3 §11.2 (preservación de metadatos: archivo, línea, columna)
HASH COMMIT: a05092f

COMPILACIÓN:
  syquex/parser.syn   — sq_bloque_interno: +T_SINO stop + token_esperar condicional
  syquex/traductor.syn — nueva funcion trad_nodo_extra(i) expone ptr_extra
  syquex/syq_json.syn  — sj_ent(0) → sj_ent(trad_nodo_extra(i)) (serializa extra slot)
  scripts/build_syquex_frontend.py → build/syq_frontend.exe (rebuild OK)

TESTS:
  tests/integration/test_paridad_syn_syq.py
    test_paridad_estructural_syn_vs_syq[basic_function]        PASSED
    test_paridad_estructural_syn_vs_syq[struct_and_constant]   PASSED
    test_paridad_estructural_syn_vs_syq[control_flow]          PASSED
    test_paridad_estructural_syn_vs_syq[function_call]         PASSED
    test_paridad_estructural_syn_vs_syq[coincidir_basic]       PASSED
    test_si_sino_paridad                                       PASSED
  Regression:
    tests/integration/test_syquex_r92_variable.py (4 tests)     ALL PASSED
    tests/integration/test_syquex_s1_e2e.py (3 tests)          ALL PASSED

COBERTURA: 6/6 paridad (100%), 4/4 regresión R92 (100%)

MODIFICACIONES DE TESTS: Ninguna. Tests nuevos creados desde cero.

MODULARIZACIÓN:
  Nuevos archivos:
    tests/fixtures/paridad/{basic_function,struct_and_constant,control_flow,
                             function_call,coincidir_basic}.{syn,syq} (10 fixtures)
    tests/integration/test_paridad_syn_syq.py (serializador canónico + tests)
    docs/reportes/R94.md
  Modificados:
    syquex/parser.syn      (sq_bloque_interno: +T_SINO stop + conditional token_esperar)
    syquex/traductor.syn   (+ trad_nodo_extra)
    syquex/syq_json.syn    (serializa ptr_extra → JSON canonical)
    MEMORIA_PROYECTO.md    (FASE 22 FINALIZADA)
    docs/AUDITORIA_ALINEACION_MANUALES.md (fila R94)

RIESGOS IDENTIFICADOS:
  - R94-fix-a: sq_bloque_interno ahora también se detiene en T_SINO.
    Esto permite que sq_si detecte y parsea el bloque sino:. Verificado.
  - R94-fix-b: token_esperar(T_DESINDENTAR) condicional — solo consume
    DEDENT cuando el token actual es DEDENT. Evita consumir T_SINO. Verificado.
  - R94-fix-c: trad_nodo_extra + syq_jsonEmitir serializa ptr_extra → JSON.
    El slot `extra` (índice 7) del SemNodo[] de salida ahora se incluye en el
    JSON plano, permitiendo que puente_canonico reconstruya cuerpo_sino.
    Manual 3 §11.2: la metadata se conserva.
  - LIMITACIÓN: structs con >1 campo → SYQ_JSON_ERROR=-3 (overflow traductor).
    No es regresión — fixture R91 (1 campo) sigue OK. Paridad verificada con
    structs de 1 campo. Requiere ME futuro para multi-field structs.

PRÓXIMO PASO: Cierre formal de Fase 22 — todos los criterios ROADMAP F22
verificados. Fase 22 formalmente cerrada con R94.
--- FIN ---
