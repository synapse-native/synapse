# REPORTE R135 — ME_29_T5 Instalador OpenSyn + fix tipo_de_expr

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: Implementar opensyn/installer.syn con detección de hardware y selección de modelo; fix bug tipo_de_expr en ExprAccesoCampo
FASE: 29 (Detección de Hardware y Gestión de Modelos) — ME_29_T5
MANUAL REFERENCIADO: Manual 7 §2.3 (instalador OpenSyn), Manual 9 §5.2-§5.4 (selección de modelo por VRAM), Manual 2 §3 (tipos struct por nombre)
HASH COMMIT: 183f97a
COMPILACION: pipeline.py opensyn/installer.syn --output tests/fixtures/installer.exe rc=0; exe rc=0, salida contiene Hardware:/Modelo:/Hilos:/Capas GPU:
TESTS: tests/opensyn/test_installer.py::test_installer_opensyn — GREEN (1 passed)
COBERTURA: N/A
MODIFICACIONES DE TESTS: test_installer.py reescrito de TDD RED (pytest.fail) a GREEN (compila + ejecuta verificando salida)
MODULARIZACION: opensyn/installer.syn (nuevo, 122 L), fix en compilador/generator/emit_expressions.py:84-93
RIESGOS IDENTIFICADOS: H-F29-T5b (bug RAII preexistente en concat() sobre CadenaSegura retornada por FFI; workaround en installer: literales separados). ME_29_T1/T2/T3 siguen RED (TDD)
PRÓXIMO PASO: ME_29_T5u (ME_29_T5-u): instalar modelo descargado + generar config.toml (Inno Setup / .deb / .dmg, Fase 30)
--- FIN ---

## 1. Evidencia

- `opensyn/installer.syn`: 122 líneas, estructuras `HardwareInfo`/`ModeloInfo`, detección HW vía `externo _syn_*` (memoria_total, vram_total, cpu_nucleos, arquitectura), selección de modelo por VRAM (`<4GB deepseek-coder-1.3b`, `<8GB codellama-7b`, `<12GB codellama-13b`, `sino codellama-34b`), selección de hilos (`<=4→2`, `<=8→4`, `sino→8`) y capas GPU (`12`/`35`).
- `compilador/generator/emit_expressions.py`: fix H-F29-T5a en `tipo_de_expr` para `ExprAccesoCampo` — acepta tipos Synapse puros sin prefijo `struct ` (Manual 2 §3).
- Salida del ejecutable: `Hardware:` / `Modelo:` / `Hilos:` / `Capas GPU:` / `deepseek-coder-1.3b-q4_k_m` / `2` / `0`.
- 1/1 test GREEN en 1.36s.
