# F1.5 / F1.6 — CIERRE DE TESTS UNITARIOS LEXER/PARSER Y CRITERIO DE EJEMPLOS DEL MANUAL 2

- **Fecha:** 2026-08-09
- **Fase:** 1 (Lexer y Parser — Manual 2)
- **Checklist:** 1.5 (tests unitarios lexer/parser, válidos e inválidos) y 1.6
  (criterio: ejemplos del Manual 2 tokenizan; errores con ubicación precisa)
- **Criterio de aceptación (Manual 2 §12):** `pytest tests/unit/test_lexer.py -v`
  y `pytest tests/unit/test_parser.py -v` con **100% pass y >95% cobertura** de
  `compilador/lexer.py` y `compilador/parser.py`.

---

## 1. CONTEXTO

El checklist 1.5/1.6 estaba en ⬜. Los tests unitarios existentes (17) solo
cubrían operadores de comparación de la Fase 0:

- `lexer.py`: **71%** cobertura (faltaban cadenas/escapes, comentarios,
  `@export`, `#lang` multi-idioma, `#pragma: no_std`, INDENT/DEDENT, errores
  léxicos con ubicación).
- `parser.py`: **32%** cobertura (faltaban declaraciones, control de flujo,
  canales, contratos, `coincidir`, recuperar, errores de sintaxis).

Ningún test validaba la ubicación precisa (línea/columna) de los errores ni la
tokenización de los ejemplos del Manual 2.

## 2. EVIDENCIA — LEXER (1.5)

`tests/unit/test_lexer.py` ampliado de 9 → **45 tests** (se conservan los 9
originales). Cobertura medida con `coverage run --source=compilador -m pytest
tests/unit/test_lexer.py tests/unit/test_parser.py`:

| Módulo | Antes | Después |
|--------|-------|---------|
| `compilador/lexer.py` | 71% | **98%** |
| `compilador/parser.py` | 32% | **97%** |

Clases nuevas de lexer (por dominio, Manual 2 §2/§3):

- `TestLexerIdiomaYDirectivas` — `#lang:` en los 6 idiomas (es/en/fr/pt/de/it),
  falta de `#lang:` (error), idioma no soportado, `#pragma: no_std`,
  `#lang:` con código vacío.
- `TestLexerIndentacion` — INDENT/DEDENT, múltiples niveles, indentación no
  múltiplo de 4 (error con línea), indentación inconsistente (error con línea).
- `TestLexerLiterales` — números enteros/flotantes con valor, cadenas con
  escapes (`\n \t \r \\ \"`), comillas simples, cadena sin cerrar (error con
  **línea 2 y columna 4**).
- `TestLexerKeywordsYContextuales` — palabras reservadas de los 6 idiomas,
  keywords contextuales con lexema preservado (`nulo`, `débil` UTF-8), `@export`,
  `@` inválido.
- `TestLexerComentarios` — `//` a línea completa y tras código, directivas `#`.
- `TestLexerOperadoresExtendidos` — bicaracteres (`-> => == != <= >= <-`),
  `|`, `?`, `&`, `*`, `[ ]`, carácter inesperado (error con **línea 2 y
  columna 4**).

## 3. EVIDENCIA — PARSER (1.5)

`tests/unit/test_parser.py` ampliado de 8 → **37 tests** (se conservan los 8
originales):

- `PROGRAMA_EXTENSO` (63 líneas, sintaxis validadas contra el fixture D-5):
  `importar`/`importar_c`/`externo`, `constante` (con y sin tipo),
  `estructura`, `tipo` (alias, ADT y genéricos `<T, E>`), `@export ( c )`,
  contratos `requiere:`/`garantiza:` (bloques), `coincidir` con patrones
  `par(x) =>`, `si/sino`, `mientras` con `siguiente`/`romper`, `para` estilo C,
  canales (`canal(entero, 4)`, `ch <- x`, `x = ch ->`, `x: entero = ch ->`,
  `escuchar ch -> f(x)`), `recuperar`, `lanzar`, `let` (3 formas), `delegar`,
  `retornar -> x` (transferencia), `inseguro` + `asm`, `log(...)`,
  asignación de campo, puntero `entero*`, `tensor(2,3)`, índice `t[0]`.
- `TestParserCasosLimite` — ramas defensivas del parser (RHS parentizado
  `tipo X = (a | b)`, genéricos sin cierre, `tipo X |`, pipe final tras
  constructor, `recuperar` sin `:`) — todas con error controlado, sin crash.

## 4. CRITERIO 1.6 — EJEMPLOS DEL MANUAL 2 Y UBICACIÓN DE ERRORES

**Ejemplos del Manual 2 tokenizan** (`TestEjemplosManual2`): el test lee
`docs/manuales/MANUAL 2.md`, extrae los 3 bloques ```synapse (§1, §4.2, §5.2)
y verifica que `Lexer(...).tokenizar()` no lanza. El `#lang:` se prepende
cuando el ejemplo no lo incluye (el Manual 2 §1 exige que todo archivo
comience con `#lang:` en la línea 1).

**Errores con ubicación precisa:**

- Léxicos: `SynapseError.mensaje/.linea/.columna` verificado en los 6 puntos
  de error del lexer (falta `#lang:`, idioma no soportado, indentación
  inválida/inconsistente, cadena sin cerrar, carácter inesperado, `@` inválido).
- De sintaxis: `DiagnosticManager.errores[0]` con `codigo`, `linea` y
  `columna` para `ERR_SYNTAX_UNEXPECTED_TOKEN` (línea 2, columna 4),
  `ERR_SYNTAX_UNEXPECTED_EXPR` (línea 2) y `ERR_SYNTAX_EXPECTED_TOKEN`.

## 5. VALIDACIÓN

- ✅ **82/82 tests** de `tests/unit/test_lexer.py` + `tests/unit/test_parser.py`
  (45 + 37), 0 fallos.
- ✅ **Criterio §12 cumplido**: lexer.py **98%**, parser.py **97%** (>95%).
- ✅ Las 10 líneas restantes sin cubrir son ramas defensivas inalcanzables por
  el dispatch público del parser (p.ej. `_parsear_enviar_canal` con token no
  identificador) — documentadas, no bloquean el criterio.
- ✅ **Regresión**: 163 tests relacionados (test_lexer, test_parser,
  test_semantico, test_borrow_checker, native_lexer_paridad, unit/generator)
  sin regresiones. No se modificó `compilador/` (solo tests).

## 6. DEUDA Y REGISTRO

Sin deuda nueva. El checklist 1.5/1.6 queda **✅ COMPLETADO** en la bitácora.
Los puntos 1.1/1.2 (lexer/parser nativos `nucleo/lexer.syn`/`nucleo/parser.syn`)
se cierran en la verificación formal correspondiente de la Fase 1.

## 7. REFERENCIAS

- `docs/manuales/MANUAL 2.md` §1 (`#lang:`), §2 (EBNF), §3 (tabla multi-idioma),
  §4.1/§4.2, §5.2 (ejemplos), §12 (pruebas obligatorias).
- `docs/AUDITORIA_ALINEACION_MANUALES.md` — checklist 1.5/1.6 (bitácora).
- `compilador/lexer.py`, `compilador/parser.py`, `compilador/diagnostics.py`,
  `exceptions.py` — infraestructura verificada.

HASH COMMIT: **9c3a487** (bitácora se actualiza al commitear; convención
FASE 2-B1).
