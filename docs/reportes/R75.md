# R75 — D-9 canon: marcar D-9 como CERRADA en verificar_alineacion.py

## Hallazgo
El canon de deudas en `auditoria/verificar_alineacion.py` marcaba D-9 como
`PENDIENTE`, pero D-9(d) fue cerrada completamente en R42
(`synapse_rt.c` 7.882 → 1.769 L; `runtime/core/` 20+ módulos).

## Fix
Actualizar `DEUDAS_CANONICAS` en `auditoria/verificar_alineacion.py`:
- Estado: `PENDIENTE` → `CERRADA`
- Resolución: detalle de los 6 sub-cortes (a) parser.syn R29, (b)
  lexer_keywords.syn R32, (c) emit_selfhost.py R33, (d) synapse_rt.c R42.

## Validación
- `python auditoria/verificar_alineacion.py` → 0 brechas.

## Trazabilidad
- **Manual 9** §9.7 (trazabilidad de deudas).
- **Regla 11** (cero deuda — D-9 cerrada en canon).

## Deuda R3 (pendiente, sin modificar tests)
R3 (`codegen parámetros ADT`) sigue PENDIENTE. El codegen S1 y S2 emite
correctamente el struct C especializado en parámetros ADT instanciados
(`Resultado<entero,texto>` → `Resultado_entero_texto`). Sin cambios de
código ni tests nuevos (regla: no modificar tests existentes).
