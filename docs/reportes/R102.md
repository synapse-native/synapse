# REPORTE R102 — CIERRE H-OWN-10: renombre de fixture aprobado (21/21)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: Cerrar H-OWN-10 renombrando la variable reservada `y` en los 3 tests de
fixture defectuoso de test_ownership_10.py (aprobacion del Arquitecto, regla 5).
FASE: 27 (herramientas de desarrollo) — ME-own10 (cierre).
MANUAL REFERENCIADO: Manual 2 §3 (palabras reservadas: `y`/`and` = AND);
Manual 2 §9 (ERR_MEM_USE_AFTER_MOVE).
HASH COMMIT: d398842
COMPILACION: N/A — solo renombre de identificador en tests.
TESTS: tests/integration/test_ownership_10.py -> 21 passed / 21 (era 14/21
antes de ME-own10, 18/21 tras el fix de codigo afb628e).
COBERTURA: N/A
MODIFICACIONES DE TESTS: SI — renombre `y`->`w` en 3 tests, APROBADO por el
Arquitecto (regla 5: tests inmutables solo con aprobacion expresa).
MODULARIZACION: ninguna.
RIESGOS IDENTIFICADOS: H-OWN-10b (paridad S1/nativo en el codigo de
use-after-move) sigue abierto y requiere aprobacion del Arquitecto para la
opcion C (crear ERR_MEM_USE_AFTER_MOVE en nucleo/*.syn y modificar ~16
assertions inmutables de test_fase2_nativa_hm.py).
PRÓXIMO PASO: si el Arquitecto lo aprueba, abordar H-OWN-10b (paridad nativa).
--- FIN ---

## 1. Cambio aplicado

tests/integration/test_ownership_10.py: renombre `y` -> `w` en:
- test_use_after_move_en_expresion: `y = x + 1` / `retornar y` -> `w = x + 1` / `retornar w`
- test_move_en_lanzar: `y = x` -> `w = x`
- test_move_en_condicion_si: `y = x` -> `w = x`

`y` es la palabra reservada AND (compilador/lexer.py:20), por lo que los tests
eran inalcanzables. Tras el renombre, el compilador S1 detecta
ERR_MEM_USE_AFTER_MOVE en los 3 casos (incluye lanzar y cruce de scope en `si`).

## 2. Validacion

- pytest tests/integration/test_ownership_10.py: 21 passed, 0 failed.
- verificar_alineacion.py: 0 brechas (el rename de test no introduce deuda).
- docs/verificacion_ME_own10.md actualizado: H-OWN-10 CERRADO (21/21).
- MEMORIA_PROYECTO.md actualizada: H-OWN-10 CERRADO.
