--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R95 — ME-1 (parser constants consolidation) + ME-2 (ABI alignment) + ME-3 (FFI pointer marshaling) + ME-9 (method/ctor detection via valor_int marker) + ME-10 (type resolution for receiver)
FASE: 22 — SyQuex backend + pipeline S1
MANUAL REFERENCIADO: Manual 1 §3.1/§3.2 (pipeline backend compartido);
  Manual 2 §7 (NodoID/TokenID canónicos); Manual 3 §3/§6.1/§6.2/§6.3/§9.1/§9.3/§11.1/§11.2;
  Manual 4 §4.2 (lifetime/safety); Manual 5 (SemNodo struct); Manual 6 §1.2/§1.3 (ABI traductor)
HASH COMMIT: 3e83fe2

COMPILACIÓN:
  nucleo/parser_constantes.syn — NODO constants 54-58 (NODO_INTENTO, NODO_LISTA_LIT,
    NODO_MAPA_LIT, NODO_PARA_EN, NODO_BLOQUE_SQ) consolidados como fuente única (ME-1).
  syquex/expr.syn — importa NODO_* de parser_constantes.sin (ME-1); removed local dup.
  nucleo/ast_abi.sin — AST_ABI_NODOS_CAMPOS=10; ast_abi_verificar() chequea 54-58;
    comentario de sección corregido (ME-2).
  runtime/core/ast_nodos.h — NODO_BLOQUE_SQ=58, NODO_PUNTERO=36 added (D-9(e) sync).
  syquex/traductor.syn — trad_funcion(): cuando deco_struct>0, marca valor_int=deco_struct
    (struct node idx) para métodos y __init__ constructores (ME-9, Manual 3 §6.1 es_metodo).
    Llamada a trad_funcion(m, d) siempre pasa struct idx (ME-9).
  compilador/puente_canonico.py — _build_contexto: detecta métodos/constructores vía
    valor_int>0 (no partition("_")); fallback prefijo-largo para fixtures legacy (ME-9).
    Popula _var_tipo desde params de función y retornos de llamadas (ME-10).
    _tipo_objeto: resuelve a.b.c campos de struct via _struct_campos (ME-10).
    t==36 (NODO_PUNTERO) → ExprObtenerDireccion (ME-3, Manual 3 §9.1/§9.3).
  compilador/generator/* — codegen &texto zero-copy: .datos arena + nulo terminator
    (ME-3, Manual 3 §9.3); assert lifetime en safe_mode (Manual 4 §4.2).

TESTS:
  tests/syquex/test_structs.py — ME-9: struct Mi_Struct con método obtener_x()
    (nombre con _ no rompe partición); PASSED.
  tests/syquex/test_ffi_marshaling.py — ME-3: &texto FFI strlen zero-copy; PASSED.
  tests/syquex/test_concurrency.py — stub ME-4 (escuchar c ->), PENDIENTE.
  tests/syquex/test_result.py — stub ME-8 (intentar/atrapar), PENDIENTE.
  Regression (SyQuex + bridge + ABI + traductor): 120 passed, 3 skipped.

COBERTURA:
  ME-1:  parser_constantes.sin 131 constantes (73 T_*, 58 NODO_*), ast_nodos.h sync OK.
  ME-2:  abi_verifier 0 brechas; NODOS_CAMPOS_ABI=10; self-check PASA.
  ME-3:  test_ffi_marshaling 5/5 PASSED; test_compila_hasta_exe PASSED.
  ME-9:  test_structs 1/1 PASSED; test_metodo_call_lowering_h_r90_5 PASSED (compat).
  ME-10: param.metodo() tipo resuelto; x = factory() tipo inferido; a.b.metodo() field type.

MODIFICACIONES DE TESTS: Ninguna modificada. Tests nuevos creados (ME-12).

MODULARIZACIÓN:
  Nuevos archivos:
    tests/syquex/test_structs.py (ME-9)
    tests/syquex/test_concurrency.py (ME-4 stub, ME-12)
    tests/syquex/test_result.py (ME-8 stub, ME-12)
    tests/fixtures/test_ffi_marshaling_txt.syq (ME-3 fixture)
    docs/reportes/PLAN_ALINEACION_FASE22.md
    docs/reportes/INFORME_AUDITORIA_FASE23.md
  Modificados:
    nucleo/parser_constantes.syn (ME-1: +NODO_BLOQUE_SQ, +NODO_PUNTERO)
    syquex/expr.sin (ME-1: import NODO_* instead of local dup)
    nucleo/ast_abi.sin (ME-2: AST_ABI_NODOS_CAMPOS=10, verifier checks 54-58)
    runtime/core/ast_nodos.h (D-9(e) sync: +NODO_BLOQUE_SQ, +NODO_PUNTERO)
    syquex/traductor.sin (ME-9: valor_int marker for methods + __init__)
    compilador/puente_canonico.py (ME-3/ME-9/ME-10: pointer→ExprObtenerDireccion,
      method detection via valor_int, type resolution)
    compilador/generator/emit_expressions.py (ME-3: &texto zero-copy codegen)
    compilador/generator/emit_declarations.py (ME-3: externo &T params)
    compilador/generator/context.py (ME-3: .datos arena + nulo term)
    compilador/generator/generator.py (ME-3: codegen dispatch)
    tests/integration/_synapse_shared.h (pre-existing D-9(e) sync comment)
    tests/integration/test_cluster_handshake.c (pre-existing D-9(e) sync comment)
    auditoria/lecturas.jsonl (lecture registrations for ME-1..10)

RIESGOS IDENTIFICADOS:
  - ME-9-fallback: funciones libres con nombre "Struct_method" y valor_int=0 usan
    fallback de prefijo-largo. No afecta a structs con _ en nombre (prefijo mas largo
    se prueba primero). Verificado con test_structs Mi_Struct.
  - ME-9-compat: __init__ constructor conserva nombre "__init__" pero ahora marca
    valor_int=struct_node_idx. El puente registra _struct_ctores pero la llamada
    al constructor (Mi_Struct(args)) sigue pendiente de H-R90-5 (lowering call-site).
  - ME-3: &texto zero-copy asume que el runtime gestiona la arena .datos + nulo
    terminator. El assert lifetime en safe_mode puede rejectear uso post-free (Manual 4 §4.2).

PRÓXIMO PASO: ME-4 (escuchar c ->), ME-5 (asignación indexada), ME-8 (intentar/atrapar).
--- FIN ---