# REPORTE F1.2c — Activación de `let` (declaración de variable) y `delegar` (propagación de error) en S1/S2/S3

> Micro-entregable de la auditoría de alineación a Manuales v8.1.0.
> Fuente de verdad: `GUIA_DE_GOBERNANZA.md` §PROTOCOLO DE ENTREGA, `docs/AUDITORIA_ALINEACION_MANUALES.md`.
> Fecha: 2026-08-04.

---

## REPORTE DE MICRO-ENTREGABLE

```
--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: F1.2c — activar `let` (declaracion_variable con tipo opcional) y `delegar`
       (propagacion de error, patron `?` de Resultado) en el frontend embebido y el
       codegen S1/S2/S3, con paridad S1 vs S2 y determinismo de bootstrap (diff 0).
FASE: 1 (Manual 2) — D-F1, FASE B (estrategia A en fases, decidida por el Arquitecto).
MANUAL REFERENCIADO: Manual 2, Seccion 2 L132 (`delegar ::= "delegar" expresion`, "Para el
       operador ? en Syquex, traducido a retornar err(...)") y L134 (`declaracion_variable
       ::= "let" IDENTIFICADOR [ ":" tipo ] [ "=" expresion ] NEWLINE`); Manual 2, Seccion
       5.1 (keywords `T_LET`/`T_DELEGAR` multi-idioma) y Seccion 7.2 (`NODO_DELEGAR` 55);
       Manual 3, Seccion 7 (Resultado + operador `?`: si es `err` retorna el error de la
       funcion actual; si es `ok` desempaqueta el valor); Manual 2, Seccion 4.1 L267-268
       (ABI: `entero`/`int` = `int64_t` 8 bytes; `decimal`/`float`/`real` = `double`
       8 bytes -> deuda D-7, ver REGISTRO DE DEUDA); Manual 9, Seccion 9.7 (determinismo:
       diff 0 bytes S2 vs S3). Hito: D-F1 / F1.2 / F1.2c.
HASH COMMIT: f104bce901a3f45b60cf33f943444299ba3485a3
COMPILACION:
  [MODULAR] Link: toolchain_gcc12\mingw64\bin\gcc.exe ... -o synapse_stage1.exe -lpthread -lm -lws2_32
  [OK] Ejecutable modular generado: synapse_stage1.exe
  [OK] AST canonico guardado: nucleo\principal.syn.json
  [OK] Etapa 1 complete: synapse_stage1.exe
  [BOOTSTRAP] Etapa 2: synapse_stage1.exe -> synapse_stage2.exe
  [OK] Etapa 2 complete: synapse_stage2.exe
  [BOOTSTRAP] Etapa 3: synapse_stage2.exe -> synapse_stage3.exe
  [OK] Etapa 3 complete: synapse_stage3.exe
  [BOOTSTRAP] Verificacion: diff 0 bytes (S2 vs S3, Manual 9 S9.7)
  BOOTSTRAP VERIFIED: diff = 0 bytes  |  Etapa 2 == Etapa 3 (byte-identical)
TESTS:
  tests/test_codegen_embebido_d_f1c.py (5 NUEVOS):
    test_canonico_let_delegar_serializable  PASSED (round-trip .syn.json; tipo explicito
                                                 y let sin inicializador preservados)
    test_codegen_s1_let_delegar             PASSED (int x = 5; int edad = 10;
                                                 CadenaSegura s = ...; float suma = 2.5f)
    test_codegen_s1_delegar_patron          PASSED (Resultado _del = f(); if (_del.tag == 1)
                                                 return _del;)
    test_e2e_s1_f12c                        PASSED (programa S1 ejecuta; salida
                                                 ["15","hola","4.000000","6.000000"])
    test_e2e_s2_f12c                        PASSED (S2/S3 nativos == S1; salida identica)
  Suite completa: 703 passed, 9 skipped, 1 xfailed (baseline 698 -> +5, cero regresiones).
COBERTURA: 40% en los modulos del ME (pipeline.py + compilador/generator/* + parser* + canonical +
       lexer.py; core del ME: generator.py 58%, parser.py 59%, lexer.py 71%). 34% global
       incluyendo nucleo/*. Medido con `coverage run -m pytest tests/test_codegen_embebido_d_f1c.py`.
       Deuda D-5 (subir a >=70% en el ME que cierre FASE B) se cumple parcialmente y se
       transfiere a F1.2d.
MODIFICACIONES DE TESTS: ninguna de tests .py modificada (regla 5). 5 tests NUEVOS anadidos
       (endurecimiento). Ningun .c de integracion regenerado en este ME.
MODULARIZACION: ninguna nueva; cambios dentro de los subm-dulos ya modularizados
       (nucleo/generador/orquestador.syn, emision_c.syn, expr_eval.syn, nodos_flujo.syn)
       reensamblados via nucleo/_rebuild_generator.py; espejo del frontend embebido
       regenerado via nucleo/_gen_frontend_p.py (H24 VIVO).
RIESGOS IDENTIFICADOS:
  - `delegar` cubre la GRAMATICA (L132, "traducido a retornar err(...)") y propaga el
    Resultado entero (tag err); el operador `?` POSTFIJO de Manual 3 §7 (L331-342, que
    desempaqueta el valor ok dentro de una expresion) NO se implementa en este ME ->
    deuda D-6 (resolucion: FASE A / Fase 2, ver REGISTRO DE DEUDA).
  - ABI de tipos del Manual 2 §4.1 L267-268: `entero`/`int` deben ser `int64_t` (8 bytes)
    y `decimal`/`float`/`real` `double` (8 bytes); el codebase usa hoy `int`/`float`
    (4 bytes). Deuda ABI SISTEMICA pre-existente, fuera del alcance F1.2c -> deuda D-7
    (resolucion: FASE A, ver REGISTRO DE DEUDA). El ME la detecto y la registra.
  - Keywords pendientes de FASE B: arc/debil/@export y mapeo rc/modulo -> deuda D-F1
    (resolucion: F1.2d / F1.4, ver bitacora).
PROXIMO PASO: continuar FASE B con arc/debil/@export (F1.2d) y ampliar el harness
       (cobertura generator.py >=70% para cerrar FASE B, D-5), luego FASE A (migrar
       frontend embebido al frontend nativo lexer.syn/parser*.syn, resolver D-6/D-7) y
       rc/modulo (F1.4). Ver REGISTRO DE DEUDA.
--- FIN ---
```

---

## CHECK DE PUNTOS RESUELTOS (verificacion con evidencia, 2026-08-04)

Ejecucion real de comprobaciones sobre los puntos que la bitacora da por resueltos. Resultado:
**5/5 PASS**. Ningun punto resuelto se ha encontrado en regresion.

| Punto resuelto | Check ejecutado ahora | Evidencia | Estado |
|---|---|---|---|
| F1.2c parser `let`/`delegar` (S1 + frontend embebido `_P_*`) | `pytest tests/test_codegen_embebido_d_f1c.py` | `test_canonico_let_delegar_serializable` PASSED (round-trip .syn.json con `SentenciaDelegar`, tipo explicito y `let` sin inicializador) | ✅ VERIFICADO |
| F1.2c codegen S1 (inferencia de tipo + patron `?`) | `pytest tests/test_codegen_embebido_d_f1c.py` | `test_codegen_s1_let_delegar` y `test_codegen_s1_delegar_patron` PASSED (`int x = 5;`, `float suma = 2.5f;`, `CadenaSegura s = ...`, `if (_del.tag == 1) return _del;`) | ✅ VERIFICADO |
| F1.2c paridad S1 vs S2 (`LiteralDecimal`) | `pytest tests/test_codegen_embebido_d_f1c.py` | `test_e2e_s2_f12c` PASSED: stage1/2/3 compilan el mismo programa y ejecutan salida idéntica `["15","hola","4.000000","6.000000"]` | ✅ VERIFICADO |
| Checklist 5.2 bootstrap 3 etapas | `build.bat bootstrap-full` | Etapas 1→2→3 completas; S1→S2→S3 generados (commit `f104bce`) | ✅ VERIFICADO |
| Checklist 5.3 determinismo diff 0 | `build.bat bootstrap-full` | `BOOTSTRAP VERIFIED: diff = 0 bytes` (S2 == S3 byte-identical, Manual 9 §9.7) | ✅ VERIFICADO |

---

## REGISTRO DE DEUDA (identificacion, ubicacion y cuando se resuelve)

Regla 9 de la auditoría: *cero deuda técnica — todo hallazgo se resuelve o se registra con
resolución asignada y fecha*. Ninguna deuda se deja sin seguimiento.

| # | Deuda | Ubicación | Impacto | Cuándo se resuelve | Criterio de cierre |
|---|---|---|---|---|---|
| D-6 | Operador `?` POSTFIJO del Manual 3 §7 (L331-342): `delegar` propaga el `Resultado` entero (gramatica L132, "retornar err(...)"), pero `expresion?` dentro de una expresion (desempaquetar `ok` o propagar `err`) NO se implementa: `retornar [ expresion ] [ "?" ]` (L128) y `"?" expresion` (L196) | `nucleo/ast_nodes.syn` (sin nodo `ExprDelegar`/operador), `compilador/parser.py`, `nucleo/generador/nodos_flujo.syn` (`gen_visitar_delegar` solo maneja el nodo sentencia) | Medio: programas Syquex que usan `?` en expresiones (`suma + dividir(i, 2.0)?`, Manual 3 §7.2) no compilan | **FASE A** (frontend nativo `lexer.syn`/`parser*.syn`) y **Fase 2** (tabla de símbolos + Hindley-Milner), hito F1.4/F2.1 | `x = f()?` desempaqueta el valor `ok` y propaga `err`; e2e del ejemplo Manual 3 §7.2 pasa |
| D-7 | ABI de tipos del Manual 2 §4.1 L267-268: `entero`/`int` = `int64_t` (8 bytes) y `decimal`/`float`/`real` = `double` (8 bytes, doble precisión); el codebase usa `int` (4 bytes) y `float` (4 bytes, simple precisión) en `MAPA_TIPOS_C`/`traducir_tipo_c` | `compilador/generator/context.py` (`MAPA_TIPOS_C`), `nucleo/generador/emision_c.syn` (`traducir_tipo_c`), runtime `synapse_rt.c` (`decimal_a_texto` usa `%f`) | ALTO (pre-existente, sistémico): tamaños y rango numérico divergen del estándar ABI del Manual 2; afecta ABI de funciones públicas e interoperabilidad C | **FASE A** (unificación S1/S2 en un único emisor) — registrada ya como ítem 3.6 ⬜ de `docs/AUDITORIA_ALINEACION_MANUALES.md` (mapeo de tipos Synapse→C) | `entero`/`int` generan `int64_t` y `decimal`/`float`/`real` generan `double`; e2e con rangos que exigen 64 bits / doble precisión pasan |
| D-F1 | Keywords del Manual 2 §3 sin mapeo en el lexer: **`let`/`delegar` ✅ RESUELTOS en F1.2c**; pendientes `arc`/`débil`/`@export` (no activados) y `rc`/`modulo` (colisión con identificadores existentes) | `nucleo/tokens.syn`, lexer embebido `_P_*` | BLOQUEANTE para F1.2/1.4 (activación completa de TokenID) | **F1.2d** (`arc`/`débil`/`@export`) y **F1.4** (`rc`/`modulo` + FASE A), Roadmap Fase 1 (2 meses) | TokenID 59–72 activos como keywords reales en el lexer; tests de lexer/parser del Manual 2 §3 pasando |
| D-5 | Cobertura del ME al 40% (core generator.py 58%): faltan casos de `let`/`delegar` en expresiones anidadas, `let` con tipos compuestos y operador `?` en expresiones | `tests/test_codegen_embebido_d_f1c.py` | Bajo: riesgo de regresión no cubierta | **Próximo ME de FASE B (F1.2d)**: ampliar el harness | Cobertura del módulo generador ≥70% en el ME que cierre FASE B |

**Nota de gobernanza:** D-F1 queda **parcialmente resuelta** con F1.2c (`let`/`delegar` activos y
verificados con e2e S1/S2/S3). D-6 y D-7 son deuda nueva detectada en este ME y quedan
registradas con propietario y fecha; D-6 es consecuencia directa del alcance acordado para
F1.2c (solo la gramática de `delegar`), y D-7 es una deuda ABI pre-existente que este ME
confirma contra Manual 2 §4.1 L267-268 y traslada a la FASE A (ítem 3.6 de la bitácora).
Ninguna deuda queda sin seguimiento.
