# REPORTE R78 — Derivación de clave de sesión crypto_kx-equivalente (Fila 19)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R78 — Sustituir la derivación de clave de sesión basada en material público por un intercambio autenticado efímero X25519 + KDF SHA-512 (crypto_kx-equivalente), con pruebas oficiales del Manual
FASE: 19 (CanalRemoto v2) / 8 (Concurrencia Distribuida)
MANUAL REFERENCIADO: Manual 5, Sección 6.2 paso 3 ("Se deriva una clave de sesión usando crypto_kx de libsodium o similar"); Manual 6, Sección 5.3 paso 4 (clave de sesión para cifrar el tráfico, flujo HELLO→HELLO_RESP→DATOS_CIFRADOS); Manual 5 §9 y Manual 6 §9 (pruebas obligatorias test_cluster_remote.py / test_handshake.py)
HASH COMMIT: a7ec2af
COMPILACION: gcc -O2 -Wall -Wextra -I. runtime/core/cluster.c rc=0 (solo 3 warnings preexistentes de parámetros sin uso en funciones ajenas); reconstrucción completa de los objetos runtime vía fixture de sesión conftest rc=0
TESTS:
- tests/integration/test_handshake.py (Manual 6 §9): **4/4 PASS** — unitarios in-process del ECDH (claves idénticas cliente/servidor, clave intrusa distinta, rechazo hex malformado/pk nula)
- tests/integration/test_cluster_remote.py (Manual 5 §9): **3/3 PASS** — e2e real UDP: handshake exitoso + ambas partes derivan la MISMA clave + envío/recepción cifrada (`DATA_OK:kx-payload-42`) + regresión anti-material-público (identidades distintas → claves distintas)
- Regresión cluster/debug: handshake_e2e 6/6 + work_stealing 5/5 + discovery + multicast + raft + nodes + live_migration ×2 + distributed_debug + reversible_debug — **62/62 PASS**; total 108/108 verde
COBERTURA: no aplica (módulo runtime C)
MODIFICACIONES DE TESTS: ninguna (solo archivos NUEVos con los nombres obligatorios del Manual que NO existían)
MODULARIZACIÓN: ninguna
DETALLE TÉCNICO:
1. `runtime/core/cluster.c`: `cluster_kx_par` (pura, testable), `cluster_kx_generar_par` (par efímero global vía `randombytes`+`crypto_scalarmult_base`), `cluster_kx_secreto_compartido` (q=X25519(sk,pk_peer); h=SHA-512(q‖kxpk_cliente‖kxpk_servidor); clave=h[0..31]; orden por ROL; rechaza secreto compartido todo-ceros), `cluster_kx_derivar` (activa `_cluster_sesion_clave`), `cluster_enviar_kx_init`, `cluster_clave_sesion_hex`. Handler `_cluster_procesar_kx_entrante` (KX_INIT firmado por la identidad Ed25519 del cliente vinculada al nonce del HELLO retenida → responde KX_RESP firmado). Prototipos en `cluster.h`.
2. `std/cluster.syn`: `conectar()` extiende el flujo tras HELLO_RESP verificado — genera par efímero, envía KX_INIT firmado sobre `kxpub:nonce`, verifica firma del servidor sobre `kxpub_serv:nonce_serv`, deriva con rol cliente. Externs nuevos documentados. La clave alimenta el cifrado XOR de transporte ya existente (Manual 6 §5.3 "DATOS_CIFRADOS").
3. Fixes de bugs latentes descubiertos por las pruebas nuevas (regla 11):
   a. `recibir_paquete(timeout_ms)` IGNORABA el timeout (warning `-Wunused-parameter` preexistente): un solo recvfrom no bloqueante → el bucle del servidor agotaba iteraciones en milisegundos. Fix: select() con el timeout prometido por la API de std/cluster.syn.
   b. Firmas del servidor usaban los bytes CRUDOS del secret como si fueran hex (128 chars) — `cluster_firmar_mensaje` hace hex-decode; nunca pudo producir una firma válida (el path servidor HELLO_RESP no estaba cubierto por ninguna prueba). Fix: forma hex canónica `_cluster_server_sk_hex[129]` generada en `iniciar_nodo`.
4. Higiene (regla 12): eliminado artefacto huérfano `axon.o` de raíz (stale, nombre correcto actual `axon_rt.o`; 0 referencias).
HALLAZGO SISTEMÁTICO REGISTRADO (resolución asignada): de las pruebas obligatorias por nombre de Manual 5 §9/6 §9/8 §6 solo existían work_stealing y (hoy) las 2 creadas; faltan los runners oficiales `test_spawn/test_channels/test_listen/test_raft/test_discovery/test_multicast/test_serialization/test_transpile/test_cluster_stress` (los probes C existen; faltan los harness pytest nominales). Resolución: siguiente ME de Fase 8/19 — crearlos envolviendo los probes C existentes.
RIESGOS IDENTIFICADOS: el cifrado XOR de transporte usa la misma clave en ambas direcciones sin contador de mensajes (keystream repetible entre paquetes idénticos); hardening futuro registrado: `crypto_secretbox` (XSalsa20-Poly1305, disponible en tweetnacl.c) — resolución asignada al siguiente ME de Fase 19.
PRÓXIMO PASO: Tarea C — cierre de filas PARCIALES 12 (IA Nativa) y 13 (Federated)
--- FIN ---
