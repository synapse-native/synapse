# REPORTE F1.2b — Codegen de `declaracion_tipo` (alias/ADT), `nulo` y `tensor()` en S2/S3

> Micro-entregable de la auditoría de alineación a Manuales v8.1.0.
> Fuente de verdad: `GUIA_DE_GOBERNANZA.md` §PROTOCOLO DE ENTREGA, `docs/AUDITORIA_ALINEACION_MANUALES.md`.
> Fecha: 2026-08-04.

---

## REPORTE DE MICRO-ENTREGABLE

```
--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: F1.2b — codegen de `declaracion_tipo` (alias/ADT), `nulo` y `tensor()` en el generador
       embebido S2/S3 (nucleo/generator.syn) con paridad funcional con el generador de referencia
       S1 (compilador/generator/generator.py).
FASE: 1 (Manual 2) — D-F1, FASE B (estrategia A en fases, decidida por el Arquitecto).
MANUAL REFERENCIADO: Manual 2, Seccion 2 L74-75 (gramatica `declaracion_tipo`); Manual 2,
       Seccion 4.1 L267-274 (`nulo` = ausencia de valor, void; `tensor` nativo IA); Manual 2,
       Seccion 4.2 L279-280 (ADT `tipo Resultado<T,E> = ok(T) | err(E)`); Manual 2, Seccion 4.3
       L194 (`tensor ::= "tensor" "(" expresion "," expresion ")"`); Manual 9, Seccion 9.7
       (determinismo: diff 0 bytes S2 vs S3). Hito: D-F1 / F1.2 / F1.2b.
HASH COMMIT: 44244a46a6acc6e2828c364ef4e78883fd92f805
COMPILACION:
  [MODULAR] Link: toolchain_gcc12\mingw64\bin\gcc.exe ... -o synapse_stage1.exe -lpthread -lm -lws2_32
  [OK] Ejecutable modular generado: synapse_stage1.exe
  [OK] AST canonico guardado: nucleo\principal.syn.json
  [OK] Etapa 1 complete: synapse_stage1.exe
  [BOOTSTRAP] Etapa 2: synapse_stage1.exe -> synapse_stage2.exe
  [OK] Etapa 2 complete: synapse_stage2.exe
  [BOOTSTRAP] Etapa 3: synapse_stage2.exe -> synapse_stage3.exe
  [OK] Etapa 3 complete: synapse_stage3.exe
  [BOOTSTRAP] Verificacion: diff 0 bytes (S2 vs S3, Manual 9 S9.7)
  BOOTSTRAP VERIFIED: diff = 0 bytes  |  Etapa 2 == Etapa 3 (byte-identical)
TESTS:
  tests/test_codegen_embebido_d_f1.py (4 NUEVOS):
    test_canonico_declaracion_tipo_serializable  PASSED (regresion fix ConstructorTipo(Nodo))
    test_codegen_s1_declaracion_tipo             PASSED (typedef alias/ADT, crear_tensor, nulo, hoisting)
    test_e2e_s1_f12b                             PASSED (programa S1 ejecuta; salida 2,3,f12b ok)
    test_e2e_s2_f12b_paridad                     PASSED (S2 nativo == S1; salida identica)
  Suite completa: 698 passed, 9 skipped, 1 xfailed (baseline 694 -> +4, cero regresiones).
COBERTURA: 40% en los modulos del ME (pipeline.py + compilador/generator/* + parser* + canonical +
       lexer.py; core del ME: generator.py 58%, parser.py 59%, lexer.py 71%). 34% global
       incluyendo nucleo/*. Medido con `coverage run -m pytest tests/test_codegen_embebido_d_f1.py`.
MODIFICACIONES DE TESTS: ninguna de tests .py modificada (regla 5). 4 tests NUEVOS anadidos
       (endurecimiento). 3 fuentes C de integracion (tests/integration/_synapse_shared.h,
       _test_cluster_handshake.c, test_cluster_handshake.c) regeneradas automaticamente por
       propagacion del header (_G_tipo_aliases*), sin cambio de aserciones.
MODULARIZACION: ninguna nueva; cambios dentro de los subm-dulos ya modularizados
       (nucleo/generador/orquestador.syn, emision_c.syn, expr_eval.syn) reensamblados via
       nucleo/_rebuild_generator.py.
RIESGOS IDENTIFICADOS:
  - Genericos de ADT (T/E) mapean a placeholder `void*` (sin instanciacion real) -> deuda, ver
    REGISTRO DE DEUDA D-2 (resolucion: FASE A / Fase 2).
  - Keywords pendientes de FASE B: let/delegar/arc/debil/@export y mapeo rc/modulo -> deuda D-F1
    (resolucion: F1.2 / F1.4, ver bitacora).
  - Formato divergente S1 vs S2 en declaracion de variables (Tensor t;/void* p; vs = {0}): ambos
    validos; no afecta al diff 0 (compara S2 vs S3 nativos) -> deuda D-3 (resolucion: FASE A).
  - Contratos requiere/garantiza (regla 4) sin uso en el generador embebido (pre-existente) ->
    deuda D-4 (resolucion: Fase 5).
PROXIMO PASO: continuar FASE B con let/delegar/arc/debil/@export (F1.2c/F1.2d), luego FASE A
       (migrar frontend embebido al frontend nativo lexer.syn/parser*.syn) y resolver rc/modulo
       (F1.4). Ver REGISTRO DE DEUDA.
--- FIN ---
```

---

## CHECK DE PUNTOS RESUELTOS (verificacion con evidencia, 2026-08-04)

Ejecucion real de comprobaciones sobre los puntos que la bitacora da por resueltos. Resultado:
**9/9 PASS**. Ningun punto resuelto se ha encontrado en regresion.

| Punto resuelto | Check ejecutado ahora | Evidencia | Estado |
|---|---|---|---|
| H19–H23 (TokenID, F1) | Suite completa 698 passed | `pytest tests/ -q` → `698 passed, 9 skipped, 1 xfailed` en 16:07 min | ✅ VERIFICADO |
| H24 frontend embebido VIVO — espejo `_G_fp*`/`_G_tk*` | `python nucleo/_gen_frontend_p.py` | `[OK] frontend_p.syn sin cambios (128597 caracteres)` — espejo byte-idéntico a emit_selfhost/emit_expressions | ✅ VERIFICADO |
| H24 reensamblaje `generator.syn` | `python nucleo/_rebuild_generator.py` | Reconstrucción 259151 chars; `git status` limpio tras regenerar → byte-idéntico al commit | ✅ VERIFICADO |
| F1.2 parser `declaracion_tipo`/`nulo`/`tensor()` | `pytest tests/test_frontend_embebido_d_f1.py` + test canónico F1.2b | Harness C pasa; `test_canonico_declaracion_tipo_serializable` PASSED (round-trip .syn.json) | ✅ VERIFICADO |
| F1.2b codegen S2/S3 | `pytest tests/test_codegen_embebido_d_f1.py` | 4/4 PASSED (canónico, codegen S1, e2e S1, e2e S2 paridad) | ✅ VERIFICADO |
| Checklist 5.2 bootstrap 3 etapas | `build.bat bootstrap-full` | Etapas 1→2→3 completas; S1→S2→S3 generados | ✅ VERIFICADO |
| Checklist 5.3 determinismo diff 0 | `build.bat bootstrap-full` | `BOOTSTRAP VERIFIED: diff = 0 bytes` (S2 == S3 byte-identical, Manual 9 §9.7) | ✅ VERIFICADO |
| Regla 10 código muerto | `git status` + listado raíz | `synapse_stage1_f12.exe`, `test_live_migration.exe`, `synapse_unity.c` eliminados; solo stage1/2/3 frescos | ✅ VERIFICADO |
| Fix S1 link `_G_tipo_aliases*` | Bootstrap Etapa 1 + e2e S1 | Link modular OK (antes: `undefined reference`); `test_e2e_s1_f12b` PASSED | ✅ VERIFICADO |

---

## REGISTRO DE DEUDA (identificacion, ubicacion y cuando se resuelve)

Regla 9 de la auditoría: *cero deuda técnica — todo hallazgo se resuelve o se registra con
resolución asignada y fecha*. Ninguna deuda se deja sin seguimiento.

| # | Deuda | Ubicación | Impacto | Cuándo se resuelve | Criterio de cierre |
|---|---|---|---|---|---|
| D-F1 | Keywords del Manual 2 §3 sin mapeo en el lexer: `let`/`delegar`/`arc`/`débil`/`@export` (no activados) y `rc`/`modulo` (colisión con identificadores existentes) | `nucleo/tokens.syn`, lexer embebido `_P_*`; evidencia: `std/cluster.syn:149`, `std/quantum_err_corr.syn:47`, `nucleo/generator.syn:343` | BLOQUEANTE para F1.2/1.4 (activación completa de TokenID) | **F1.2 (siguientes ME de FASE B**: F1.2c `let`/`delegar`, F1.2d `arc`/`débil`/`@export`) y **F1.4 (`rc`/`modulo` + FASE A)**, Roadmap Fase 1 (2 meses) | TokenID 59–72 activos como keywords reales en el lexer; tests de lexer/parser del Manual 2 §3 pasando |
| D-2 | ADT con genéricos (`tipo X<T,E> = ok(T) \| err(E)`) generan campos placeholder `void*`; sin instanciación real de tipos parametrizados | `nucleo/generador/orquestador.syn` (rama ADT de `gen_visitar_top_level`) | Alto: programas que usan `Resultado<entero, texto>` no especializan | **FASE A (migración al frontend nativo)** y **Fase 2** (tabla de símbolos + Hindley-Milner), Roadmap Fase 1→2 (hito F1.4/F2.1) | El codegen de un ADT instanciado con tipos concretos emite campos tipados (no `void*`) y el semántico rechaza usos mal tipados |
| D-3 | Divergencia cosmética de formato S1 vs S2 en declaración de variables sin inicializador: S1 `Tensor t;` / `void* p;`, S2 `Tensor t = {0};` / `void* p = {0};` | `compilador/generator/generator.py` vs `nucleo/generador/emision_c.syn` | Bajo: ambos válidos; el diff 0 de bootstrap no se ve afectado (compara S2 vs S3) | **FASE A** (cuando S1 y el generador nativo se unifiquen en un único emisor) | Un programa idéntico compilado por S1 y S2 produce `.c` byte-idénticos o el diff se documenta como aceptado |
| D-4 | Contratos `requiere`/`garantiza` (regla 4) no emitidos por el generador embebido: `garantizas_nombres`/`garantizas_valores`/`garantizas_total` definidos en `contexto.syn` pero sin uso por ninguna función del generador | `nucleo/generador/contexto.syn:176-178` y todo `generator.syn` | Medio (pre-existente, no introducido por F1.2b): funciones públicas del compilador sin contratos verificables | **Fase 5** (CONTRATOS Y BOOTSTRAP, Manual 4): `nucleo/verificador_formal.syn` — el generador debe emitir aserciones `requiere`/`garantiza` | `requiere`/`garantiza` del código fuente se traducen a aserciones C; checklist 5.1 ✅ |
| D-5 | Cobertura del ME al 40% (core generator.py 58%): faltan casos de ADT con más constructores, alias encadenados y `nulo`/`tensor()` en expresiones anidadas | `tests/test_codegen_embebido_d_f1.py` | Bajo: riesgo de regresión no cubierta | **Próximo ME de FASE B (F1.2c)**: ampliar el harness | Cobertura del módulo generador ≥70% en el ME que cierre FASE B |

**Nota de gobernanza:** las deudas D-F1 (BLOQUEANTE) y D-2 son las únicas que condicionan el avance
de F1.2/1.4; D-3, D-4 y D-5 tienen resolución asignada a fases posteriores y no bloquean el hito
actual. Ninguna queda sin propietario ni fecha.
