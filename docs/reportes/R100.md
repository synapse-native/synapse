# REPORTE R100 — INTEGRACIÓN DEL MÉTODO DE TRABAJO SEGURO (MTS)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: Integrar el mecanismo anti-olvido MTS (docs/METODO_TRABAJO.md) en el
arranque de AGENTS.md, en el mapa de lectura y en el gate de auditoría del
pre-commit (auditoria/contrastar.py).
FASE: 27 (herramientas de desarrollo) — tarea de gobernanza transversal.
MANUAL REFERENCIADO: Manual 1 §2 (Los Pilares / El Pacto — disciplina de
cumplimiento), Manual 2 §12 (pruebas obligatorias — el oráculo del MTS).
HASH COMMIT: ce19055
COMPILACIÓN: N/A — cambios de gobernanza (proceso), sin código de producción.
TESTS: smoke-test de auditoria/contrastar.py — positivo rc=0, negativo
(archivo de producción sin cita) rc=1. verificar_alineacion.py → 0 brechas.
COBERTURA: N/A
MODIFICACIONES DE TESTS: ninguna
MODULARIZACIÓN: ninguna (auditoria/contrastar.py ya existía sin trackear; se
reforzó para evaluar solo archivos staged y para ser robusto en consolas
Windows cp1252 mediante sys.stdout.reconfigure utf-8).
RIESGOS IDENTIFICADOS: el gate MTS solo se dispara cuando el commit incluye un
docs/plan_ME_*.md (progressive rollout); los commits sin plan no se ven
afectados, evitando bloquear trabajo legado que aún no lleva citas // cumple.
PRÓXIMO PASO: aplicar MTS en los próximos MEs de Fase 27 (plan_ME + citas en
código de producción).
--- FIN ---

## 1. Cambios aplicados

- `AGENTS.md`: FUENTES DE VERDAD #5 apunta a docs/METODO_TRABAJO.md; REGLAS
  DURAS incluye la obligación MTS; CICLO pasa a citar el formato de plan MTS y
  añade la fase 4b (gate contrastar antes de integrar).
- `docs/mapa_manuales.md`: nueva sección "Método de Trabajo Seguro (MTS)"
  documentando plan, cita en código y gate; aclara que el comentario
  `cumple Manual X §Y` debe citar la sección ya exigida por el mapa.
- `scripts/githooks/pre-commit`: tras verificar_alineacion, detecta
  `docs/plan_ME_*.md` staged y ejecuta `auditoria/contrastar.py --plan`;
  bloquea el commit si falla. ASCII-safe (sin emojis) para evitar mojibake.
- `auditoria/contrastar.py`: evalúa solo archivos de producción staged
  (`git diff --cached`) para no bloquear trabajo sin stagear ajeno; salida
  UTF-8 robusta en Windows.
- `docs/METODO_TRABAJO.md`: ya existía sin trackear; se integra como fuente de
  verdad (commiteado en ce19055).

## 2. Validación

- `python -m py_compile auditoria/contrastar.py` → rc=0.
- `auditoria/verificar_alineacion.py` → SIN BRECHAS.
- Smoke positivo (plan + .syn con `// cumple Manual 2 §2`) → rc=0.
- Smoke negativo (plan + .syn sin cita) → rc=1 (bloquea, como se espera).
