--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: ME-F23-3 — débil<T> referencias débiles (Manual 4 §4.2)
FASE: 23 — Modelo de memoria SyQuex
MANUAL REFERENCIADO: Manual 4 §4.1 (problema de ciclos — detección estática); §4.2 (WeakRef: {RcHeader* header, uint32_t version}; no incrementa ref_count; se invalida al destruir fuerte); §4.3 (sintaxis SyQuex: débil<T>)
HASH COMMIT: e9b667e

COMPILACIÓN:
  synapse_rt_types.h — +version field en RcHeader/ArcHeader; WeakRef struct +
  6 declaraciones (rc_weak_ref/arc_weak_ref/rc_weak_upgrade/arc_weak_upgrade/
  rc_weak_release/arc_weak_release)
  runtime/core/memory.c — +version=0 en rc_alloc/arc_alloc; rc_decrementar/
  arc_decrementar ahora: ref_count→0 + weak_count>0 → version++ (no free, §4.2);
  +rc_weak_ref/arc_weak_ref (weak_count++, snapshot version); +rc_weak_upgrade/
  arc_weak_upgrade (version check + ref_count>0→inc); +rc_weak_release/
  arc_weak_release (weak_count--, free header si strong también 0)

TESTS:
  tests/test_weak.c + tests/syquex/test_weak.py
    test_compila              PASSED
    test_creation_and_upgrade   PASSED  (weak ref creada, upgrade exitoso)
    test_invalidated_after_free    PASSED (version incremented, upgrade=NULL)
    test_header_survives_weak_refs  PASSED (header no freed mientras weak>0)
    test_arc_weak_ref             PASSED (atomic version + weak_count)
    test_null_safety              PASSED (weak_ref(NULL), upgrade/release NULL)
    test_no_leaks                 PASSED (header freed tras release)
  Sub-tests C: 25/25 passed
  Regression: test_rc 12/12, test_arena_scope 4/5(1 skip ASAN),
  test_memory_snapshots 9/9, test_syquex_s1_e2e 3/3

COBERTURA: 7/7 Python tests, 25/25 C sub-tests, 0 regressions
ME-F23-2 (rc/arc) re-verificado con weak_count>0 path: 0 regressions

MODIFICACIONES DE TESTS: Tests nuevos creados desde cero. Tests existentes no modificados.

RIESGOS/RESIDUALES:
  - Header sobrevive ref_count=0 cuando weak_count>0 → versión++ para invalidar
    WeakRef. El cliente debe liberar weak antes de que el header se libere.
  - arc_weak_ref usa __atomic para weak_count/version (compatibilidad MinGW).
  - La detección ESTÁTICA de ciclos (§4.4) se implementa en ME-F23-5 (analizador_alcance.syq).

PRÓXIMO PASO: ME-F23-4 — syquex/analizador_alcance.syq (Manual 3 §5 + Manual 4 §4.4/§5)
--- FIN ---
