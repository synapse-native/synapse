# REPORTE R87 — FASE 22: `syquex/parser.syn` (parser Syquex)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R87 — Implementar el parser de Syquex según Manual 3 §3 (EBNF completa): OOP (estructura/campo/crear/metodo), enumeracion, funciones clásicas/una-expresión con parámetros por defecto, intentar/atrapar, para..rango+paso (desugar a mientras) y para..en, coincidir con literales/wildcard/patrón ctor, tipos lista/mapa/arena/rc/arc/débil/[T]/&T, export/externo, let/constante/tipo-sinónimo
FASE: 22 (Fundación de Syquex — ROADMAP entregable 2; decisión D-F22-A: frontends Syquex escritos en Synapse)
MANUAL REFERENCIADO: Manual 3 §3 L56-209 (EBNF: declaracion L62-70, funcion L78-79, parametro con default L82, estructura/miembro/campo/metodo/constructor L88-92, enumeracion L95-96, externo/exportacion L99-100, sentencias L104-152, tipos L155-177, expresiones/precedencia L180-208); §6 L276-294 (OOP); §8.1 L372 (`Canal<texto>(10)` uso constructor genérico); §11.1 L441-448 (mapeo a SemNodo: crear→`__init__`, metodo→función con self implícito — pre-mapeado en el parser, patrón D-F22-C); Manual 2 §12 (contratos requiere/garantiza en funciones nuevas); Manual 6 §1.2/§1.3 (ABI canónica — nodos verificados contra nucleo/ast_abi.syn de R85); Manual 9 §9.7 (no aplica bootstrap: F22 aislada, cero cambios en nucleo/compilador/runtime)
HASH COMMIT: b22a37b (código+tests; docs en commit separado con este hash citado)
COMPILACIÓN: e2e S1 rc=0 en ambos drivers (lexer R86 y parser R87; concatenación modular completa, link modular con fallback unity preexistente, 0 warnings nuevos)
TESTS:
  - tests/unit/test_syquex_parser.py **6/6 PASS** (~43 s): parsea_sin_errores (>60 nodos), oop_estructura_crear_metodo (FUNCION>=5: __init__/desplazar/sumar/mitad/visible/principal), enumeracion_y_constructores (DECL_TIPO=2 + CONSTRUCTOR=2 rojo/verde), funciones_especiales (EXTERNO+EXPORT+LET 48), sentencias_nuevas_syquex (INTENTO/PARA_EN/INDICE/COINCIDIR/LANZAR/ESCUCHAR), desugar_para_rango_a_mientras
  - tests/unit/test_syquex_lexer.py **5/5 PASS** (~80 s, regresión R86)
  - Probe interno de 18 constructos atómicos (instrumentación temporal, eliminada): 18/18 OK
COBERTURA: n/a (módulos .syn auto-contenidos; criterio §12 de cobertura aplica al S1 Python)
MODIFICACIONES DE TESTS: tests/unit/test_syquex_parser.py era un artefacto NO commiteado de esta misma sesión R87 (sin estado aprobado previo); se alineó su constante `DECLARACION(34)`→`LET(48)` porque la ABI canónica v1 (R85, nucleo/ast_abi.syn) mapea `let`→NODO_LET=48 y el parser emite NODO_LET. Ningún test ya-aprobado fue modificado. Suite lexer R86 intacta.
MODULARIZACIÓN: syquex/parser.syn (908 líneas, <1200 regla 13 OK); syquex/expr.syn (399 l.); movimiento sq_tokens_hi/sq_tokens_ext desde lexer.syn→parser.syn (H-R87-4: el lexer vuelve a ser autónomo, el puente TokenExt depende de parser_base.syn). El driver de muestra vive en tests/fixtures/test_syquex_parser_drv.syn (fixture de test, no código de producción).
DETALLE TÉCNICO (desviaciones corregidas contra M3 §3):
  1. **sq_funcion reestructurada (bloqueante):** la forma una-expresión (L79) no consumía `[-> tipo]` antes de decidir → el cursor quedaba en T_FLECHA(35) y el bucle de declaraciones emitía SQ_DECL_UNKNOWN. Ahora ambas formas comparten `funcion ID ( params )`, el `[-> tipo]` (L78) se consume antes del dispatch, y el span del tipo queda en slot2 (insumo R88).
  2. **coincidir:** faltaba consumir el `:` tras el sujeto (L139) y el cuerpo de caso exigía `:` inexistente; nuevo `sq_caso_cuerpo` implementa L140 exacto (`patron "=>" ( sentencia | NEWLINE INDENT bloque DEDENT )`) con el bucle extraído a `sq_bloque_interno` (reutilizado por sq_bloque).
  3. **externo:** consume la palabra `funcion` (L99: `"externo" funcion`).
  4. **Constructores de colección:** `lista<T>()`/`mapa<K,V>()`/`arena` aceptados como keywords contextuales en posición de nombre (L157-158/L167/L197; patrón rc/modulo de FASE A) + `sq_scan_genericos` consume `<...>` SOLO si cierra con `>` inmediatamente seguido de `(` (uso canónico §8.1 `Canal<texto>(10)`); el span de los argumentos queda en slot2 del nodo llamada para el traductor R88. La comparación `a < b` no se ve afectada (verificado en probe bool_y_distinto).
  5. **para-rango sin `paso`:** incremento unitario explícito (NODO_NUMERO "1") en el desugar a MIENTRAS (antes código -1 con RHS vacío).
HALLAZGOS REGISTRADOS (regla 11):
  - H-R87-1 (Media): M3 §3 L74 define la keyword `variable` para declaraciones a nivel de módulo, pero NO existe TokenID asociado (tabla M3 §4 no la lista; set compartido M2 §3 tampoco). Resolución adoptada: el parser acepta `let` a nivel módulo (paridad M2/M6); decisión sobre añadir T_VARIABLE vs documentar `let` como forma canónica → Arquitecto en R88/R89.
  - H-R87-2 (Baja): formas `externo estructura` y `externo constante ID = STRING` (M3 §3 L99) sin implementar — la muestra ejercita solo `externo funcion`. Resolución asignada: R88/R89 cuando el driver las ejerza.
  - H-R87-3 (Baja, riesgo documentado): heurística de `sq_scan_genericos` (parsers sin backtracking): `<` tras identificador es genérico únicamente con el cierre `>(`; un programa que compare `(a < b)(...)` con llamadas envueltas caería en parse distinto — caso no presente en la gramática de muestras; registrado.
  - H-R87-4 (resuelta aquí): sq_tokens_hi/sq_tokens_ext estaban ubicados en lexer.syn dependiendo de `struct TokenExt` de parser_base.syn → el harness del lexer R86 dejaba de compilar (`array type has incomplete element type`). Movidos a parser.syn (capas correctas: lexer=TokenLex, parser=TokenExt/NodoAST); suite R86 vuelve a 5/5.
  - H-R86-1 (dedup str_eq en integración unity) sigue pendiente para R89 (ya registrado en R86, sin cambio).
RIESGOS IDENTIFICADOS: (i) la decoración de métodos (`Struct_metodo`, M6 §1.3) quedó diferida en R87 (sq_decorar_metodo noop, nombre simple conservado; el struct se identifica por contexto) — el traductor R88 debe resolver el prefijo; (ii) los patrones de coincidir con constructor `ok(x)` crean NODO_LLAMADA (semántica de binding en R88); (iii) integración unity futura (R89) requerirá dedup str_eq (H-R86-1).
PRÓXIMO PASO: R88 — `syquex/traductor.syn`: NodoAST[] Syquex → SemNodo[] canónico (Manual 6 §1.3; métodos→funciones con self, metadatos preservados M3 §11.2; incluye dedup str_eq H-R86-1).
--- FIN ---
