# REPORTE R92 — FASE 22: cierre H-R87-1 — keyword `variable` a nivel de módulo (Opción B estricta)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R92 — Implementar la keyword `variable` para declaraciones a nivel de
módulo según Manual 3 §3 L74 y RETIRAR la tolerancia de `let` a nivel de
módulo (Opción B del Arquitecto: los manuales son ley; sin caminos
alternativos tolerados). Incluye alineación de fixtures commiteados y
registro del hallazgo de backend H-R92-1.

FASE: 22 (Fundación de Syquex — ROADMAP entregable 1/2; decisión D-F22-A).

MANUAL REFERENCIADO: Manual 3 §3 L62-74 (declaracion a nivel de módulo;
`variable ::= "variable" IDENTIFICADOR [ ":" tipo ] [ "=" expresion ]`),
L120-122 (declaracion_variable_local ::= "let" ... — SOLO ámbito local);
§4 tabla de keywords propios (no define traducciones para `variable`);
Manual 2 §2 L47-54 (grammar Synapse: SIN globales mutables — solo constante);
Manual 6 §1.2 (ABI v1 aditiva); precedentes de keyword contextual F1.4.

DECISIONES DEL ARQUITECTO REGISTRADAS EN ESTA SESIÓN:
  - Opción B para H-R87-1: implementar `variable` Y retirar tolerancia
    let-global ("los manuales son ley... más fácil identificar problemas
    cuando todo está según la norma").
  - RATIFICADO el endurecimiento de test_syquex_r90.py (self tipado primero
    en __init__).
  - APROBADA la semántica por valor de self (H-R91-1) — documentación de
    usuario diferida a lib/pruebas (Fase 24).

HASH COMMIT: 3967696 (código+tests+fixtures; docs en commit separado citando
este hash).

COMPILACIÓN: frontend reconstruido rc=0 anti-stale. Pruebas manuales:
`let global_malo = 5` → rechazado con mensaje normativo
("[Syquex] 'let' solo es válido dentro de funciones; usa 'variable' a nivel
de módulo (M3 §3 L74/L122)"); `variable INICIO = 7` → traduce rc=0.

TESTS:
  - tests/integration/test_syquex_r92_variable.py **3 passed + 1 xfailed**:
    parser_acepta_variable_global (flat → DeclaracionVariable('INICIO', 7)),
    let_global_rechazado (pipeline rc≠0),
    variable_local_sigue_valida_en_syn (.syn con let local compila y ejecuta
    "4" — paridad M3 §3 L122 / M2),
    variable_global_compila_y_ejecuta **XFAIL estricto** ligado a H-R92-1.
  - Regresión F22 completa: **58 passed, 1 xfailed** (r90 · puente · r91 e2e ·
    s1_e2e · r92 · r89 · r88 · parser · lexer) — los drivers R87/R88
    alineados siguen en verde.

MODIFICACIONES DE TESTS/FIXTURES (por decisión explícita del Arquitecto,
Opción B):
  - tests/fixtures/test_syquex_traductor_drv.syn: `let global_sq = 5` →
    `variable global_sq = 5` (fuente embebida contraria a M3 §3).
  - tests/fixtures/test_syquex_parser_drv.syn: ídem.
  - Ningún test Python existente modificado; solo archivos nuevos.

MODULARIZACIÓN: syquex/parser.syn 1044 líneas (<1200 OK); sq_let refactorizado
a sq_let_nucleo compartido por sq_let/sq_variable (sin duplicación).

DETALLE TÉCNICO:
  1. **T_VARIABLE = 90** en syquex/lexer.syn (rango propio Syquex 75-90; NO
     toca el canon compartido ni runtime/core/ast_nodos.h — verificado: gate
     D-9(e) sigue en 126 constantes). Keyword SOLO en el bloque español:
     M3 §4 no define traducciones y NO-INVENTAR prohíbe completarlas; en
     en/fr/pt la palabra queda como identificador hasta especificación.
  2. **Refactor**: sq_let dividido en sq_let_nucleo(est,linea,col) compartido
     por sq_let (consume T_LET, local) y sq_variable (consume T_VARIABLE,
     módulo) — misma forma ID [":" tipo] ["=" expr] con span slot2.
  3. **sq_declaracion**: rama T_VARIABLE nueva; rama T_LET ahora produce
     hay_error + mensaje normativo por stderr y retornar 0 (la decl chain lo
     ignora y el parse aborta con código limpio).
  4. **Fixtures drivers alineados**: única fuente embebida con let-global era
     `global_sq`; sin ella los tests R87/R88 mantienen 6/6 y 9/9.

HALLAZGOS REGISTRADOS (regla 11):
  - H-R92-1 (Alta, resolución asignada R93): el backend compartido NO soporta
    variables globales mutables — M2 §2 L47-54 no define el concepto (solo
    constante inmutable) y un .syn con `let G = 7` top-level falla igual que
    el .syq (probe incluido en la sesión). Implementar exige: pasada 2 de la
    semántica (registro de globales), emisión C (definición global +
    inicialización arbitraria vía función _syn_globals_init antes de
    principal) y RE-VERIFICACIÓN bootstrap S2==S3 diff 0 bytes (regla de
    hierro). El xfail estricto del test fuerza su cierre.
  - H-R87-1: CERRADA (implementación + decisión Opción B).
  - H-R91-1: CERRADA por decisión del Arquitecto — semántica por valor
    aprobada; documentación de usuario en Fase 24 (lib/pruebas).
  - Ratificación registrada: endurecimiento de test_syquex_r90.py aprobado.

RIESGOS IDENTIFICADOS: (i) H-R92-1 deja `variable` traducible pero no aún
compilable hasta R93 (xfail visible, no silencioso); (ii) programas Syquex
que usaran `variable` como identificador romperán (ruptura esperada y deseada
bajo norma estricta; grep en fixtures/examples: cero ocurrencias).

PRÓXIMO PASO: R93 — globales mutables en el backend compartido (pasada 2
semántica + emisión C + init pre-principal + bootstrap S2==S3), cierra el
xfail H-R92-1. Después: inventario de criterios ROADMAP F22 restantes
(paridad dumps .syn/.syq equivalentes) para cierre formal de fase.
--- FIN ---
