# REPORTE R83 — Hardening del transporte remoto: crypto_secretbox (Fila 19)

--- REPORTE DE MICRO-ENTREGABLE ---
TAREA: R83 — Sustituir el cifrado XOR repetitivo del transporte remoto por AEAD real (crypto_secretbox XSalsa20-Poly1305 de TweetNaCl) con nonce por mensaje
FASE: 19 (CanalRemoto v2)
MANUAL REFERENCIADO: Manual 6, Sección 5.3 paso 4 (clave de sesión para "DATOS_CIFRADOS"); Manual 5, Sección 6.2 paso 3; regla 8 (cero dependencias — primitiva ya presente en axon/tweetnacl.c)
HASH COMMIT: 9f626a9
COMPILACION: gcc -O2 -Wall -Wextra -I. runtime/core/cluster.c rc=0 (solo 2 warnings preexistentes ajenos)
TESTS:
- Unitarios nuevos A1–A7 en tests/test_cluster_kx.c: roundtrip AEAD, formato wire [nonce24][MAC16][ct], ciphertext≠plano, clave incorrecta→rechazo MAC (-2), tamper de 1 byte→rechazo, entrada corta→-1
- e2e real UDP: test_handshake.py **4/4** + test_cluster_remote.py **3/3** (handshake + claves idénticas + DATA_OK:kx-payload-42 descifrado con secretbox) = **7/7 PASS**
DETALLE TÉCNICO:
1. API pública `cluster_sesion_cifrar_buffer` / `cluster_sesion_descifrar_buffer` (runtime/core/cluster.h); internamente usadas por `cluster_canal_remoto_enviar` / `cluster_recibir_paquete` cuando hay sesión activa. Formato: payload = [nonce 24B aleatorio][MAC 16B][ct]; sin sesión → texto plano (compat).
2. Fix necesario en el receptor: parseo DETERMINISTA del framing `DATA:<chan>:<seq>:<payload>` (dos memchr) — el antiguo "último ':'" caía dentro del nonce/MAC aleatorios (bytes 0x3A probables) y rompía el split.
3. Bugs del ME corregidos durante el desarrollo (verificados con probe mínimo contra TweetNaCl): buffer de salida con tamaño d-16 insuficiente (overflow) y copia truncada sin MAC; padding de open reconstruido como [16 ceros][MAC][ct] con d = blob+16.
4. Regla 12: eliminado `cluster_xor_cifrar` (sin callers tras la migración).
RIESGOS: el nonce aleatorio por datagrama viaja inline (+40 bytes/mensaje); reutilización de nonce estadísticamente despreciable (96 bits). No hay re-keying de sesión (futuro si se requiere).
PRÓXIMO PASO: R84 serialización §6.3
--- FIN ---
