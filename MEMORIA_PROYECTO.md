# MEMORIA DEL PROYECTO SYNAPSE

> **Sistema de memoria persistente del agente.** Leer al inicio de cada sesión ANTES de programar.
> Última actualización: 2026-08-14 (F3-7 CERRADA: `escuchar` alineado al Manual 2 L113 en AMBOS compiladores — la sintaxis parseada `escuchar canal -> callback` NO existe en el manual (`escuchar_canal ::= "escuchar" expresion ":" NEWLINE INDENT bloque DEDENT`); el ejemplo 03_concurrencia estaba roto en ambos (S1: `_listener_1` sin declarar + `return principal();` ANTES de `synapse_esperar_hilos()` → el listener moría con el proceso; nativo: `SentenciaEscuchar` sin rama). Fix en 7 frentes: (1) gramática L113 en S1/nativo/puente/AST (`respuesta` → `cuerpo: ListaNodo`)/flatten F8/analizador/oráculo `_P_*` (rama T_LISTEN con `bloque()`); (2) main S1 captura `int64_t _rc = principal(); synapse_esperar_hilos(); return _rc;`; (3) listener nativo completo: globals `_G_listeners*` en cabecera (orquestador.syn + S1 `_emit_cabecera_comun`), helpers reales `_G_native_contar_escuchar`/`_G_native_escuchar_vars` en monomorfizacion.syn (patrón `_G_native_scan_ctor_exprs` — el intento como texto de cabecera rompía el unity: referencian structs del AST inexistentes en programas de usuario; con `requiere:`/`garantiza:`), pre-scan de externs + flush antes del main, `gen_visitar_escuchar` (con contratos), modo escuchar en `_oo_expr_a_c`; (4) S1 `Canal<T>` (Manual 2 L144): `_CANAL` incluye `Canal` + `_tipo_normalizado` mapea `Canal<...>` → `CanalConcurrencia*`; (5) ejemplo 03_concurrencia a la sintaxis del Manual (`escuchar ch:` + `Canal<entero>`); (6) tests alineados + caso `escuchar` en native_puente_paridad.py; (7) limpieza regla 12 solicitada: 849 artefactos no trackeados eliminados, logs citados marcados como eliminados. Bootstrap S2==S3 `68f0a4b7…`; e2e `escuchar` bloque 42/99 en AMBOS compiladores rc=0; paridad frontend 27/27; regresión S1 núcleo 369 passed/2 skipped + embebido 21/9 + misc 268; verificador 0 brechas. Historial previo: F3-6 CERRADA: emisión de canales en el codegen nativo — `ExprCrearCanal`/`ExprRecibirCanal` definidos en AST/parser/puente pero SIN rama en el generador: `let ch = canal(entero, 10)` emitía `int64_t ch = 0;` (nunca llamaba canal_crear) y `r = ch ->` emitía `0`; los HM R14 solo compilan rc=0 sin ejecutar, por eso no lo detectaron. Fix en 4 frentes (paridad emit_expressions.py L523-529/L55-56): (1) expr_eval.syn `_oo_expr_a_c` ramas ExprCrearCanal→`canal_crear(cap)` (default 10) y ExprRecibirCanal→`canal_recibir(canal)`; (2) orquestador.syn `_syn_nativo_expr_tipo_c` ExprCrearCanal→CanalConcurrencia*, ExprRecibirCanal→void*; (3) nodos_flujo.syn gen_visitar_declaracion + funciones.syn hoisting ME-B7 ramas para declarar CanalConcurrencia* (let y asignación patrón R14); (4) analizador_semantico.syn `_prim[]` + `"Canal"` (Manual 2 L144). Bootstrap S2==S3 `9d84b3c1…`, probes e2e productor/consumidor rc=0 con 42 viajando por el canal (antes ch=0 sin llamadas), paridad frontend 27/27, regresión S1 `logs/regresion_s1_f36.log`. Hallazgos nuevos con resolución: F3-7 `escuchar` (S1 genera `_listener_1` sin declarar → ejemplo 03_concurrencia roto en AMBOS; sintaxis parseada `-> callback` ≠ Manual L113 `escuchar expr: bloque` → ME de diseño aparte) y F3-8 forma sin `let` (`x: entero = 5` — Manual L134 exige let; nativo correcto, S1 lenient). Historial previo: F3-4: gap de paridad en la inferencia del `let` sin anotación para builtins con retorno no-`int64_t` (hallazgo F3-4): seed `_G_rt_builtin_ret` ampliado (17→25 builtins: `abrir→canal`, `crear_tensor`/`reserva`/`suma_tensor`/`producto_punto`/`suma`/`producto`/`relu→tensor`, paridad `context.py` `_BUILTINS` L72-118) en `escaneo.syn` + rama genérica `_G_native_tipo_retorno` en `gen_visitar_declaracion` (nodos_flujo.syn, patrón hoisting ME-B6 L219-230, filtra vacio/nulo/entero→default int64_t); `generator.syn` regenerado (lección R5); bootstrap S2==S3 `a817533f…`, probes e2e `Canal archivo = abrir(...)` y `Tensor t = crear_tensor(...)` rc=0 (antes C inválido `int64_t`); regresión S1 `logs/regresion_s1_f34.log`; verificador 0 brechas. Historial previo: F3-2 — std.io externs `_syn_escribir/_syn_escribir_linea/_syn_leer_linea/_syn_abrir/_syn_leer` DEFINIDOS en `runtime/core/io.c` (antes declarados sin definición — mina latente regla 12; `importar std.io` funcionaba solo por `--gc-sections` descartando los wrappers) + trío `abrir`/`leer`/`cerrar` (Canal-como-handle + librerías virtuales LIB_*) MIGRADO de `synapse_rt.c` a `io.c`; bootstrap S2==S3 `32ea2d4d…`, probe de link C de los externs OK, e2e archivos nativo rc=0 (con `let archivo: Canal`), e2e_io rc=0, regresión S1 en `logs/regresion_s1_f32.log`; observación: forma calificada `io.funcion(...)` (Manual 5/7) NO soportada por la gramática (Manual 2 EBNF no la define — sin acción); **hallazgo F3-4 registrado**: `archivo = abrir(...)` sin anotación infiere `int64_t` en el nativo vs S1 rc=0 (mapa `_BUILTINS` context.py L70-80) — resolución: port al `_syn_nativo_expr_tipo_c` nativo (patrón R20/R25/R27) + regenerar generator.syn (ME independiente de Fase 3). Historial de sesión previa: F3-B1/F3-1 — **FASE 3 iniciada** (generador C y runtime, ROADMAP): inventario B1 — checklist 3.1-3.6 con **manuales corregidos** (el checklist citaba "Manual 3" = SYQUEX/Fases 22+; los entregables F3 están gobernados por Manual 2/4/5/9) — 3.1 emisor ✅ (determinismo S2==S3 continuo), 3.2 memory.c ✅ (pool + escapes R10, Manual 4 §2), 3.3 io.c ✅ **CERRADA en F3-1** (único entregable inexistente: I/O disperso — `escribir`/`escribir_linea`/`leer_linea` en `concurrency.c` y archivos en `synapse_rt.c` → `runtime/core/io.c` creado con las 6 funciones; pipeline.py S1 + `principal.syn` L623 nativo (con el 6º `_rt_dir` del snprintf — bug propio del ME corregido) lo enlazan; bootstrap S2==S3 `16fbbbef…`, e2e nativo hola_mundo rc=0), 3.4 ⚠️ (canales ✅, **fibras → Fase 4**, F3-3), 3.5 RAII ✅ (M22.2/M22.3), 3.6 ✅ (D-7); hallazgos registrados: **F3-2** (std/io.syn declara externs `_syn_abrir/_syn_leer/_syn_escribir/_syn_escribir_linea/_syn_leer_linea` SIN definición — wrapper muerto; resolución: siguiente ME de Fase 3), D-9(d) avanzada (primer corte real del monolito synapse_rt.c). Historial de sesión previa: R33 — D-9(c) CERRADA: poda de `compilador/generator/emit_selfhost.py` (regla 12): `emitir_generar` (L1106-1487, ~380 líneas, generador auto-hospedado antiguo del bootstrap S1) MUERTO tras A4 (0 callers) eliminado — 1487 → 1105 líneas con CRLF y funciones vivas intactas; reevaluación honesta: `gen_parse` NO era muerto (`emitir_parsear`, oráculo `_P_*` de los harnesses, lo llama) → se conservó; harnesses `native_puente_paridad.py`/`native_parser_paridad.py` adaptados (regresiones NO detectadas de R29/R30/R32: unity builds sin `lexer_keywords.syn`/parser_sentencias/declaraciones/canales.syn y `SentenciaPara.cuerpo` como ListaNodo — Manual 2 §2.2 L108); paridad frontend 28/28; verificador 0 brechas. Historial de sesión previa: R32 — D-9(b) CERRADA: modularización de `nucleo/lexer.syn` → `lexer_keywords.syn` (regla 13, patrón R29): las 4 tablas keyword es/en/fr/pt (339 líneas) extraídas con texto BYTE-IDENTICO al HEAD (336/336 líneas-no-blancas verificadas); orquestador `keyword_token` + resto quedan en `lexer.syn` (769 líneas); registro en `principal.syn` (import + `_files[]`); C byte-idéntico (emisor ordena funciones alfabéticamente, Manual 8 §8.2 — 8/8 funciones del lexer idénticas HEAD vs split, solo cambia `_files[]`); harness `native_lexer_paridad.py` adaptado (compila el par lexer+keywords con import inline + dir_base); gate de contratos del verificador refinado (`_funciones_en_head()`: función MOVIDA ≠ nueva — no exige `requiere:`/`garantiza:` a las movidas, paridad D-4); bootstrap S2==S3 `423008d8…` (1.102.625 bytes), **105/105 HM**, S1 **530 passed/9 skipped**, probes multi-idioma es/en/fr/pt OK; verificador 0 brechas; deuda D-9(b) CERRADA — queda (c) podar `emit_selfhost.py` como siguiente ME de Fase 2. Historial de sesión previa: R31b — ENFORCEMENT: gates de cumplimiento obligatorio (el programador humano o IA NO puede eludir los manuales): verificador ampliado con 4 gates — (3) deuda citada sin registro en el canon D-1..D-9/D-F1/H12/R3 (regla 11: toda deuda PENDIENTE requiere resolución asignada), (4) scripts `_*.py` en raíz = código muerto (regla 12), (5) módulos >1200 líneas sin registrar en D-9 (regla 13; `generator.py` 1,585 y `analizador_semantico.syn` 1,393 evaluados COHESIVOS y registrados en D-9(e) con resolución), (6) función NUEVA en nucleo/*.syn sin `requiere:`/`garantiza:` (Manual 2 §12; las 306 existentes sin contrato quedan en D-4 Fase 5); CI: job `auditoria` en `.github/workflows/ci-tests.yml` (todo push/PR a cualquier rama, fetch-depth 0, falla si hay brechas — infalible); pre-commit hook `githooks/pre-commit` + instalador `instalar_hooks.sh` (core.hooksPath, resuelve python/venv/py); D-9 ampliada; sesión R31 — VERIFICADOR MECÁNICO DE ALINEACIÓN: creado `auditoria/verificar_alineacion.py` (solo stdlib + git en modo lectura, regla 8) que comprueba de forma OBJETIVA la trazabilidad bitácora↔reportes↔git: cada fila de la bitácora cita reporte/hash existente, cada `Hash:`/`commit X` citado existe en `git log`, cada `§N` citada existe en el reporte, los reportes con bloque `REPORTE DE MICRO-ENTREGABLE` cumplen el protocolo (TAREA/FASE/MANUAL REFERENCIADO/HASH COMMIT/COMPILACION/TESTS/PROXIMO PASO, acepta `CAMPO (…):`), y las refs a archivos existen (o son históricas/planificadas → informativas). Primer despliegue: 64 filas, 28 reportes, 39 hashes — **0 brechas** tras resolver 9 hallazgos reales: 8 reportes FASE A con `HASH COMMIT: pendiente` completados con el hash real del tramo (`198707d` A1/A2.0/A2.1/A2.2/A2.3/A2.4; `8930ec1` A2.3b/A3.1; verificados con `git log --all -- <reporte>`), fila F2-2.4d-CK sin hash/reporte completada (288ec67 + FASE_2_CHECKLIST.md), typo `test_a23_paridad.py`→`test_a23_parity.py` corregido. 46 notas informativas por diseño (pre-convención Fase 0, formato propio, refs históricas/planificadas). Documentado en `docs/AUDITORIA_ALINEACION_MANUALES.md` (sección final). Historión de sesión previa: R30 — `para` alineado al Manual 2 §2.2 L108 CERRADA (hallazgos H-R29-1/H-R29-2): codegen nativo `gen_visitar_para` declara `<tipo> <var> = <init>` (antes `for (0LL; i < 3LL; )` sin declarar la variable, gcc rc=5) + registro en `_G_fn_vars`; `SentenciaPara.nombre` + `cuerpo: ListaNodo` (antes solo la primera sentencia del bloque); flatten F8 mapea SentenciaPara/Romper/Siguiente (45/20/21) + caso NODO_PARA en el analizador — el cuerpo del `para` antes era INVISIBLE (use-after-move dentro del bucle ahora E-501 con línea); S1 `_parsear_para` al dialecto del Manual (`mientras`, el C-style `;` NO existe en el Manual) + `visitar_para` declara la variable; `siguiente`/`romper` (Manual 2 L115-116) verificados end-to-end en ambos compiladores; limpieza regla 12: 21 archivos muertos eliminados (19 scripts `_*.py` + fixtures smoke_backend.syn/test_bucle_para.syn); bootstrap S2==S3 (`e52818ac…`), **105/105 HM** (101+4 R30), S1 197 passed; historial de sesión previa: R29 — modularización de `nucleo/parser.syn` CERRADA (regla 13): split mecánico en 4 módulos (parser.syn 494 / parser_sentencias.syn 563 / parser_declaraciones.syn 427 / parser_canales.syn 58), texto idéntico sin cambios de lógica, C byte-idéntico (diff solo el array `_files[]`, 2 líneas — prueba de transparencia), bootstrap S2==S3 (`1f3be399…`), 101/101 HM, S1 196 passed; hallazgos registrados con resolución asignada: keyword `continuar` sin parser (smoke_backend.syn nunca compiló — fixture muerta) y codegen `para` nativo `for (0LL; i < 3LL; )` sin declarar la variable de bucle (Manual 2 §2.2 L108; dialecto S1 C-style divergente) — modularización de `nucleo/parser.syn` CERRADA (regla 13): split mecánico en 4 módulos (parser.syn 494 / parser_sentencias.syn 563 / parser_declaraciones.syn 427 / parser_canales.syn 58), texto idéntico sin cambios de lógica, C byte-idéntico (diff solo el array `_files[]`, 2 líneas — prueba de transparencia), bootstrap S2==S3 (`1f3be399…`), 101/101 HM, S1 196 passed; hallazgos registrados con resolución asignada: keyword `continuar` sin parser (smoke_backend.syn nunca compiló — fixture muerta) y codegen `para` nativo `for (0LL; i < 3LL; )` sin declarar la variable de bucle (Manual 2 §2.2 L108; dialecto S1 C-style divergente); historial de sesión previa: R28 — instancia ADT anidada en ctors sin anotación CERRADA: `let r = ok(ok(42))` sin anotación, con la instancia SIMPLE registrada por firma, ahora compila rc=0 con la instancia anidada completa en AMBOS compiladores (antes C basura rc=5/rc=1 — los scans de monomorfización ignoraban ctors en expresiones); fix en 4 frentes: fixpoint de derivación en D-2 nativo (`_G_native_scan_ctor_exprs`) y S1 (`_recolectar_instancias_adt`) + registro de la variable ligada del caso `ok(inner)` en el coincidir (S1 `ctx._variables` / nativo `_G_fn_vars`) — el probe triple `ok(ok(ok(42)))` delató el fallback de la primera instancia del base en el binding del nivel 2; caso sin ninguna firma sigue fallando en ambos (paridad R20); bootstrap S2==S3 (`e580ffcf…`), **101/101 HM** (96+5 R28), S1 196 passed; historial de sesión previa: R27 — ctor de estructura en `let` sin anotación CERRADA (hallazgo R26): el ctor de struct documentado `Punto()` (Manual 2 L67) ahora infiere `struct Punto` en el codegen nativo (antes C inválido rc=5) y la forma NO documentada `Punto(1,2)` se rechaza con `ERR_SEM_ARGUMENTOS_INVALIDOS` esperados=0 (paridad S1; antes rc=0 mudo con C inválido); bootstrap S2==S3 (`538516a6…`), **96/96 HM** (94+2 R27), S1 196 passed; historial de sesión previa: R26 — sintaxis de transferencia de ownership en parametros (`->`) en el parser nativo CERRADA (fix `3b5ba0a`): cierre del pendiente "resto del borrow checker S1" (Manual 2 L59-60) — la DETECCION de UAF ya estaba portada en R14/R15 (E-501 canal/lanzar); faltaba la gramatica `parametro ::= [ ">" ] IDENTIFICADOR ":" tipo` (nativo rc=8, test S1 xfail con sintaxis invertida); fix en 3 capas (parser T_FLECHA opcional → valor_int → puente es_transferencia → flatten) + test S1 corregido a la sintaxis del Manual (xfail eliminado, 3/3); semantica lenient (paridad S1); probe `-> pos: entero` rc=0; bootstrap S2==S3 (`d0cc550d…`), **94/94 HM** (92+2 R26), S1 221 passed; siguiente por prioridad: inferencia de la instancia anidada en `let ok(ok(42))` sin anotacion (caso ambiguo de paridad) y el hallazgo R26 del ctor de estructura en `let` sin anotacion).

---

## 1. CONTEXTO ACTUAL

- **Fase del roadmap:** Fase 2 — Tabla de símbolos y análisis semántico (brecha 2.4 P0 Hindley-Milner portada al nativo; rama `feature/fase2-nativa-hm`).
- **Objetivo actual:** Cerrar la auditoría de alineación con los manuales del analizador nativo (`nucleo/analizador_semantico.syn`): checklist 2.1/2.2/2.3/2.5/2.6 (scopes, taxonomía `ERR_SEM_*`, ownership, exhaustividad) — ver `docs/AUDITORIA_ALINEACION_MANUALES.md`.
- **Estado R21:** ✅ **CERRADA** (fix `26987fe`, 2026-08-12) — línea/columna reales en el flatten F8 (Manual 2 §10.1): el flatten solo propagaba línea para `ExprObtenerDireccion` (R12), `Identificador` y `SentenciaEnviarCanal` (R14); el resto de nodos nacía con 0 y los diagnósticos salían con `(linea 0, columna 0)`. Fix en 3 capas (ast_nodes→puente→flatten, patrón R12/R14, sin tocar lexer/parser — el puente lee `linea_n`/`col_n` del NodoAST plano): `DefinicionFuncion`, `DefinicionEstructura`, `DeclaracionExterna`, `DeclaracionTipo`, `AsignacionVariable`, `DeclaracionVariable`, `NodoCoincidir` (REDEFINICION/CONSTANTE_INMUTABLE/EXHAUSTIVE_MATCH) + `LlamadaFuncion` y `Parametro` (R1 AMBIGUOUS/INCOMPATIBLE y 2.4 aridad/base en parámetros). 7 probes con línea/columna reales (ADT 3:6, const 4:5, match 4:15, AMBIGUOUS 5:9, aridad-param 3:18, var 4:5, INCOMPATIBLE 5:9), bootstrap S2==S3 (sha256 `62e4647f…`), **76/76 HM** (69+7 R21). **Hallazgo (paridad S1):** REDEFINICION de función/estructura/externa NO observable — el unity merge de `principal.syn` deduplica símbolos top-level (`_seen_sym[2048][64]`, pipeline.py:374; rc=0 en nativo y S1) — los checks de pasadas 1/2 quedan como defensa redundante; resolución asignada: distinguir duplicados del archivo del usuario vs espejo entre módulos. Residual: nodos sin anclaje de diagnóstico sin línea (documentado en el reporte §24).
- **Estado R22:** ✅ **CERRADA** (fix `6f6e5a6`, 2026-08-12) — cuerpo de caso en BLOQUE + coincidir ANIDADO en el parser nativo (Manual 2 §2.4 L124 / Manual 3 L140): la gramática `caso_coincidir ::= patron "=>" ( sentencia | NEWLINE INDENT bloque DEDENT )` estaba sin implementar en NINGUN parser — el bucle del cuerpo del caso terminaba en la primera NL y el INDENTAR del bloque confundía el bucle de casos (probe R20 p3: rc=8 nativo / «Se esperaba IDENTIFICADOR, se encontró INDENT» S1); y un coincidir anidado de una línea se tragaba el caso siguiente (AST corrupto «Nodo tipo DefinicionFuncion no reconocido»). Fix en AMBOS parsers: nativo `parsear_coincidir` — forma bloque tras `=>` (saltar NLs A4.5, `T_INDENTAR`, sentencias hasta `T_DESINDENTAR`/`T_FIN`, consumir el DESINDENTAR; idioma `parsear_inseguro`) + **guard de columna** (`token_columna <= col_c`) en la forma de una línea; S1 `_parsear_coincidir` — `NEWLINE` → `_parsear_bloque()` (idioma `_parsear_si`) + guard `columna > tok_patron.columna`. Heurística de indentación documentada (asume espacios; tabs mixtos, reporte §25). 6 probes nativos (bloque rc=0; anidado-en-bloque rc=0 — antes rc=8; anidado-una-línea rc=0; switch anidado ejecuta → 42; anidado no exhaustivo con línea real (6:23); bloque vacío rc=0), bootstrap S2==S3 (sha256 `96f7f21e…`), **82/82 HM** (76+6 R22), S1 parser/match/tipos 69 passed. **Hallazgo S1 (preexistente):** codegen de `coincidir` en funciones NO genéricas emite `Resultado_T` — `test_match.py` solo valida semántica, nunca generó C; resolución asignada (registrar instancias de parámetros no genéricos en el S1). Ver sección 2.
- **Estado R23:** ✅ **CERRADA** (fix `603c754`, 2026-08-12) — REDEFINICION de función/estructura/externa/constante OBSERVABLE (cierre del hallazgo R21; Manual 3 §3.1 orden inline / Manual 2 §10.1): el dedup `_seen_sym` first-wins del unity merge deduplicaba los símbolos top-level sin distinguir origen → la redefinición del archivo del usuario era silenciosa (los checks de pasadas 1/2 quedaban como defensa redundante) y la leniency R9 de constantes tapaba los espejos entre módulos del compilador. Fix en 7 puntos (paridad S1+nativo): (1) **dedup por PROFUNDIDAD** en `principal.syn` ME-B9.z — `_desde_modulo = (_stk_n > 1)` (solo símbolos de módulos importados; los duplicados del propio archivo llegan al analizador); (2) **dedup por proveniencia** en `pipeline.py` — `_origenes` marca `de_importacion`; (3) **ruta Unity Build del self-hosted** sin dedup (la leniency retirada destapaba ~110 espejos falsos) → pasada first-wins post-merge (`_seen_sym2`/`_nd2`); (4) **plantilla REDEFINICION** en `sem_error` (`Redefinicion de '%s' en el mismo ambito`, 7 call-sites con nombre crudo); (5) **constante global ESTRICTA** (leniency R9 retirada); (6) **`NODO_CONSTANTE` con línea/columna** en `puente_ast.syn` (las constantes globales reportaban 0,0); (7) **paridad S1 del checker** — ADT duplicado (rama `DeclaracionTipo` con `if not in` silencioso) y variable local duplicada (`declarar` sin comprobar retorno, pasaba a gcc `redefinition of 'x'`) ahora reportan REDEFINICION (el shadowing en ámbito anidado sigue válido: `declarar` retorna False solo en el mismo scope). Validación: probes nativos r1/r2/r3/c1 rc=7 con línea real de la 2.ª definición (f 4:9, Punto 4:12, ayuda 3:17, MAXIMO 3:11) + control r4 rc=0; paridad S1 a1/v1 (`Redefinición de 'Color'`/`'x'`); imports m23 con espejo rc=0 (el dedup por profundidad SÍ elimina espejos de módulos); bootstrap S2==S3 (sha256 `ddf2e0bf…`, 1.093.651 bytes), **87/87 HM** (82+5 R23), regresión S1 **212 passed**; tests: `test_fase2_nativa_hm.py` (5 R23) + `test_type_inference.py` (3: ADT/variable/shadowing). Residual: asimetría first-wins por orden de recorrido (un import visitado antes que la definición del usuario gana — paridad S1 `vistos`); el dedup del unity build cubre también función/estructura/externa como rama defensiva. Ver sección 2.
- **Estado R24:** ✅ **CERRADA** (fix `d5baf31`, 2026-08-12) — ADT builtin implícito (Resultado/Opcion) materializa su instancia monomorfizada en firmas NO genéricas del S1 (cierre del hallazgo R22; Manual 2 §4.2 L279-280, paridad semantic_scope.py L101-102): el S1 compilaba `funcion f(r: Resultado<entero,texto>)` sin declarar el ADT (builtins implícitos del checker — valida aridad/exhaustividad del coincidir) pero el generador emitía el placeholder `Resultado_T` → C inválido en gcc (`'Resultado_T' has no member named 'tag'`). Causa: `GeneratorContext.__init__` (context.py L250) inicia `_adt_parametros` vacío y solo lo llena con `DeclaracionTipo` del usuario → `_recolectar_instancias_adt` retorna temprano. Fix: precarga de los builtins (`_adt_parametros['Resultado']=['T','E']`, `['Opcion']=['T']`; `_adt_constructores` con ok/err y algun/ninguno→entero) con la regla **declaración del usuario gana** (una `DeclaracionTipo` sobreescribe — mismo comportamiento que el checker). Validación: probe p2 S1 rc=0 con typedef `Resultado_entero_texto` tipado (`int64_t ok; CadenaSegura err;`) + ejecución real `procesar(ok(7))` → rc=7; el nativo sigue estricto (p2 rc=5 `tipo base 'Resultado' no definido` — exige declarar el ADT, Manual 2 §4.2); bootstrap S2==S3 sin cambios (`ddf2e0bf…`, el self-hosted no usa `Resultado<` en firmas); **89/89 HM** (87+2 R24); regresión S1 **216 passed**; tests: `test_match.py` (1: builtin_implicito_codegen_valido — asserts `Resultado_entero_texto` presente y `Resultado_T r` ausente) + `test_fase2_nativa_hm.py` (2: parámetro ADT compila rc=0 y ejecuta imprime 7). **Hallazgo registrado (resolución asignada):** `let r = ok(7)` sin anotación infiere `int64_t` en el codegen nativo (el tipo del constructor no resuelve la instancia sin anotación; la forma anotada o la llamada directa funcionan) — pendiente: inferencia de la instancia desde el constructor en `let` sin anotación. Ver sección 2.
- **Estado R25:** ✅ **CERRADA** (fix `dfe469e`, 2026-08-12) — `let` con ctor ADT sin anotación infiere la instancia monomorfizada en el codegen nativo (cierre del hallazgo R24; Manual 2 §4.2 L279-280): `gen_visitar_declaracion` (nodos_flujo.syn) infería el tipo del `let` sin anotación por el tipo de NODO de la expresión (LiteralCadena→CadenaSegura, LiteralDecimal→double, ExprTensor→Tensor, LiteralNulo→void*) con **default int64_t** → `LlamadaFuncion` (ctor ADT) caía al default y el C quedaba `int64_t r = (Resultado_entero_texto){...}` (gcc incompatible types). Fix: rama `else if` para `LlamadaFuncion` que sea ctor ADT (`_G_native_es_adt_ctr`) que resuelve la instancia vía `_G_native_adt_inst_ctr(base, tag, tipo_c_del_argumento)` con el tipo del argumento calculado recursivamente (`_syn_nativo_expr_tipo_c`, paridad R20 — soporta ctors anidados como argumento); fallback al nombre del ADT para ADT simple (`typedef struct Punto {...} Punto;`). La asignación implícita `r = ok(7)` ya funcionaba (el hoisting usa `_syn_nativo_expr_tipo_c` desde R20). Validación: probe `let r = ok(7)` emite `Resultado_entero_texto r = (Resultado_entero_texto){...}` y ejecuta imprimiendo **7**; asignación implícita → 7; ADT simple `let p = punto(3,4)` rc=0; literales rc=0 (regresión); bootstrap **S2==S3** (sha256 `dd436c46…` — nuevo hash con la rama R25); **91/91 HM** (89+2 R25); tests: 3 HM. **Caso ambiguo documentado (paridad):** `let r = ok(ok(42))` sin anotación falla en AMBOS compiladores por igual (instancia anidada no registrada sin contexto de firma; con anotación compila y ejecuta — el R20 cerró el anidado como ARGUMENTO de llamada, no en `let` sin contexto). Ver sección 2.
- **Estado R26:** ✅ **CERRADA** (fix `3b5ba0a`, 2026-08-12) — sintaxis de transferencia de ownership en parámetros (`->`) en el parser nativo (resto del borrow checker S1; Manual 2 L59-60: `parametro ::= [ ">" ] IDENTIFICADOR ":" tipo`). Contexto: el pendiente "Uso-after-move / resto del borrow checker S1" se verificó — la DETECCIÓN de UAF ya estaba portada en R14/R15 (`NODO_IDENTIFICADOR` → `ERR_SEM_VAR_MOVIDA` E-501; EnviarCanal/Lanzar marcan movido; probes de regresión rc=7 con línea real). Lo que faltaba era la gramática de parámetro por move: no implementada en NINGÚN parser (nativo rc=8; el test S1 xfail usaba la sintaxis INVERTIDA `pos: -> entero` que el Manual no define). Fix (paridad S1+nativo): (1) `parser.syn` `parsear_parametros` acepta el prefijo T_FLECHA opcional (`es_trans_p`) y marca `valor_int=1` en el nodo (no consume la flecha del retorno — está fuera del paréntesis); (2) `puente_ast.syn` NODO_PARAMETRO: `_p->es_transferencia = val` (antes 0 fijo); (3) flatten F8 de Parametro copia `_pa->es_transferencia` a `_f8_nodos[idx].valor_int`; (4) `test_ownership.py`: corregido a la sintaxis del Manual (`-> pos: entero` que el S1 ya soportaba) y xfail eliminado (3/3 pass); (5) 2 tests HM (parámetro move + call-site `->x` regresión R15). Semántica lenient (paridad S1: se parsea, no invalida en call-site; el UAF real se detecta por canal/lanzar R14/R15 y el call-site `->x` R15). Validación: probe `-> pos: entero` nativo rc=0 (antes rc=8) + runtime 0; regresión UAF canal/lanzar rc=7 E-501; bootstrap **S2==S3** (sha256 `d0cc550d…`); suite **94/94 HM** (92+2 R26); S1 **221 passed** (ownership 3/3 + borrow + unit + e2e). Code review: sin colisión de `valor_int` en NODO_PARAMETRO (solo NODO_PUNTERO lo lee), campos de estructura con bloque separado, flag write-only = paridad lenient. **Hallazgo registrado (resolución asignada):** `let p = Punto(1,2)` (ctor de ESTRUCTURA) sin anotación — el nativo emite `int64_t p = (struct Punto){...}` (C inválido) mientras el S1 lo rechaza en el checker (`ERR_SEM_ARGUMENTOS_INVALIDOS`) — divergencia preexistente (el R25 solo cubrió ctors ADT, no de estructura); con anotación funciona rc=0. Resolución: port de la rama R25 del ctor al ctor de struct + paridad del checker S1. Ver sección 2.
- **Estado R27:** ✅ **CERRADA** (fix `08c4d70`, 2026-08-12) — ctor de estructura en `let` sin anotación (hallazgo R26). Contexto: `let p = Punto()` (forma documentada, Manual 2 L67 `declaracion_estructura` — sin argumentos) fallaba en el codegen nativo: el branch R25 solo cubría ctors ADT, el nombre de struct caía al default `int64_t` y el C quedaba `int64_t p = (struct Punto){0};` (C inválido, rc=5 GCC) mientras el S1 lo compilaba rc=0. Además `Punto(1,2)` (forma NO documentada) se aceptaba mudo y emitía C inválido, mientras el S1 lo rechaza en el checker (`ERR_SEM_ARGUMENTOS_INVALIDOS` esperados=0, `semantic_types.py` L355-360). Fix (paridad S1, 2 frentes): (1) `nucleo/generador/nodos_flujo.syn` `gen_visitar_declaracion`: rama `else if` con `_G_native_es_estructura` tras el branch R25 — `let p = Punto()` infiere `struct Punto` vía `snprintf(_var_type_buf, 127, "struct %s ", ...)` (paridad `_syn_nativo_expr_tipo_c` generator.syn L3239; acotado tras code review); (2) `nucleo/analizador_semantico.syn` rama NODO_LLAMADA: check de aridad de struct (args != 0 → `ERR_SEM_ARGUMENTOS_INVALIDOS` con plantilla S1 `Cantidad de argumentos invalida para '<nombre>': se esperaban 0` y línea/columna reales R21) + guard `_fn27<0` (si el callee es función de usuario se salta — paridad S1: `tabla.buscar` → DefinicionFuncion se valida antes del check de `_estructuras`; struct+funcion homónimos es inviable en ambos por colisión de símbolo C — defensa en profundidad); (3) `nucleo/generator.syn` REGENERADO con `nucleo/_rebuild_generator.py` (lección R5 — el cambio en `nodos_flujo.syn` no surte efecto hasta regenerar el unity); (4) 2 tests HM (campo `z` — `y` es palabra reservada del lexer, operador AND). Validación: `let p = Punto()` nativo rc=0 (antes rc=5) + runtime 0 (C `struct Punto p = (struct Punto){0};`), paridad S1 rc=0; `Punto(1,2)` nativo rc=7 con `(linea 6, columna 13)` (antes rc=0 mudo), paridad S1 rc=1 error 19; bootstrap **S2==S3** (sha256 `538516a6…`); suite **96/96 HM** (94+2 R27); S1 **196 passed**; code review: shadowing (guard `_fn27<0`) + snprintf acotado aplicados. La forma con anotación (`let p: Punto = Punto(1,2)`) sigue funcionando (ME-B4, no pasa por el check de aridad). Ver reporte §30.
- **Estado F3-6:** ✅ **CERRADA** (2026-08-14) — emisión de canales en el codegen nativo (checklist 3.4, Manual 2 L144 `Canal<T>`, Manual 5 §3.4). Hallazgo de mayor impacto técnico: `ExprCrearCanal` (NODO 41) y `ExprRecibirCanal` (NODO 43) definidos en AST/parser/puente pero **sin rama en el generador** — `let ch = canal(entero, 10)` emitía `int64_t ch = 0;` (nunca llamaba `canal_crear`; el canal no existía en runtime) y `r = ch ->` emitía `0`. Los HM R14 no lo detectaban: solo compilan (rc=0) sin ejecutar. **Fix en 4 frentes** (paridad `emit_expressions.py` L523-529/L55-56): (1) `expr_eval.syn` `_oo_expr_a_c` — ramas `ExprCrearCanal`→`canal_crear(%s)` (capacidad o default 10) y `ExprRecibirCanal`→`canal_recibir(%s)`; (2) `orquestador.syn` `_syn_nativo_expr_tipo_c` — `ExprCrearCanal`→`CanalConcurrencia*`, `ExprRecibirCanal`→`void*`; (3) `nodos_flujo.syn` `gen_visitar_declaracion` + `funciones.syn` hoisting ME-B7 — ramas para declarar `CanalConcurrencia*` (cubre `let ch = canal(...)` Y la asignación `ch = canal(...)` patrón HM R14); (4) `analizador_semantico.syn` `_prim[]` — añadido `"Canal"` (el analizador rechazaba `Canal<entero>` como parámetro, Manual 2 L144). **Validación:** bootstrap **S2==S3 `9d84b3c1…`**; probe e2e productor/consumidor `let ch = canal(entero, 10)` + `ch <- dato` + `r = ch ->` → C `CanalConcurrencia* ch = canal_crear(10LL); canal_enviar(ch, (void*)(dato)); canal_recibir(ch);` rc=0 **imprime 42** (el dato viaja por el canal real; antes `ch = 0` sin llamadas); probe asignación patrón R14 rc=0; paridad frontend 27/27; regresión `logs/regresion_s1_f36.log`; verificador 0 brechas. **Hallazgos nuevos registrados:** **F3-7** `escuchar` — el S1 genera `_listener_1` SIN declararlo (ejemplo 03_concurrencia → gcc `undeclared` rc=1), el nativo no reconoce `SentenciaEscuchar`, y la sintaxis parseada `escuchar canal -> callback` NO es la del Manual 2 L113 (`escuchar_canal ::= "escuchar" expresion ":" NEWLINE INDENT bloque DEDENT`) → **ME de diseño aparte** (regla 1: el manual manda). **F3-8** forma sin `let` (`x: entero = 5`) — Manual L134 exige `let`; el nativo emite `x;` (correcto rechazarla), el S1 lenient (divergencia preexistente). Ver reporte `docs/reportes/FASE_3_B1.md` §3d.
- **Estado F3-4:** ✅ **CERRADA** (2026-08-14) — gap de paridad en la inferencia del `let` sin anotación para builtins con retorno no-`int64_t` (hallazgo F3-4 registrado en F3-2): `archivo = abrir(...)` infería `int64_t` en el codegen nativo (C inválido `incompatible types`) mientras el S1 compilaba rc=0 vía el mapa `_BUILTINS` de `context.py` L72-118. Fix en 2 frentes: (1) `nucleo/generador/escaneo.syn` — seed `_G_rt_builtin_ret` ampliado de 17 a 25 builtins (`abrir→canal`, `crear_tensor`/`reserva`/`suma_tensor`/`producto_punto`/`suma`/`producto`/`relu→tensor`); (2) `nucleo/generador/nodos_flujo.syn` `gen_visitar_declaracion` — rama genérica `else if (_lf->nombre.datos)` que consulta `_G_native_tipo_retorno` (patrón hoisting ME-B6 L219-230, filtra `vacio`/`nulo`/`entero` → default int64_t) y traduce con `traducir_tipo_c` (`canal→Canal`, `tensor→Tensor`, `cadena→CadenaSegura`); (3) `generator.syn` regenerado (lección R5). Validación: bootstrap **S2==S3 `a817533f…`**; probes e2e nativos `let archivo = abrir(...)` → C `Canal archivo = abrir(...)` rc=0 ejecuta `OK_F3_4` + contenido, `let contenido = leer(archivo)` → `CadenaSegura`, `let t = crear_tensor(2,3)` → `Tensor t = crear_tensor(2LL, 3LL);` rc=0; regresión S1 `logs/regresion_s1_f34.log`; verificador 0 brechas. Nota: `si_no` (con guion bajo) NO es del Manual 2 L104/L222 (`sino` es la forma correcta) — el nativo lo emite literal; el dialecto del probe se corrigió. Ver reporte `docs/reportes/FASE_3_B1.md` §3c.
- **Estado F3-2:** ✅ **CERRADA** (2026-08-14) — externs `_syn_escribir`/`_syn_escribir_linea`/`_syn_leer_linea`/`_syn_abrir`/`_syn_leer` definidos en `runtime/core/io.c` (cierre del hallazgo F3-2 del inventario B1; regla 12: estaban declarados en `std/io.syn`/`src/main.c` sin definición — mina latente; `importar std.io` funcionaba solo porque el generador mapea los nombres directos vía `_rtb[]` y `--gc-sections` descarta los wrappers) + trío `abrir`/`leer`/`cerrar` (Canal-como-handle, librerías virtuales LIB_*) migrado de `synapse_rt.c` (L31-95) a `io.c` (los directos quedan como wrappers de los `_syn_*`; `io.c` ahora incluye `embedded_libs.h`). Reevaluación del hallazgo: `importar std.io` NO estaba roto en el camino directo (los builtins `_rtb[]` ganan); la mina eran los externs. **Observación sin acción:** la forma calificada `io.escribir_linea(...)` de Manual 5/7 no es soportada por la gramática (Manual 2 EBNF no define llamadas calificadas). **Validación:** gcc -c io.c OK; probe de link C que llama a los 5 externs enlaza y ejecuta (antes undefined); bootstrap **S2==S3 `32ea2d4d…`**; e2e nativo `let archivo: Canal = abrir(...); leer; escribir_linea; cerrar` rc=0 imprime el contenido del archivo; `e2e_io.syn` rc=0; regresión S1 `logs/regresion_s1_f32.log`; verificador 0 brechas. **Hallazgo F3-4 (resolución asignada):** `archivo = abrir(...)` sin anotación infiere `int64_t` en el nativo (C inválido `incompatible types`) vs S1 rc=0 (mapa `_BUILTINS` de `context.py` L70-80: `'abrir': 'Canal'`, `'leer': 'texto'`, `'crear_tensor': 'tensor'`, …) — con `let x: Canal` funciona en ambos; resolución: port del mapa al `_syn_nativo_expr_tipo_c` nativo (patrón R20/R25/R27) + regenerar `generator.syn` (lección R5) — ME independiente. Ver reporte `docs/reportes/FASE_3_B1.md` §3b.
- **Estado F3-B1/F3-1:** 🔄 **FASE 3 EN CURSO** (2026-08-14) — inventario B1 del generador C y runtime (ROADMAP Fase 3) + **3.3 CERRADA (F3-1)**: `runtime/core/io.c` creado (único entregable inexistente). Hallazgo de auditoría: el checklist citaba "Manual 3" (SYQUEX, Fases 22+ — regla 7) como fuente de 3.1-3.5; corregido → Manual 2/4/5/9. Estado: 3.1 ✅ (emisor, determinismo S2==S3 verificado en cada ME), 3.2 ✅ (memory.c pool + R10, Manual 4 §2), 3.3 ✅ **CERRADA en F3-1** (io.c: `escribir`/`escribir_linea`/`leer_linea` desde concurrency.c + `_syn_leer_archivo`/`_syn_escribir_archivo`/`_syn_leer_archivo_como_texto` desde synapse_rt.c; enlazado en pipeline.py `_RT_FUENTES` y en principal.syn L623 con el **6º `_rt_dir`** del snprintf — el primer intento sin el argumento corrompía el comando (stage2 rc=5, `-o "(null)"`); bootstrap **S2==S3 `16fbbbef…`** (1.100.897 bytes), e2e nativo hola_mundo rc=0 "Hola desde Synapse!", 0 callers internos rotos), 3.4 ⚠️ (canales ✅ CanalConcurrencia; **fibras/Fibra/Scheduler ausentes → Fase 4**, hallazgo F3-3), 3.5 ✅ (RAII M22.2/M22.3, cleanup blocks completos = Fase 23/D-1), 3.6 ✅ (D-7). **Hallazgo F3-2 (resolución asignada):** `std/io.syn`/LIB_IO declara externs `_syn_abrir`/`_syn_leer`/`_syn_escribir`/`_syn_escribir_linea`/`_syn_leer_linea` sin definición → `importar std.io` no enlaza (los nombres directos `escribir`/`escribir_linea`/`leer_linea` sí funcionan vía io.c); resolución: definir los `_syn_*` en io.c o reescribir std/io.syn sobre los directos + migrar `abrir`/`leer`/`cerrar` (Canal-como-handle) a io.c. D-9(d) avanzada (primer corte del monolito). Ver reporte `docs/reportes/FASE_3_B1.md`.
- **Estado R34/F3-7:** ✅ **CERRADA** (2026-08-14) — `escuchar` alineado al Manual 2 L113 (bloque) en AMBOS compiladores + `Canal<T>` en el S1 + limpieza regla 12. Contexto: la sintaxis parseada `escuchar canal -> callback` NO existe en el Manual (`escuchar_canal ::= "escuchar" expresion ":" NEWLINE INDENT bloque DEDENT`); ejemplo `03_concurrencia` roto en ambos (S1: `_listener_1` sin declarar + main `return principal();` antes de `synapse_esperar_hilos()` → listener moría con el proceso; nativo: `SentenciaEscuchar` sin rama). Cambios (7 frentes): (1) gramática L113 — S1 `_parsear_escuchar` → `SentenciaEscuchar(canal, cuerpo)`, nativo `parsear_escuchar` → NODO_ESCUCHAR bloque INDENT/DEDENT, AST `respuesta` → `cuerpo: ListaNodo`, flatten F8 (canal slot[6], cuerpo en hijo_izq), analizador rama NODO_ESCUCHAR, oráculo `_P_*` rama T_LISTEN con `bloque()` (patrón T_IF); (2) main S1: `int64_t _rc = principal(); synapse_esperar_hilos(); return _rc;`; (3) listener nativo (patrón R28/R30): globals `_G_listeners[8][16384]`/`_G_listeners_count`/`_G_listener_modo` en cabecera (orquestador.syn + S1 `_emit_cabecera_comun`), helpers REALES `_G_native_contar_escuchar`/`_G_native_escuchar_vars` en monomorfizacion.syn (patrón `_G_native_scan_ctor_exprs`; el intento como texto de cabecera rompía el unity — referencian structs del AST inexistentes en programas de usuario; ambos con `requiere:`/`garantiza:`), pre-scan de externs + flush de listeners antes del main (recorrido.syn), `gen_visitar_escuchar` + rama dispatcher (con contratos), modo escuchar en `_oo_expr_a_c` (`ExprRecibirCanal` → `canal_recibir`); (4) S1 `Canal<T>` (Manual 2 L144): `_CANAL` incluye `Canal`, `_tipo_normalizado` mapea `Canal<...>` → `CanalConcurrencia*` (branch ADT-`<` corregido de orden); (5) ejemplo 03_concurrencia a la sintaxis del Manual (`escuchar ch:` + `Canal<entero>` — `CanalConcurrencia*` es el tipo de implementación, no el del manual); (6) tests alineados (test_parser/semantico/unit/test_lexer/cobertura_d5/generator) + caso `escuchar` NUEVO en native_puente_paridad.py; (7) limpieza regla 12 (solicitud explícita del usuario): **849 artefactos no trackeados eliminados** (`_*.c`/`_*.c.o`/`*.exe`/`*.log`/`_test_*_temp/`/`.o` runtime), logs citados en reportes (transitorios, nunca commiteados) marcados como eliminados en los propios reportes — evidencia en bitácora/reporte. Validación: bootstrap **S2==S3 `68f0a4b7…`** (Manual 9 §9.7); e2e `escuchar` bloque rc=0 imprime **42/99** en AMBOS compiladores (listener sale al cerrarse el canal, Manual 5 §4.3); ejemplo con ambos rc=0; paridad frontend **27/27** (16 casos puente incl. escuchar); regresión S1 núcleo **369 passed/2 skipped** + embebido **21 passed/9 skipped** + misc 268; verificador **0 brechas**. Ver reporte `FASE_3_B1.md` §3e.
- **Estado R33:** ✅ **CERRADA** (2026-08-13) — poda de `compilador/generator/emit_selfhost.py` (D-9(c), regla 12): el generador auto-hospedado antiguo del bootstrap S1 (`emitir_generar`, L1106-1487, ~380 líneas) quedó MUERTO tras A4 (0 callers en todo el repo — la única otra mención es `gen_emitir_generar_c`, el generador NATIVO S2/S3, distinto). Poda mecánica: 1487 → 1105 líneas (CRLF preservado), texto de las funciones vivas intacto (verificado por líneas). **Reevaluación honesta del diagnóstico**: `gen_parse` NO era muerto — `emitir_parsear` (oráculo `_P_*` de `tests/native_puente_paridad.py` y `tests/test_frontend_embebido_d_f1.py`) lo llama; se conservó (el D-9(c) original lo asumió muerto sin notar la dependencia). **Harnesses adaptados** (regresiones NO detectadas de R29/R30/R32 que el frontend único destapó): `native_puente_paridad.py` y `native_parser_paridad.py` no incluían `lexer_keywords.syn` (R32) ni `parser_sentencias/declaraciones/canales.syn` (R29) en sus unity builds de concatenación → funciones faltantes (`keyword_token_es`, `parsear_inseguro`, `parsear_export`, `parsear_declaracion_tipo`, `parsear_asignacion`, canales); `_ser_nodo` serializaba `SentenciaPara.cuerpo` como `Nodo*` cuando desde R30 es `ListaNodo` (Manual 2 §2.2 L108) → error C de punteros corregido con `_ser_list`. Paridad frontend **28/28 passed** (4 harnesses). Verificador: `emit_selfhost.py` quitado de `MODULOS_D9` (ya no supera 1200 líneas), D-9(c) CERRADA en el canon, 0 brechas. Ver reporte §35.
- **Estado R32:** ✅ **CERRADA** (2026-08-13) — modularización de `nucleo/lexer.syn` (D-9(b), regla 13, patrón R29): las 4 tablas keyword es/en/fr/pt (`keyword_token_es/en/fr/pt`, 339 líneas) extraídas con split MECÁNICO a `nucleo/lexer_keywords.syn` — texto byte-idéntico al HEAD (336/336 líneas-no-blancas verificadas); el orquestador `keyword_token` y el resto del lexer quedan en `lexer.syn` (769 líneas); registro en `principal.syn` (import + `_files[]`); **C byte-idéntico** (el emisor ordena funciones alfabéticamente — Manual 8 §8.2; verificado 8/8 funciones del lexer idénticas HEAD vs split, solo cambia el array `_files[]`); harness `native_lexer_paridad.py` adaptado (compila el par lexer+keywords con import inline + dir_base); gate de contratos del verificador refinado (`_funciones_en_head()`: función MOVIDA ≠ nueva); bootstrap **S2==S3** (`423008d8…`), **105/105 HM**, S1 **530 passed, 9 skipped**, probes multi-idioma es/en/fr/pt OK (`si`/`if`/`si vrai`/`se verdadeiro`); verificador 0 brechas; **D-9(b) CERRADA** — queda (c) podar `emit_selfhost.py` como siguiente ME de Fase 2. Ver reporte §34.
- **Estado R30:** ✅ **CERRADA** (2026-08-13) — `para` alineado al Manual 2 §2.2 L108 + `siguiente`/`romper` + limpieza regla 12 (resolución de H-R29-1/H-R29-2). Codegen nativo `gen_visitar_para` declara `<tipo> <var> = <init>` (tipo inferido vía `_syn_nativo_expr_tipo_c`, paridad R20/R25/R27) y registra en `_G_fn_vars`/`_G_fn_var_tipos` (patrón R28); `SentenciaPara.nombre` + `cuerpo: ListaNodo` (antes solo la PRIMERA sentencia del bloque); puente NODO_PARA lee el nombre y construye el cuerpo como lista; flatten F8 mapea `SentenciaPara`→45/`SentenciaRomper`→20/`SentenciaSiguiente`→21 + caso `NODO_PARA` en `analizar_sentencia` (scope + declara la variable + analiza init/cond/cuerpo — antes el cuerpo del `para` era INVISIBLE al analizador; use-after-move dentro del bucle ahora E-501 con línea real); walkers de escaneo (D-2/hoisting/ctor-scan) adaptados a ListaNodo; S1 `_parsear_para` al dialecto del Manual (`mientras`, sin `incremento` — el C-style `;` NO existe en el Manual) + `visitar_para` declara la variable con tipo inferido; `siguiente`/`romper` (Manual 2 L115-116) verificados end-to-end en AMBOS compiladores; limpieza regla 12: 21 archivos muertos eliminados (19 scripts `_*.py` de FASE A + fixtures `smoke_backend.syn`/`test_bucle_para.syn`) y dialecto corregido en `test_parser.py`/`test_d5_cobertura.syn`/`examples/synapse/03_concurrencia`; bootstrap S2==S3 (`e52818ac…`, 1.102.625 bytes), **105/105 HM** (101+4 R30), S1 197 passed. Ver reporte §33.
- **Estado R29:** ✅ **CERRADA** (2026-08-12) — modularización de `nucleo/parser.syn` (regla 13, diagnóstico FASE A): split mecánico en 4 módulos (`parser_sentencias.syn` 563 / `parser_declaraciones.syn` 427 / `parser_canales.syn` 58 / `parser.syn` 494), texto idéntico sin cambios de lógica; registro en `principal.syn` (imports + `_files[]`); C byte-idéntico (diff solo el array `_files[]` — prueba de transparencia más fuerte que el bootstrap), bootstrap S2==S3 (`1f3be399…`), **101/101 HM**, S1 196 passed; hallazgos registrados con resolución asignada (regla 11): keyword `continuar` sin parser (`smoke_backend.syn` fixture muerta, nunca compiló — candidata a eliminación regla 12) y codegen `para` nativo `for (0LL; i < 3LL; )` sin declarar la variable de bucle (Manual 2 §2.2 L108 — el dialecto nativo `mientras` es el del manual, el C-style `;` del S1 diverge). Ver reporte §32.
- **Estado R28:** ✅ **CERRADA** (fix `7f8bdff`, 2026-08-12) — instancia ADT ANIDADA en ctors sin anotación (prioridad registrada en R25). Contexto: con solo la instancia SIMPLE registrada por firma (`procesar(r: Resultado<entero,texto>)`), `let r = ok(ok(42))` sin anotación emitía C basura en AMBOS compiladores (nativo rc=5 `struct Resultado` base; S1 rc=1 `int64_t r = (Resultado){...}`) porque los scans de monomorfización (D-2 nativo / `_recolectar_instancias_adt` S1) solo miran firmas + `let` ANOTADOS, nunca ctors en EXPRESIONES. La doc R25/R20 que daba por fallido el caso era stale (solo se probó sin contexto). Fix (paridad S1, 4 frentes): (1) S1 `generator.py` — fixpoint en `_recolectar_instancias_adt` (scan de cadenas de ctors en cuerpos ancladas en instancias registradas, post-orden, `','.join` sin espacio → mangling limpio `Resultado_Resultado_entero_texto_texto`); (2) S1 `emit_control.py` — `visitar_coincidir` registra la variable ligada del caso en `ctx._variables` (solo instancias concretas); (3) nativo `monomorfizacion.syn` — 3 helpers Synapse (`_G_native_adt_registrar_syn` / `_G_native_adt_resolver_ctor` / `_G_native_scan_ctor_exprs` fixpoint ≤4 pasos) invocadas en D-2 (solo bootstrap — lección R5: regenerar `generator.syn`); (4) nativo `nodos_flujo.syn` — `gen_visitar_coincidir` registra el binding del caso en `_G_fn_vars`/`_G_fn_var_tipos` (hallazgo del probe TRIPLE `ok(ok(ok(42)))`: el coincidir del nivel 2 caía al fallback de la primera instancia del base → `int64_t b = (a).dato.ok;` C inválido; el doble pasaba por casualidad). Validación: probes secuenciales (sin raza sobre `synapse_unity.c`) r25shape/auto_con_firma/nested_con_firma/triple — nativo rc=0 + runtime 42 con instancia anidada completa; S1 rc=0 en let y auto (paridad); caso sin firma sigue rc=5/rc=1 (paridad, test R20 intacto); bootstrap **S2==S3** (`e580ffcf…`); suite **101/101 HM** (96+5 R28); S1 **196 passed**; code review: scoping del binding (tabla plana por función, reset en `funciones.syn`; overwrite de homónimo = limitación de shadowing DOCUMENTADA, paridad S1 — `ctx._variables` no restaura tampoco), bounds 63 verificados. Ver reporte §31.
- **Estado R9:** ✅ **CERRADA** (fix `d36dcac` + docs `f0d1d00`) — inmutabilidad REAL de constantes: marcador `es_constante` en `AsignacionVariable` (puente→flatten→`valor_int`), registro en pasada 2 (globales) y pasada 3 (locales), `tabla_buscar` innermost-first. Bootstrap S2==S3 (1068718 bytes, md5 `3862049e`); 129 passed.
- **Estado R10:** ✅ **CERRADA** (fix `d233ee0` + docs `82b6dc5` + hardening `16322da`) — fix en el RUNTIME (`runtime/core/memory.c`): registro `_g_extra_ptrs[]` de los mallocs de escape de `pool_alloc`; `pool_free` solo libera punteros registrados, literales estáticos → no-op (Manual 4 §2.1). Arregla S1 Y nativo. Bootstrap S2==S3 (fix: `fcb2651c`; post-hardening: `1b1cacb0`); 122 passed; 2 tests R10 (18/18 HM).
- **Estado R11:** ✅ **CERRADA** (fix `fe5e7aa` + hardening `695aa57`, 2026-08-10) — exhaustividad `coincidir` nativa cableada (ver sección 2). Diagnóstico observable `faltan variantes ok/err`, ejecución real del switch 42/0, bootstrap S2==S3 (md5 `d78eabac` post-hardening), 26/26 HM (25+1 anti-cuelgue), regresión 176 passed.
- **Estado R1:** ✅ **CERRADA** (fix `d001141`, 2026-08-10) — unificación HM de TVars en llamadas genéricas nativas (residual 2.4). `info_funciones` con TODAS las `DefinicionFuncion` (precedencia de usuario sobre builtins), `validar_llamada_generica` (occurs check + unificador W, AMBIGUOUS/INCOMPATIBLE observables), **fix de raíz**: el marcador R9 en `valor_int` colisionaba con `slot[6]` (SemNodo int64, little-endian) → anulaba el RHS de TODA asignación → movido a `hijo_der`. Bootstrap S2==S3 (md5 `7228b678`), 30/30 HM (26+4), regresión 176 passed. Ver sección 2.
- **Estado R12:** ✅ **CERRADA** (fix `a568600`, 2026-08-10) — préstamos M21.4 nativos (checklist 2.5): el cableado NODO_PUNTERO existía (flatten `hijo_izq`+`valor_int` es_mutable → analizador `registrar_prestamo`) pero 2 bugs lo inutilizaban: (1) `proximo_lifetime=0` en la init de la pasada 3 → primer préstamo `OUTLIVES(0→0)` self-loop → ciclo FALSO en programas válidos; fix: arranca en 1 (el 0 es el lifetime original; paridad S1 objetos-con-identidad vs índices); (2) conflicto sin plantilla ni línea; fix: plantilla S1 (`diagnostics.py` L79) + `linea`/`columna` reales (`ExprObtenerDireccion` en ast_nodes→puente→flatten). Bootstrap S2==S3 (md5 `ce247ef6`), 36/36 HM (30+6), regresión 176. Ver sección 2.
- **Estado R13:** ✅ **CERRADA** (fix `ee7fbb1`, 2026-08-11) — tipos ADT anidados en firmas (residual 2.4, Manual 2 §8.2): el bloqueador era un bug de PARSEO compartido S1+nativo (los parsers consumían hasta el primer `>` sin profundidad → `Resultado<Resultado<entero,texto>,texto>` → «Se esperaba COLON» en S1 y rc=8 en el nativo). Fix de profundidad en los 4 sitios (S1 `_parsear_tipo_parametro`+`tipo_retorno`; nativo `parsear_tipo_compuesto`/`parsear_tipo_retorno`/constructor/alias). Destapó 2 bugs: S1 `es_tipo_conocido` comparaba `len(args)` contra la LISTA `['T','E']` → falsos positivos en argumentos anidados (fix: lista y conteo entero); nativo `validar_tipo_instanciacion` contaba 1 argumento (contador y divisor hasta el primer `>`) → fix con profundidad. 7 probes de paridad, bootstrap S2==S3 (md5 `fab5a61a`), 41/41 HM (36+5), regresión 206 (176+30). Residuales: codegen ADTs anidados (D-2 `Resultado_T`), TVar-en-ADT sin TVar desnudo (S1 estricto, nativo lenient — portar requiere contexto de tvars). Ver sección 2.
- **Estado R14:** ✅ **CERRADA** (fix `38f8100`, 2026-08-11) — use-after-move por envío de canal (Manual 4 §3.3, checklist 2.5 ownership COMPLETO con R12): el cableado estaba INERTE en 3 eslabones — (1) **lexer**: producía `T_FLECHA_IZQ` para `-<` (orden INVERTIDO; la sintaxis real es `<-`, paridad S1 `lexer.py:326 '<-': ARROW_LEFT`) → `ch <- dato` se parseaba como `ch < -dato` (OpBinaria) y el nodo 42 nunca nacía; (2) flatten F8 sin mapeo `SentenciaEnviarCanal`=42 ni rama (canal→ptr_str/ptr_hi, valor→slot[6]); (3) analizador sin ramas `NODO_ENVIAR_CANAL`/`NODO_IDENTIFICADOR` (`tabla_marcar_movido`/`esta_movido` sin call-sites). Fix: lexer `<-` + bloque `-<` muerto eliminado; flatten con línea/columna reales (`Identificador` y `SentenciaEnviarCanal` en ast_nodes→puente, patrón R12); analizador (envío: analiza valor luego marca movido → doble envío E-501; identificador en `analizar_expr`: `esta_movido` → `ERR_SEM_VAR_MOVIDA` E-501); codegen `canal_enviar(canal,(void*)(valor))` (`nodos_flujo.syn` + `generator.syn` regenerado); **aborto global `hay_error` ACTIVADO** (paridad S1 rc=1 — R7 eliminó los 653 falsos positivos, compilar principal.syn da 0 errores). 6 probes de paridad, bootstrap S2==S3 (md5 `fa5bdb9e`), **46/46 HM** (41+5 R14), regresión 206. Residuales: boxeo de primitivos en `canal_enviar` (`_synapse_box_int/float` S1 — deuda D-4), ambigüedad léxica `x<-5` (compartida con S1). Ver sección 2.
- **Estado R16:** ✅ **CERRADA** (fix `68cf9a5`, 2026-08-11) — codegen de ADTs anidados (D-2, Manual 2 §4.2): el scan de monomorfización nativo y el S1 usaban split NAIVE de args (primer `>`) → la instancia interna nunca se registraba → campo `Resultado_T` placeholder (nativo rc=5; S1 compilaba degradado al fallback). Fix en 2 lados: **S1** `_dividir_args_tipo` (split depth-aware en `context.py`), `_registrar` con recursión post-orden + `_mangle_arg`, `emit_declarations.py` ordena los typedefs por PROFUNDIDAD; **nativo** scan D-2 como cola FIFO con post-orden en `orquestador.syn` (deps anidadas antes que el contenedor, guard `_d2pops<4096`). Hallazgo del emisor S1: el heurístico `needs_semi` (generator.py L318-324) añade `;` a líneas asm que terminan en `}` con `{` interno → el `else if ('>')` se parte en 3 `asm()`. Bonus: ADTs builtin `Resultado`/`Opcion` en `_adt_parametros` (regresión preexistente `test_match.py` → 4/4). Structs idénticos S1/nativo, bootstrap S2==S3 (md5 `a18a7ce2`), **51/51 HM** (47+4 R16), regresión **215**. Residuales: **R17** (scan nativo solo firmas — `let r: Resultado<...>` sin firma rc=5), constructores anidados, cross-base, overflow de cola. Ver sección 2.
- **Estado R17:** ✅ **CERRADA** (fix `115f6df`, 2026-08-11) — scan nativo D-2 extendido a `let`/campos/externos (Manual 2 §4.2, paridad total con el S1): el scan solo cubría firmas top-level → instancia ADT solo en `let` local emitía `Resultado_T` (rc=5 nativo); el S1 registraba el `let` pero fallaba en campos por ORDEN de emisión. **Fix (S1 + nativo):** nativo — colección única `_d2all` (firmas retorno+params, `let` con walk recursivo por si/mientras/para/inseguro/coincidir-casos, campos de `DefinicionEstructura`, `DeclaracionExterna`) + cola FIFO `_d2pend[128]` post-orden con guard 124, typedefs de instancias a un **pre-bloque** antes del recorrido top-level, campos de tipo struct por **puntero** (`struct Caja*`, paridad S1 `campos_pointer`); S1 — helper `_emitir_typedefs_instancias` en modos `header`/`completo` antes de las estructuras (loop eliminado de `visitar_declaracion_tipo`). **Hallazgo del code-reviewer** (`emit_control.py` `visitar_coincidir`): el ternario devolvía `''` para todo tipo Synapse → binding `.dato.valor` inválido en TODO `coincidir` sobre ADT (nunca detectado: `test_match.py` no compila con gcc); fix: miembro = ctor del tag + `_dividir_args_tipo`. Probes let/campo/mix/struct-arg rc=0 S1 y nativo (runtime 7 / 42+7), bootstrap S2==S3 (md5 `b56c9b82`), **55/55 HM** (51+4 R17), regresión **211+21**. Ver sección 2.
- **Estado R18:** ✅ **CERRADA** (fix `75c6000`, 2026-08-11) — binding del `coincidir` nativo con multi-instancia del mismo base (Manual 2 §4.2): la heurística "primera instancia" usaba la instancia equivocada con dos instancias del mismo base. **Fix (3 puntos del generador nativo):** `_G_fn_var_tipos` registra el tipo C de **parámetros** (`orquestador.syn`, antes solo el nombre) y de los `let` explícitos (`nodos_flujo.syn` `gen_visitar_declaracion`, paridad S1 L127); el binding resuelve la instancia **EXACTA** por el tipo C de la variable (paridad S1 `tipo_de_expr` + `_instancias_adt`) con fallback. Probe multi-instancia rc=0 (C con `int64_t v`/`CadenaSegura s` por tag), runtime 7+1=8, bootstrap S2==S3 (`f804c52e`), **62/62 HM**. Hallazgos: paréntesis balanceados por línea en comentarios asm (S1 acumulativo), em dash UTF-8 rompe el lexer S1 (bug H26). Residual: constructores anidados `ok(ok(42))` (deuda separada del R16).
- **Modularización M2 (rama DefinicionFuncion):** ✅ **CERRADA** (`36ce8aa`, 2026-08-11, AUDITORIA regla 13) — la rama `DefinicionFuncion` de `gen_visitar_top_level` (242 líneas asm) extraída a `gen_visitar_funcion` en el módulo nuevo `funciones.syn` (253 líneas); orquestador 807→566 líneas; call-site partido en 3 líneas (regla R18 del emisor S1), extern de `_es_builtin_runtime` documentado; C byte-idéntico (md5 `2379e59d`), bootstrap S2==S3 (`13cf8ee0`), 69/69 HM.
- **Modularización M:** ✅ **CERRADA** (`4edc7ff`, 2026-08-11, AUDITORIA regla 13) — `orquestador.syn` 101KB/1350→754 líneas con 3 módulos nuevos (escaneo/monomorfizacion/recorrido, asm textual intacto); C generado **byte-idéntico** (md5 `fb17775c` antes/después), bootstrap S2==S3 (`f8205fcb`), **62/62 HM**; riesgo de mutación de `est` por valor descartado (0 escrituras `est->`/`est.`).
- **Estado R19:** ✅ **CERRADA** (fix `9155120`, 2026-08-11) — divergencia genérico+transferencia (Manual 2 §8.2 + Manual 4 §3.3): `validar_llamada_generica` no tenía rama para `NODO_TRANSFERIDO` (30) → el argumento `->expr` no aportaba su tipo a la unificación → TVar libre → `ERR_SEM_TYPE_AMBIGUOUS` espurio en `identidad(->n)` (rc=7). **Fix:** desenrollado del transferido a su expr envuelta (`hijo_izq`, guard `_g<8`) antes del dispatch de inferencia (paridad S1 `semantic_types.py` L167-168) + guardado previo de `_her` (hermano del argumento real) para conservar la cadena de la llamada. Probe `identidad(->n)` rc=7→rc=5 con 0 errores semánticos (codegen de TVars falla igual en S1, R1); AMBIGUOUS legítimo sigue diagnosticándose; bootstrap S2==S3 (`07b3bbe0`), **65/65 HM** (62+3 R19). Code-reviewer: 4 puntos verificados sin cambios. Ver sección 2.
- **Estado R15:** ✅ **CERRADA** (fix `762cf81`, 2026-08-11) — transferencia de ownership por argumento `->expr` en `lanzar` (Manual 4 §3.3, paridad S1 `semantic_checker.py` L565-568; checklist 2.5 ownership COMPLETO con R12+R14+R15): el cableado estaba INERTE — `NODO_TRANSFERIDO`=30 definido sin uso (el parser nativo nunca producía `ArgumentoTransferido`). Fix en 5 eslabones: (1) **parser** `parser_expr.syn` detecta `T_FLECHA` antes de un argumento → `NODO_TRANSFERIDO` con expr en `hijo_izq` (`arg=0` pre-inicializado por el definite-assignment del S1); (2) **puente** rama `NODO_TRANSFERIDO` → `ArgumentoTransferido(expr)`; (3) **flatten** `_f8_tipo` mapea `ArgumentoTransferido`=30 y `SentenciaLanzar`=18 + ramas (expr→`hijo_izq`, llamada→`slot[6]`); (4) **analizador** rama `NODO_TRANSFERIDO` en `analizar_expr` (lee el expr → E-501 en doble transferencia, paridad `semantic_types.py` L167-168) + rama `NODO_LANZAR` en `analizar_sentencia` (analiza la llamada y marca movido los transferidos; **solo `lanzar`, no llamadas normales** — paridad); (5) **generador** rama `SentenciaLanzar` (llamada directa; thread real del S1 = deuda D-4) + rama `ArgumentoTransferido` en `_oo_expr_a_c` (hallazgo del code-reviewer: sin ella `foo(->dato)` emitía `foo(0)`). 5 probes de paridad, bootstrap S2==S3 (md5 `31cd1a85`), **51/51 HM** (46+5 R15), regresión 206. Divergencias documentadas: genérico+transferencia (`ERR_SEM_TYPE_AMBIGUOUS` espurio potencial), nativo sin "no declarada" en lecturas (preexistente). Ver sección 2.
- **Dependencias bloqueantes:** ninguna técnica.
- **Estado checklist 2.x:** ✅ **2.1/2.2/2.3/2.6 CERRADOS** (2026-08-10) — `sem_error` observable (Manual 2 §10.1), fix de falsos REDEFINICION de constantes espejo entre módulos, newline 2 BS, y **2.6 con la deuda R11 RESUELTA** (coincidir nativo activo); ✅ **2.5 CERRADO (2026-08-11)** — ownership completo: R12 préstamos M21.4 + **R14 use-after-move por envío de canal** (`ERR_SEM_VAR_MOVIDA` E-501 observable con línea real; aborto global `hay_error` activo); ver `docs/reportes/FASE_2_CHECKLIST.md`.
- **Próximo paso:** 🔄 **FASE 3 EN CURSO (2026-08-14)** — ✅ **F3-6 CERRADA**: emisión de canales en el codegen nativo (expr_eval/orquestador/nodos_flujo/funciones/analizador; bootstrap `9d84b3c1`, probe e2e 42 viaja por el canal, regresión `logs/regresion_s1_f36.log`). **Siguiente ME de Fase 3 (mayor importancia técnica): F3-7** — `escuchar` (listen): alinear parser/puente a la gramática del Manual 2 L113 (`escuchar_canal ::= "escuchar" expresion ":" NEWLINE INDENT bloque DEDENT`; hoy se parsea `-> callback`, sintaxis NO documentada) + portar el listener thread del S1 con el fix del `_listener_N` sin declarar (el ejemplo 03_concurrencia está roto en AMBOS compiladores). ✅ **F3-4 CERRADA**: inferencia del `let` sin anotación para builtins no-`int64_t` (seed 17→25 + rama `_G_native_tipo_retorno`; bootstrap `a817533f`, probes `Canal`/`Tensor` rc=0, regresión `logs/regresion_s1_f34.log`). ✅ **F3-2 CERRADA**: externs `_syn_*` de std.io definidos en `io.c` + `abrir`/`leer`/`cerrar` migrados de `synapse_rt.c` (bootstrap `32ea2d4d`, e2e archivos nativo OK, regresión `logs/regresion_s1_f32.log`). ✅ **F3-B1/F3-1 CERRADAS**: inventario B1 (3.1/3.2/3.5/3.6 ✅, 3.4 ⚠️ fibras→Fase 4) + **3.3 CERRADA** — `runtime/core/io.c` creado (único entregable inexistente; I/O extraído de concurrency.c y synapse_rt.c; pipeline.py + principal.syn L623 con 6º `_rt_dir`; bootstrap S2==S3 `16fbbbef`, e2e nativo OK, regresión S1 `logs/regresion_s1_f31.log`). **Siguiente ME de Fase 3: F3-2** — std/io.syn (externs `_syn_abrir/_syn_leer/_syn_escribir/_syn_escribir_linea/_syn_leer_linea` sin definición → no enlaza) + migrar `abrir`/`leer`/`cerrar` (Canal-como-handle) a io.c; luego evaluar el resto de D-9(d) (synapse_rt.c monolito). ✅ **R33 CERRADA (2026-08-13)** — **D-9(c)**: poda de `compilador/generator/emit_selfhost.py` (regla 12): `emitir_generar` MUERTO eliminado (1487 → 1105 líneas, CRLF preservado; reevaluación: `gen_parse` NO muerto — lo llama `emitir_parsear`, oráculo `_P_*` de los harnesses de paridad → conservado); harnesses `native_puente_paridad`/`native_parser_paridad` adaptados (módulos R29/R32 faltantes en los unity builds + `SentenciaPara.cuerpo` ListaNodo R30); paridad frontend 28/28; verificador 0 brechas; **D-9 CERRADA COMPLETA (a/b/c)**. Siguientes pendientes de Fase 2 con resolución asignada: ver checklist AUDITORÍA — D-3 (threads reales, Fase 5), D-4 (contratos 306 funciones, Fase 5), D-5 (frontend único, natural de la poda R33), D-9(e) (módulos cohesivos vigilados por el gate de modularización). ✅ **R32 CERRADA (2026-08-13)** — D-9(b) modularización de `lexer.syn` → `lexer_keywords.syn` (4 tablas keyword es/en/fr/pt, texto byte-idéntico, C byte-idéntico salvo `_files[]`, bootstrap S2==S3 `423008d8`, 105/105 HM, S1 530 passed). ✅ **R30 CERRADA (2026-08-13)** — `para` alineado al Manual 2 §2.2 L108 (codegen declara la variable de bucle en nativo y S1, dialecto S1 corregido del C-style al `mientras` del Manual), `siguiente`/`romper` verificados end-to-end, flatten F8 + analizador procesan el cuerpo del `para` (E-501 observable dentro del bucle), limpieza regla 12 completada (21 archivos muertos eliminados); bootstrap `e52818ac`, **105/105 HM** (101+4 R30), S1 197 passed. ✅ **R29 CERRADA (2026-08-12)** — modularización de `parser.syn` en 4 módulos (C byte-idéntico, bootstrap `1f3be399`, 101/101 HM, S1 196 passed); hallazgos nuevos registrados con resolución asignada: `continuar` sin parser y codegen `para` nativo sin declarar la variable de bucle (Manual 2 §2.2 L108; el S1 C-style diverge del manual). ✅ **R21 CERRADA (2026-08-12, fix `26987fe`)** — línea/columna reales en TODOS los anclas de diagnóstico del flatten F8 (Manual 2 §10.1); ✅ **R22 CERRADA (2026-08-12, fix `6f6e5a6`)** — cuerpo de caso en bloque + coincidir ANIDADO en el parser nativo (Manual 2 L124, probe R20 p3 rc=8 → rc=0; 82/82 HM); ✅ **R23 CERRADA (2026-08-12, fix `603c754`)** — REDEFINICION de función/estructura/externa/constante OBSERVABLE por profundidad (Manual 3 §3.1; dedup solo para módulos importados + pasada first-wins en la ruta unity build + plantilla sem_error + constante estricta + NODO_CONSTANTE con línea + paridad S1 checker ADT/variable; bootstrap `ddf2e0bf`, 87/87 HM, S1 212 passed); ✅ **R24 CERRADA (2026-08-12, fix `d5baf31`)** — ADT builtin implícito (Resultado/Opcion) monomorfizado en firmas NO genéricas del S1 (cierre del hallazgo R22; precarga de builtins en `GeneratorContext.__init__` con regla 'declaración del usuario gana'; probe p2 rc=0 con `Resultado_entero_texto` + ejecución real rc=7; bootstrap sin cambios `ddf2e0bf`, 89/89 HM, S1 216 passed); ✅ **R25 CERRADA (2026-08-12, fix `dfe469e`)** — `let` con ctor ADT sin anotación infiere la instancia monomorfizada en el codegen nativo (cierre del hallazgo R24; rama else-if en `gen_visitar_declaracion` vía `_G_native_adt_inst_ctr` recursivo con fallback ADT simple; probe `let r = ok(7)` imprime 7; bootstrap `dd436c46`, 91/91 HM); ✅ **R26 CERRADA (2026-08-12, fix `3b5ba0a`)** — sintaxis de transferencia de ownership `->` en parámetros del parser nativo (resto del borrow checker S1, Manual 2 L59-60; la DETECCIÓN de UAF ya estaba en R14/R15; fix en 3 capas parser→puente→flatten + test S1 corregido a la sintaxis del Manual con xfail eliminado; probe rc=0; bootstrap `d0cc550d`, 94/94 HM, S1 221 passed); ✅ **R27 CERRADA (2026-08-12, fix `08c4d70`)** — ctor de estructura en `let` sin anotación (hallazgo R26): `Punto()` (Manual 2 L67) infiere `struct Punto` en el codegen nativo (rama R25 extendida con `_G_native_es_estructura`, snprintf acotado; antes C inválido rc=5) y la forma NO documentada `Punto(1,2)` se rechaza con `ERR_SEM_ARGUMENTOS_INVALIDOS` esperados=0 + línea/columna (check en NODO_LLAMADA con guard de precedencia de funciones `_fn27<0`; paridad S1 semantic_types.py L355-360; antes rc=0 mudo); bootstrap `538516a6`, **96/96 HM**, S1 196 passed. Queda por prioridad: la inferencia de la instancia anidada en `let ok(ok(42))` sin anotación quedó **CERRADA en R28** (fixpoint de derivación en ambos scans + registro del binding del caso; 101/101 HM). Próximo: **limpieza de la sesión** — borrar los scripts `_*.py` de depuración y `probes_r28/` del working tree (regla 12: código muerto se elimina) y valorar la modularización pendiente. Histórico: constructores anidados `ok(ok(42))` CERRADO en R20. Histórico: genérico+transferencia CERRADO en R19 (desenrollado del NODO_TRANSFERIDO en la inferencia de argumentos de `validar_llamada_generica`). Histórico: D-2 COMPLETO en R17 (firmas R16 + let/campos/externos R17), binding multi-instancia en R18, modularización M cerrada. El borrow checker S1 quedó COMPLETO con R14+R15. (fase 3, generador — el analizador 2.4 ya los acepta tras R13) o **propagar línea/columna al resto de nodos del flatten** (hoy `ExprObtenerDireccion`, `Identificador`, `SentenciaEnviarCanal` y `SentenciaLanzar` llevan línea real — calidad de diagnósticos). El borrow checker S1 quedó COMPLETO con R14+R15. Nota R12: el riesgo de colisión `es_mutable`/`slot[6]` del NODO_PUNTERO quedó DESCARTADO. Nota R13: residual 2.4 de tipos anidados CERRADO (era parseo, no unificación). Nota R14: el lexer producía `-<` (orden invertido) — la sintaxis de envío es `<-`; la ambigüedad `x<-5` es compartida con S1. Nota R15: el S1 expande los imports inline y reporta los errores de módulo en el punto del import; `arg` en ramas condicionales requiere pre-inicialización (definite-assignment del S1); divergencias documentadas: `lanzar` nativo emite llamada directa (thread real = deuda D-4), genérico+transferencia (`ERR_SEM_TYPE_AMBIGUOUS` espurio potencial), nativo sin "no declarada" en lecturas. Nota R18: hallazgos del build — el parser S1 exige paréntesis balanceados por línea en comentarios `asm` (análisis acumulativo) y el em dash UTF-8 rompe el lexer S1 (bug H26). Nota M: la división de `orquestador.syn` fue mecánica con verificación de autocontenencia (sin `return` ejecutable ni asignación a `r`); el C generado quedó byte-idéntico (prueba de transparencia más fuerte que el bootstrap).

---

## 2. ARQUITECTURA Y DECISIONES CLAVE

- **2026-08-10 — R7 (fix `3e9cb84`):** la pasada 3 del analizador nativo declara los parámetros en el scope de la función; `NODO_ASIGNACION` hace declaración implícita ("primera declaración del scope", paridad S1) y chequea `ERR_SEM_CONSTANTE_INMUTABLE`; `NODO_DECLARACION` reporta REDEFINICIÓN solo del MISMO scope (vía retorno de `tabla_declarar`). **653 falsos positivos «variable no declarada» → 0.**
- **2026-08-10 — R9 (working tree):** inmutabilidad REAL de constantes + scoping. Marcador `es_constante` en `estructura AsignacionVariable` (el puente lo pone a 1 en `NODO_CONSTANTE`; el flatten F8 lo copia a `SemNodo.valor_int`); pasada 2 registra las globales marcadas (las asignaciones globales planas NO, paridad S1); pasada 3 declara las locales y ACTIVA el chequeo `ERR_SEM_CONSTANTE_INMUTABLE` con diagnóstico observable `[Synapse] Error semantico ... No se puede reasignar la constante 'X'` (no aborta: solo `hay_error_2_4`); `tabla_buscar` innermost-first (recorre desde el final; la tabla conserva solo la cadena de scopes — paridad `reversed(_scopes)`). **Lección reaplicada: builds S1 de `nucleo/principal.syn` necesitan timeout ≥ 900s** — el build matado a los 30s dejó un `synapse_stage1.exe` stale (los `_*.c` sí se escribieron, el exe no) que no emitía el diagnóstico; el bootstrap (stage1 viejo compilando los fuentes R9) sí salió correcto. El S1 rechaza un parámetro llamado igual que una constante global por limitación pre-existente de su codegen (macros C `#define X (5)`).
- **2026-08-10 — Checklist 2.x (working tree):** cierre formal 2.1/2.2/2.3. `sem_error` del analizador nativo ahora EMITE el diagnóstico `[Synapse] Error semantico (linea L, columna C): mensaje` (antes mudo; paridad S1 Manual 2 §10.1; excluye CONSTANTE_INMUTABLE con su fprintf propio del R9). **Hallazgo:** el bootstrap emitía ~110 REDEFINICION falsos — las constantes espejo entre módulos del compilador (tokens/lexer/parser_constantes/diagnostics/errores.syn) se concatenan en un solo scope global nativo, mientras el S1 las ve en scopes de módulo separados → fix: duplicado de constante global = lenient (primera gana, sigue inmutable). Newline de los fprintf asm corregido (4 BS→2 BS; lección R5 refinada: en asm normal del analizador 2 BS producen `\n` real en C). **Deudas:** R11 (exhaustividad nativa INERTE — flatten F8 sin `NodoCoincidir`; P1) y R12 (préstamos M21.4 sin diagnóstico en probe; P2).
- **2026-08-11 — R13 (fix `ee7fbb1`):** tipos ADT anidados en firmas (Manual 2 §8.2, residual 2.4). **Hallazgo:** el bloqueador era un bug de PARSEO compartido S1+nativo — los 4 parsers de tipos consumían hasta el primer `>` sin profundidad (`mientras token_mirar != T_MAYOR` / `while ... not GREATER`) → `Resultado<Resultado<entero,texto>,texto>` fallaba en ambos («Se esperaba COLON» S1 / rc=8 nativo). Fix: consumo balanceado `<...>` (solo el `>` de nivel 0 cierra); en S1 `<`/`>` se añaden explícitamente a `partes` (tokens puntuación sin valor); en el nativo variables únicas por sitio (`prof13`/`t13`, `prof13a/t13a`, `prof13b/t13b`) por scoping de función. **2 bugs destapados al desbloquear el parseo:** (1) S1 `es_tipo_conocido` comparaba `len(args)` contra `adt_parametros[nombre]` que es LISTA → `False` para TODO ADT registrado → falsos positivos en argumentos anidados; fix con soporte lista+conteo (los tests unitarios usan `{'Resultado': 2}`); (2) nativo `validar_tipo_instanciacion`: el contador `nargs` y el divisor de argumentos se detenían en el primer `>` → falso «instanciado con 1 argumento(s)»; fix con profundidad. RHS de declaraciones del S1 usa `_parsear_tipo_parametro` → sin asimetría (verificado). **Lección:** los «residuales anti-cuelgue» del parser nativo a veces esconden un bug de PARSEO completo compartido con el S1 — probar con probes de paridad antes de asumir que solo falta «unificación». Bootstrap S2==S3 (md5 `fab5a61a`), 41/41 HM, regresión 206.
- **2026-08-10 — R12 (fix `a568600`):** préstamos M21.4 nativos activados (Manual 4 §4.2, paridad `test_borrowing.py` 6 casos). El cableado NODO_PUNTERO existía pero 2 bugs lo inutilizaban: (1) **ciclo falso**: la pasada 3 inicializaba `proximo_lifetime=0` y la rama crea `OUTLIVES(0→_lt)` con `_lt=proximo_lifetime` → el primer préstamo usaba `_lt=0` → **self-loop 0→0** (el índice 0 es el lifetime original de la función; el S1 usa objetos Lifetime con identidad, el nativo índices enteros → colisión) → `detectar_ciclo_outlives` marcaba ciclo en TODO programa con un préstamo; fix: `proximo_lifetime=1`. (2) **diagnóstico malformado**: el call-site pasaba el nombre crudo a `sem_error` → `: x` sin plantilla ni línea; fix: snprintf con plantilla S1 (`diagnostics.py` L79) + `ExprObtenerDireccion` con `linea`/`columna` (ast_nodes→puente→flatten; antes TODOS los nodos aplanados nacían con línea 0 y todos los diagnósticos semánticos salían con `(linea 0, columna 0)`). Residuales documentados: propagación de línea solo para este nodo (resto con 0), plantilla `diagnostics.syn` L171 divergente sin uso, guard de préstamos `<4096` preexistente. **Lección reutilizada:** cuando un diagnóstico no aparece, instrumentar con probes de paridad ANTES de tocar el cableado — el flatten del Puntero escribe `hijo_izq`+`valor_int` (sin slot[6]), así que el riesgo de colisión del R1 quedó DESCARTADO.
- **2026-08-10 — R1 (fix `d001141`):** unificación HM de TVars en llamadas genéricas nativas. `struct SemFuncionInfo` + `info_funciones` (pasada 2, TODAS las `DefinicionFuncion` — el filtro `es_builtin` previo dejaba `total_fns=1` y la validación nunca disparaba; la precedencia de usuario sobre builtins es la paridad S1 L264-265/L309-310); `validar_llamada_generica` nested C (TVars retorno+parámetros con occurs check, inferencia de argumentos, unificador iterativo W, aridad, `ERR_SEM_TYPE_AMBIGUOUS`/`ERR_SEM_TYPE_INCOMPATIBLE` observables) + hook en `analizar_expr` NODO_LLAMADA (que recursa en operadores/argumentos → llamadas anidadas validadas). **Bug latente de raíz (hallazgo mayor):** en `SemNodo` (int64 por campo), `((int*)&nodo)[6]` = bytes 24-27 = **parte baja de `valor_int`** (little-endian); el flatten de `AsignacionVariable` escribía slot6=expresión y luego el marcador R9 `valor_int=es_constante` sobreescribía el int64 → `slot6=0` → el RHS de TODA asignación se perdía (`analizar_expr` recibía idx=0). Fix: marcador movido a `hijo_der`. strdup del nombre en `info_funciones` (use-after-free). Límites silenciosos del unificador (8 params/8 TVars/worklist 8) documentados; revisión code-reviewer APROBADA.
- **2026-08-10 — R11 (fix `fe5e7aa`):** exhaustividad `coincidir` nativa CABLEADA (antes INERTE: flatten F8 sin `NodoCoincidir`). Cambios por capa con paridad S1: lexer (paréntesis con **lexema real** — patrón A3.1 — antes con valor `""`, los spans multi-token `ok(valor)` daban `len_str` basura 0x6B2D736D y segfault en `puente_str`); parser (`parsear_coincidir` guarda patrón+cuerpo+casos en NODO_CASO + anti-cuelgue de patrones no-nombre); ast_nodes (`NodoCoincidir`/`NodoCaso`); puente (ramas tipadas); flatten F8 (`_f8_tipo` 38/39); analizador (`parsear_patron_coincidir` con **buffers C por puntero**: antes tag/var por VALOR no propagaban y el marcado de variantes quedaba inerte); generador (`gen_visitar_coincidir` switch sobre `.tag` + inferencia de tipo ADT en asignaciones); orquestador (**D-2 escanea retorno+parámetros**: el ADT en un parámetro no se registraba y `traducir_tipo_c` emitía `Resultado_T`; hoisting ME-B7 con tipo ADT para `ok(21)`). Diagnóstico `[Synapse] Error semantico ... coincidir no exhaustivo: faltan variantes ok/err` (no aborta, lenient por diseño); ejecución real 42/0; bootstrap S2==S3 (md5 `d78eabac` post-hardening), ruido 0; 4 tests R11 + 1 anti-cuelgue (26/26 HM); regresión 176 passed. **Hardening post-revisión (`695aa57`):** bounds `_tp < 63`/`_vp < 63` en `parsear_patron_coincidir` (un tag de >63 chars desbordaba el stack del compilador) + test `test_r11_patron_literal_no_cuelga`; el revisor descartó como no-bug el aliasing de buffers estáticos (tabla_declarar hace strdup) y verificó el wildcard → `default:`. Pendiente residual: patrones literales sin codegen (RC=5 al ejecutar, sin cuelgue).
- **2026-08-10 — R10 (fix `d233ee0` + docs `82b6dc5` + hardening `16322da`):** RAII sobre literales estáticos (0xC0000374). Causa raíz: `_syn_texto_liberar(s) → pool_free(s.datos)` y el fallback de `pool_free` para punteros fuera de slabs/pool era `free(ptr)` — legítimo para los mallocs de escape de `pool_alloc` pero fatal para literales estáticos (`.rodata`). Fix en el RUNTIME (único punto para S1, nativo, bootstrap y programas de usuario): registro `_g_extra_ptrs[]` (protegido por `_g_pool_mutex`) donde `pool_alloc` registra cada puntero de sus 3 rutas de malloc de escape; `pool_free` solo `free()` a punteros registrados (los consume) — **literal estático / puntero ajeno → no-op** (Manual 4 §2.1: nunca liberar lo que no se asignó vía el allocator); `pool_destroy` libera el registro. **Lección R10 (deadlock):** la primera versión usaba un helper `_extra_consumir` que re-tomaba `_g_pool_mutex` (ya tomado en `pool_free`) → los probes colgaban (timeout); corregido con scan INLINE bajo el mutex ya tomado y `free()` tras liberarlo; helper eliminado (código muerto). **Hardening post-revisión (`16322da`):** `pool_init` resetea `_g_extra_count=0` (re-init sin destroy no conserva entradas stale) + contrato de ownership documentado en `pool_free`; bootstrap post-hardening S2==S3 `1b1cacb0`.
- **2026-08-10 — R8 (commits `8136fd8` + `4adbee7`):** `log(...)` → puente crea `LogLlamada` → el generador nativo no lo manejaba → fallback `0;`. Fix: `gen_visitar_log` en `nucleo/generador/nodos_flujo.syn` (paridad S1 `visitar_log` de `emit_expressions.py`): `printf` con `%s`+`.datos` para texto, `%f` para decimal, `%d` para el resto; dispatch en `gen_visitar_expr` + rama defensiva en `gen_visitar_stmt_generico`. **Regenerar `nucleo/generator.syn` con `_rebuild_generator.py` SIEMPRE después de editar módulos de `nucleo/generador/`.**
- **2026-08-09 — R5 (fix `54f5ee7`):** el pipeline nativo aborta con `{1,8}` (rc=8) en errores de parseo (paridad S1), vía wrapper `parsear()` + global `_G_parse_error`.
- **2026-08-09 — R2 (fix `8f9dc54`):** anti-cuelgue del parser nativo ante tipos anidados (`A<B<C>,D>`): `parsear_funcion` verifica el retorno de `parsear_tipo_retorno` + fallback `token_avanzar` en los 7 bucles de cuerpo.
- **2026-08-09 — Port nativo 2.4 (fix `b7cd505`):** validación Hindley-Milner (aridad/base/argumentos de ADT) en `nucleo/analizador_semantico.syn` con flag dedicado **`hay_error_2_4`** (aborta rc=7; el aborto global `hay_error` rompía el bootstrap). Flatten F8: **root reservado en idx 0**; `Parametro.tipo_param` se lee vía `ptr_extra` (el puente llena `tipo_param`, NO `tipo` — causa raíz de los 653 falsos positivos originales); `nodo_cadena_retorno` usa `strdup` (fix del free sobre buffer estático).
- **Decisión de arquitectura del pipeline:** S1 (Python, `compilador/`) y nativo (`nucleo/*.syn` auto-compilado) deben mantener **paridad de comportamiento**; el bootstrap es **S1→S2→S3 con S2==S3 byte-idénticos** (criterio de aceptación de cada cierre).
- **Gobernanza:** cada entrega debe referenciar Manual X Sección Y, pasar la regresión y documentarse en el reporte `docs/reportes/FASE_2_2.4_NATIVA.md` + bitácora `docs/AUDITORIA_ALINEACION_MANUALES.md`.

---

## 3. ERRORES Y SOLUCIONES (con logs)

- **Error:** `ERR_SEM_EXHAUSTIVE_MATCH_REQUIRED redefined` (warning GCC) en `_principal.c`, `_lexer.c`, `_diagnostics.c` de la compilación modular.
  - **Contexto:** `_etapa1.log`; macros ERR_* emitidas en varios módulos con `#ifndef` incompleto en compilación modular histórica.
  - **Solución aplicada:** el emisor S1 emite los ERR_* con guards `#ifndef` (`generator.py` `_emitir_error_defines`); es un warning pre-existente, no bloqueante. Verificar que no reaparezca como error al tocar la taxonomía.
- **Error (R9):** el build de `synapse_stage1.exe` vía S1 fue matado por el timeout de 30s del basher → quedó un stage1 STALE (timestamp antiguo) que no emitía el diagnóstico R9 aunque los `_*.c` intermedios (escritos antes del kill) sí tenían el fix. Síntoma: stage2/3 correctos (el stage1 viejo los compiló desde los fuentes R9) pero stage1 roto.
  - **Contexto:** sesión R9; `python main.py nucleo/principal.syn -o synapse_stage1.exe` tarda 2-5 min.
  - **Solución aplicada:** relanzar el build con `timeout_seconds: 900`; verificar con `ls -la --time-style=full-iso synapse_stage1.exe` (timestamp NUEVO) + prueba de comportamiento. **Regla: NUNCA asumir que un build S1 terminó solo por `tail` del log; comprobar timestamp y comportamiento.**
- **Error:** `warning: argument 1 range [18446744056529682440, ...] exceeds maximum object size [-Walloc-size-larger-than=]` en `synapse_rt.c:3393` (`malloc((size_t)vs * sizeof(PV))`).
  - **Contexto:** aparece en CADA build de stage1 (warnings del runtime, no de nuestro código). No bloqueante.
  - **Solución aplicada:** ignorar; no modificar `synapse_rt.c`.
- **Error:** instrumentación temporal con comillas SIN escapar dentro de `asm()` → el lexer S1 no lexeaba `nucleo/analizador_semantico.syn` (bloqueante durante R7).
  - **Contexto:** sesión R7; patrón `asm("fprintf(stderr, "...")")` roto.
  - **Solución aplicada:** escapado canónico `\"` dentro del string del `.syn` (doble escapado Synapse→C). **Regla: verificar SIEMPRE con `Lexer(...).tokenizar()` el archivo .syn editado antes de compilar.**
- **Error:** `asm() solo puede usarse dentro de un bloque 'inseguro:'` (6 errores semánticos S1) al compilar el unity regenerado.
  - **Contexto:** sesión R8; el helper generaba las líneas `asm(...)` con 12 espacios de indentación (el archivo usa 8 dentro de `inseguro:`).
  - **Solución aplicada:** corregir la indentación a 8 espacios y re-aplicar el fix.
- **Error (R10, RESUELTO):** el exe de un programa de usuario con variable texto (`saludo = "hola"`) crasheaba con **0xC0000374 (heap corruption)** al salir; exit 127 en git-bash.
  - **Contexto:** sesión R8; `_syn_texto_liberar(saludo)` (RAII) liberaba un literal estático. **Pre-existente** (afectaba igual a `escribir_linea`); el S1 también crasheaba al reasignar (`s = "a"; s = entero_a_texto(7)`).
  - **Solución aplicada (2026-08-10):** fix en `runtime/core/memory.c` — registro `_g_extra_ptrs[]` de los mallocs de escape de `pool_alloc`; `pool_free` solo libera punteros registrados; literales estáticos → no-op. Probes: nativo y S1 rc=0 (`hola`/`7`); estrés 100 it rc=0.

---

## 4. LECCIONES APRENDIDAS

- **2026-08-10 — Spans multi-token del lexer nativo:** `nodo_guardar_span` reconstruye el lexema por resta de punteros entre tokens; si un token se crea con valor `""` (literal estático) en vez del slice real de la fuente (p. ej. paréntesis), la resta da basura (0x6B2D736D) y `puente_str` segfaulta. **Regla: todo token debe conservar el lexema real del buffer de la fuente** (patrón `lexer_push_token_punt`, A3.1).
- **2026-08-10 — Parámetros de salida en Synapse nativo:** una función no puede "devolver" una `CadenaSegura` por valor si las asignaciones internas deben propagar al caller (las estructuras se copian por valor). **Regla: usar buffers C por puntero** (`char*` estáticos pasados por referencia) y construir la `CadenaSegura` en el caller (patrón de `parsear_patron_coincidir`).
- **2026-08-10 — El scan D-2 del orquestador debe cubrir TODA la firma:** solo escaneaba `tipo_retorno`; un ADT solo presente en los parámetros (`r: Resultado<entero,texto>`) no se registraba y `traducir_tipo_c` caía al placeholder `Resultado_T` (tipo indefinido, rc=5 GCC). **Regla: retorno Y parámetros.**
- **2026-08-10 — Escapado en `.syn`:** para que el C emitido contenga `\n` (backslash+n) en un string, el `.syn` necesita `\\n` (2 BS); si el string va en un **array C embebido**, se necesitan `\\\\n` (4 BS, el array contenía un newline real con 2 BS). Confirmado empíricamente con tests de lexer S1.
- **2026-08-10 — `nucleo/generator.syn` es un UNITY regenerado:** editar cualquier `nucleo/generador/*.syn` sin ejecutar `python nucleo/_rebuild_generator.py` produce un bootstrap sin los cambios (link error). El script tiene orden de concatenación estricto (contexto → emision_c → expr_eval → nodos_flujo → frontend_nativo → orquestador).
- **2026-08-10 — Los builds completos tardan >30 s:** `python main.py nucleo/principal.syn -o synapse_stage1.exe` necesita timeout ≥ 900 s en el basher; con timeout corto el exe queda STALE (verificar con `strings synapse_stage1.exe | grep -c simbolo`).
- **2026-08-09 — El aborto semántico global rompe el bootstrap:** el analizador nativo genera ruido pre-existente; usar flags dedicados (`hay_error_2_4`) y abortar solo para la validación en curso.
- **2026-08-09 — El puente `puente_ast.syn` es la fuente de verdad de nombres de campo:** los nodos aplanados no siempre usan `tipo` (p.ej. `Parametro.tipo_param` vía `ptr_extra`); verificar contra el flatten F8 en `principal.syn` antes de asumir.
- **2026-08-09 — S1 `semantic_checker.py` es el espejo del analizador nativo:** para paridad de comportamiento, consultar primero cómo lo hace el S1 (`_analizar_funcion`, `_analizar_sentencia`).

---

## 5. TAREAS PENDIENTES

- [x] R2 — anti-cuelgue parser nativo (tipos anidados) — commit `8f9dc54`
- [x] R5 — pipeline nativo aborta en errores de parseo (rc=8) — commit `54f5ee7`
- [x] R7 — 653 falsos positivos «variable no declarada» → 0 — commit `3e9cb84`
- [x] R8 — `log(...)` emite `printf` — commits `8136fd8` + `4adbee7` (2026-08-10)
- [x] R9 — constantes `StmtConstante` reales: marcador `es_constante` (puente→flatten→`valor_int`), pasada 2/3 con `es_constante=verdadero`, `tabla_buscar` innermost-first — bootstrap S2==S3 `3862049e`, 129 tests (fix `d36dcac` + docs `f0d1d00`)
- [x] R10 — RAII sobre literales estáticos (0xC0000374): fix en `runtime/core/memory.c` (registro de punteros fuera-del-pool; literales → no-op) + hardening `pool_init`/contrato ownership — bootstrap S2==S3 (`fcb2651c` → post-hardening `1b1cacb0`), 122 tests (fix `d233ee0`, docs `82b6dc5`, harden `16322da`)
- [x] Checklist 2.x — 2.1 scopes / 2.2 pasadas / 2.3 taxonomía CERRADOS (sem_error observable + fix constantes espejo lenient; 3 tests; bootstrap `6814fddc`); 2.6 CERRADO con R11 (ver abajo); 2.5 PARCIAL (R12)
- [x] R11 — exhaustividad nativa cableada: `NodoCoincidir` en flatten F8 + parser/ast_nodes/puente/analizador (buffers por puntero)/generador (switch `.tag`)/D-2 (parámetros) — fix `fe5e7aa` + harden `695aa57` (bounds + anti-cuelgue), bootstrap `d78eabac`, 26/26 HM, 176 passed
- [x] R12 — préstamos M21.4 nativos: `proximo_lifetime=1` (fix ciclo falso) + plantilla S1 + línea real — commit `a568600` (2026-08-10), bootstrap `ce247ef6`, 36/36 HM, 176 passed; riesgo `es_mutable`/`slot[6]` DESCARTADO (el flatten del Puntero escribe `hijo_izq`+`valor_int`)
- [x] R1 — TVars nativo (unificación HM en llamadas genéricas) — commit `d001141` (2026-08-10), bootstrap `7228b678`, 30/30 HM, 176 passed
- [x] R13 — tipos ADT anidados en firmas (residual 2.4, Manual 2 §8.2): parseo balanceado `<...>` S1+nativo (4 sitios) + fix S1 `es_tipo_conocido` (lista vs int) + fix nativo aridad con profundidad — commit `ee7fbb1` (2026-08-11), bootstrap `fab5a61a`, 41/41 HM, 206 passed
- [x] R17 — scan nativo D-2 completo (`let`/campos/externos): colección única `_d2all` + cola FIFO `_d2pend[128]` + pre-bloque de typedefs + campos por puntero (S1 `_emitir_typedefs_instancias` en header/completo) + fix binding del `coincidir` S1 (hallazgo del code-reviewer) — commit `115f6df` (2026-08-11), bootstrap `b56c9b82`, 55/55 HM, regresión 211+21
- [x] R18 — binding del `coincidir` nativo con multi-instancia: `_G_fn_var_tipos` con tipo C de parámetros y `let` explícitos + instancia EXACTA por tipo de variable — commit `75c6000` (2026-08-11), bootstrap `f804c52e`, 62/62 HM
- [x] M — modularización de `orquestador.syn` (1350→754 líneas + escaneo/monomorfizacion/recorrido): C byte-idéntico (md5 `fb17775c`), bootstrap `f8205fcb`, 62/62 HM — commit `4edc7ff` (2026-08-11)
- [x] R19 — divergencia genérico+transferencia: TVars desde `->expr` (desenrollado del NODO_TRANSFERIDO en `validar_llamada_generica`) — commit `9155120` (2026-08-11), bootstrap `07b3bbe0`, 65/65 HM
- [x] R20 — constructores anidados `ok(ok(42))`: ctor como argumento aporta su tipo (`_syn_nativo_expr_tipo_c` + resolución por tipo del argumento) + parser del `let` con profundidad `< >` — commit `6e903b8` (2026-08-11), bootstrap `925b9046`, 69/69 HM
- [x] R21 — línea/columna reales en el flatten F8 (9 nodos más: DefinicionFuncion/Estructura/Externa/Tipo, AsignacionVariable, DeclaracionVariable, NodoCoincidir, LlamadaFuncion, Parametro) — commit `26987fe` (2026-08-12), bootstrap S2==S3 (sha256 `62e4647f…`), 76/76 HM; hallazgo: REDEFINICION función/estructura/externa silenciosa por el dedup del unity merge (paridad S1)
- [x] Coincidir ANIDADO + cuerpo de caso en BLOQUE en el parser nativo (Manual 2 L124: `caso_coincidir ::= patron "=>" ( sentencia | NEWLINE INDENT bloque DEDENT )`) — commit `6f6e5a6` (2026-08-12), bootstrap S2==S3 (sha256 `96f7f21e…`), 82/82 HM; hallazgo S1: codegen de coincidir en funciones NO genéricas emite `Resultado_T` (preexistente)
- [x] R23 — REDEFINICION de función/estructura/externa/constante OBSERVABLE: dedup por profundidad (`_stk_n > 1` solo módulos importados) + pasada first-wins en la ruta Unity Build del self-hosted + plantilla `sem_error` + constante global estricta (leniency R9 retirada) + `NODO_CONSTANTE` con línea/columna + paridad S1 checker (ADT/variable) — commit `603c754` (2026-08-12), bootstrap `ddf2e0bf`, 87/87 HM, S1 212 passed (cierra el hallazgo R21)
- [x] R24 — ADT builtin implícito (Resultado/Opcion) monomorfizado en firmas NO genéricas del S1: precarga de builtins en `GeneratorContext.__init__` (regla 'declaración del usuario gana') + tests S1/HM — commit `d5baf31` (2026-08-12), bootstrap `ddf2e0bf`, 89/89 HM, S1 216 passed (cierra el hallazgo R22); hallazgo registrado: `let r = ok(7)` sin anotación (instancia no resuelta en el codegen nativo)
- [x] R25 — `let` con ctor ADT sin anotación infiere la instancia monomorfizada en el codegen nativo: rama else-if en `gen_visitar_declaracion` (nodos_flujo.syn) vía `_G_native_adt_inst_ctr` recursivo + fallback ADT simple + 3 tests HM — commit `dfe469e` (2026-08-12), bootstrap `dd436c46`, 91/91 HM (cierra el hallazgo R24); caso ambiguo de paridad: `ok(ok(42))` en `let` sin anotación (con anotación funciona)
- [x] R26 — Uso-after-move / resto del borrow checker S1: la DETECCIÓN de UAF ya estaba portada en R14/R15 (E-501 canal/lanzar); portada la sintaxis de transferencia de ownership `->` en parámetros (Manual 2 L59-60) al parser nativo (parser→puente→flatten) + test S1 corregido a la sintaxis del Manual (xfail eliminado) + 2 tests HM — commit `3b5ba0a` (2026-08-12), bootstrap `d0cc550d`, 94/94 HM, S1 221 passed; hallazgo registrado: ctor de estructura en `let` sin anotación (→ R27 CERRADA)
- [x] R27 — Ctor de estructura en `let` sin anotación (hallazgo R26): `Punto()` (Manual 2 L67) infiere `struct Punto` en el codegen nativo (rama R25 extendida con `_G_native_es_estructura`, snprintf acotado) y la forma NO documentada `Punto(1,2)` se rechaza con `ERR_SEM_ARGUMENTOS_INVALIDOS` esperados=0 + línea/columna reales (check en NODO_LLAMADA con guard de precedencia de funciones) + 2 tests HM — commit `08c4d70` (2026-08-12), bootstrap `538516a6`, 96/96 HM, S1 196 passed
- [x] R29 — Modularización de `nucleo/parser.syn` (regla 13): split mecánico en 4 módulos (parser_sentencias/parser_declaraciones/parser_canales), texto idéntico, C byte-idéntico (diff solo `_files[]`), bootstrap `1f3be399`, 101/101 HM, S1 196 passed; hallazgos registrados (continuar sin parser, codegen para) — commit `1511843` (2026-08-12)
- [x] R28 — Instancia ADT anidada en ctors sin anotación (`ok(ok(42))`/`ok(ok(ok(42)))`): fixpoint de derivación en los scans de monomorfización (D-2 nativo con 3 helpers / `_recolectar_instancias_adt` S1) + registro de la variable ligada del caso en el coincidir (S1 `ctx._variables` / nativo `_G_fn_vars`; el probe triple delató el fallback de la primera instancia del base) + 5 tests HM — commit `7f8bdff` (2026-08-12), bootstrap `e580ffcf`, 101/101 HM, S1 196 passed; caso sin firma sigue fallando en ambos (paridad R20); limitación de shadowing del binding documentada (paridad S1)
- [x] Calidad de diagnósticos: propagar línea/columna al resto de nodos del flatten — R21 CERRADA (commit `26987fe`, 2026-08-12); residual: nodos sin anclaje de diagnóstico sin línea (ver reporte §24)
- [ ] Checklist auditoría 2.6 cerrado; queda 2.5 (R12) — `docs/AUDITORIA_ALINEACION_MANUALES.md` sección 3

---

## 6. PRÓXIMOS PASOS CONCRETOS

- ✅ **R33 CERRADA (2026-08-13)** — **D-9(c)**: poda de `compilador/generator/emit_selfhost.py` (regla 12) — `emitir_generar` (L1106-1487, ~380 líneas) MUERTO eliminado (1487 → 1105, CRLF preservado); reevaluación: `gen_parse` NO era muerto (lo llama `emitir_parsear`, oráculo `_P_*` vivo) → conservado; harnesses `native_puente_paridad`/`native_parser_paridad` adaptados (módulos R29/R32 en los unity builds + `SentenciaPara.cuerpo` como ListaNodo R30); paridad frontend 28/28; verificador 0 brechas. **D-9 CERRADA COMPLETA** (a/b/c). Queda de Fase 2 por prioridad: ver próximos pasos.
- ✅ **R32 CERRADA (2026-08-13)** — **D-9(b)**: modularización de `nucleo/lexer.syn` → `lexer_keywords.syn` (4 tablas keyword es/en/fr/pt; split mecánico texto byte-idéntico; C byte-idéntico salvo `_files[]`; bootstrap `423008d8`; 105/105 HM; S1 530 passed; harness paridad adaptado; gate de contratos refinado). **Siguiente de la D-9: (c) podar `emit_selfhost.py`** — CERRADA en R33.
0. ✅ **R31 CERRADA (2026-08-13)** — verificador mecánico de alineación (`auditoria/verificar_alineacion.py`): 0 brechas en 64 filas/28 reportes/39 hashes; 9 hallazgos reales resueltos (8 hashes FASE A + fila CK + typo a23). **R31b ENFORCEMENT (mismo día)**: gates obligatorios en 2 capas — CI job `auditoria` (infalible, todo push/PR) + pre-commit hook local (`githooks/`); verificador ampliado con gates de deuda sin registrar (regla 11), código muerto `_*.py` raíz (regla 12), módulos >1200 líneas sin D-9 (regla 13) y contratos en funciones NUEVAS (Manual 2 §12); `generator.py`/`analizador_semantico.syn` evaluados cohesivos y registrados en D-9(e). **Integrarlo en el flujo**: `python auditoria/verificar_alineacion.py` DEBE reportar 0 brechas antes de cada commit (el hook lo fuerza localmente y el CI lo fuerza en remoto) — los reportes nuevos deben nacer con `HASH COMMIT` real (convención: commitear y luego actualizar el hash en el reporte/bitácora, como hacen F1.2d/F1.4/A3.2).
1. **Commit del checklist 2.x** (protocolo de esta sesión): fix `nucleo/analizador_semantico.syn` + tests `tests/test_fase2_nativa_hm.py` (+3) en un commit; luego docs (reporte `FASE_2_CHECKLIST.md`, checklist AUDITORIA 2.1-2.6, bitácora fila F2-2.4d-CK, memoria) con el hash real en otro commit.
2. **R11 — exhaustividad nativa** ✅ **CERRADA** (2026-08-10, fix `fe5e7aa` + harden `695aa57`, docs en el mismo tramo): cableado completo del `coincidir` (flatten F8 + parser + analizador con buffers por puntero + generador switch `.tag` + D-2 parámetros); diagnóstico observable, ejecución real 42/0, bootstrap `d78eabac`, 26/26 HM, 176 passed.
3. ✅ **R12 — préstamos M21.4 nativos** **CERRADA** (2026-08-10, fix `a568600`): `proximo_lifetime=1` (fix del ciclo falso) + plantilla S1 del conflicto + línea real (`ExprObtenerDireccion` con linea/columna); bootstrap `ce247ef6`, 36/36 HM, 176 passed. Riesgo `es_mutable`/`slot[6]` DESCARTADO (el flatten del Puntero escribe `hijo_izq`+`valor_int`).
4. ✅ **R1 — TVars nativo** **CERRADA** (2026-08-10, fix `d001141`): unificación de TVars de función con occurs check + fix de raíz de la colisión `slot[6]`/`valor_int`; bootstrap `7228b678`, 30/30 HM, 176 passed.
5. ✅ **R13 — tipos ADT anidados nativos** **CERRADA** (2026-08-11, fix `ee7fbb1`): parseo balanceado S1+nativo + `es_tipo_conocido` + aridad nativa con profundidad; bootstrap `fab5a61a`, 41/41 HM, 206 passed.
6. **Uso-after-move / resto del borrow checker S1** (`test_borrow_checker.py` 5 + `test_ownership.py` 3): port futuro del borrow checker completo.
7. ✅ **R21 — Calidad de diagnósticos** **CERRADA** (2026-08-12, fix `26987fe`): línea/columna reales en TODOS los anclas de diagnóstico del flatten F8 (9 nodos más; Manual 2 §10.1); bootstrap S2==S3 (sha256 `62e4647f…`), 76/76 HM.
8. ✅ **R22 — Cuerpo de caso en bloque + coincidir ANIDADO en el parser nativo** **CERRADA** (2026-08-12, fix `6f6e5a6`): gramática `caso_coincidir ::= patron "=>" ( sentencia | NEWLINE INDENT bloque DEDENT )` (Manual 2 §2.4 L124) implementada en AMBOS parsers (nativo forma bloque + guard de columna; S1 `_parsear_bloque`); probe R20 p3 rc=8 → rc=0, switch anidado ejecuta → 42, anidado no exhaustivo con línea real; bootstrap S2==S3 (sha256 `96f7f21e…`), 82/82 HM. Próximo por prioridad: revisar el dedup `_seen_sym` del unity merge (REDEFINICION de función/estructura/externa hoy silenciosa) + hallazgo S1 `Resultado_T` en coincidir no genérico.
