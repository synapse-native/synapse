#lang: es
importar std.cluster

funcion principal() -> entero:
    par = cluster_generar_par_claves()
    mensaje = "Prueba de firma"
    firma = cluster_firmar_mensaje(mensaje, par)
    valido = cluster_verificar_firma(mensaje, firma, par)
    si valido != 0:
        retornar 1
    retornar 0